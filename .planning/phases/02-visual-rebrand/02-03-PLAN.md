---
phase: 02-visual-rebrand
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/en/translation.json
  - src/i18n/locales/es/translation.json
  - src/i18n/locales/fr/translation.json
  - src/i18n/locales/vi/translation.json
autonomous: true
requirements: [VISU-04, ONBR-01, LANG-01]

must_haves:
  truths:
    - "No visible 'Handy' text appears in the UI when running in any of the four supported locales"
    - "Onboarding permission description uses Dictus-branded privacy-aware tone"
    - "App language selector description says 'Dictus' not 'Handy'"
    - "Shortcut title says 'Dictus Shortcuts' not 'Handy Shortcuts'"
  artifacts:
    - path: "src/i18n/locales/en/translation.json"
      provides: "English locale with all 11 Handy references replaced"
    - path: "src/i18n/locales/es/translation.json"
      provides: "Spanish locale with all Handy references replaced"
    - path: "src/i18n/locales/fr/translation.json"
      provides: "French locale with all Handy references replaced"
    - path: "src/i18n/locales/vi/translation.json"
      provides: "Vietnamese locale with all Handy references replaced"
  key_links:
    - from: "src/i18n/locales/en/translation.json"
      to: "src/components/onboarding/Onboarding.tsx"
      via: "t('onboarding.permissions.description')"
      pattern: "onboarding.permissions.description"
    - from: "src/i18n/locales/en/translation.json"
      to: "src/components/settings/AppLanguageSelector.tsx"
      via: "t('appLanguage.description')"
      pattern: "appLanguage.description"
---

<objective>
Replace all "Handy" references in all four locale files (en, es, fr, vi) with Dictus-branded copy that matches the iOS app's warm, direct, privacy-aware tone.

Purpose: Eliminates all visible Handy text strings from the UI across all supported languages. Also addresses LANG-01 by clarifying the language selector description.
Output: Updated translation.json files for en, es, fr, vi.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-rebrand/02-CONTEXT.md
@.planning/phases/02-visual-rebrand/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Handy references in English locale</name>
  <files>src/i18n/locales/en/translation.json</files>
  <read_first>
    - src/i18n/locales/en/translation.json (full file — find all 11 "Handy" occurrences)
    - ../dictus/DictusApp/Localizable.xcstrings (iOS tone reference — read first 200 lines for tone examples)
  </read_first>
  <action>
Replace all 11 "Handy" occurrences in `src/i18n/locales/en/translation.json` with Dictus-branded equivalents. The tone should match the iOS app: warm, direct, privacy-aware.

**Exact key-by-key replacements (English):**

1. `onboarding.permissions.description`:
   - FROM: "Handy needs a couple of permissions to work properly."
   - TO: "Dictus needs a couple of permissions to work properly. Your recordings stay on your device."

2. `settings.general.shortcut.title`:
   - FROM: "Handy Shortcuts"
   - TO: "Dictus Shortcuts"

3. `settings.advanced.autostart.description`:
   - FROM: "Automatically start Handy when you log in to your computer."
   - TO: "Automatically start Dictus when you log in to your computer."

4. `settings.advanced.showTrayIcon.description`:
   - FROM: "Display the Handy icon in the system tray."
   - TO: "Display the Dictus icon in the system tray."

5. `settings.debug.updateChecks.description`:
   - FROM: "Automatically check for new versions of Handy"
   - TO: "Automatically check for new versions of Dictus"

6. `settings.about.version.description`:
   - FROM: "Current version of Handy"
   - TO: "Current version of Dictus"

7. `settings.about.appDataDirectory.description`:
   - FROM: "Location where Handy stores its data"
   - TO: "Location where Dictus stores its data"

8. `settings.about.supportDevelopment.description`:
   - FROM: "Help us continue building Handy"
   - TO: "Help us continue building Dictus"

9. `settings.about.acknowledgments.whisper.details`:
   - FROM: "Handy uses Whisper.cpp for fast, local speech-to-text processing. Thanks to the amazing work by Georgi Gerganov and contributors."
   - TO: "Dictus uses Whisper.cpp for fast, local speech-to-text processing. Thanks to the amazing work by Georgi Gerganov and contributors."

10. `accessibility.permissionsDescription`:
    - FROM: "Handy needs accessibility permissions to type transcribed text."
    - TO: "Dictus needs accessibility permissions to type transcribed text."

11. `appLanguage.description` (LANG-01):
    - FROM: "Change the language of the Handy interface"
    - TO: "Choose the language for the Dictus interface. Auto follows your system language."

Preserve all JSON structure, indentation, and surrounding keys exactly.
</action>
<verify>
<automated>grep -c "Handy" src/i18n/locales/en/translation.json && grep "Dictus Shortcuts" src/i18n/locales/en/translation.json && grep "Choose the language for the Dictus interface" src/i18n/locales/en/translation.json</automated>
</verify>
<acceptance_criteria> - `grep -c "Handy" src/i18n/locales/en/translation.json` returns 0 - `grep -c "Dictus" src/i18n/locales/en/translation.json` returns at least 11 - File contains `"Dictus Shortcuts"` (key: settings.general.shortcut.title) - File contains `"Choose the language for the Dictus interface. Auto follows your system language."` (key: appLanguage.description) - File contains `"Your recordings stay on your device"` (onboarding privacy message) - File is valid JSON (no parse errors)
</acceptance_criteria>
<done>All 11 Handy references replaced with Dictus-branded English copy. Tone matches iOS: warm, direct, privacy-aware. LANG-01 description clarified.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Handy references in es, fr, vi locales</name>
  <files>src/i18n/locales/es/translation.json, src/i18n/locales/fr/translation.json, src/i18n/locales/vi/translation.json</files>
  <read_first>
    - src/i18n/locales/es/translation.json (find all "Handy" occurrences)
    - src/i18n/locales/fr/translation.json (find all "Handy" occurrences)
    - src/i18n/locales/vi/translation.json (find all "Handy" occurrences)
    - src/i18n/locales/en/translation.json (reference — English values just updated in Task 1)
  </read_first>
  <action>
For each of the three locale files (es, fr, vi), replace all "Handy" occurrences with Dictus-branded translations. The same 11 keys need updating in each file. "Dictus" is a proper noun — it stays as "Dictus" in all languages.

**Spanish (es/translation.json) — Key-by-key:**

1. `onboarding.permissions.description` — Replace "Handy" with "Dictus". Add privacy note: "Tus grabaciones se quedan en tu dispositivo."
2. `settings.general.shortcut.title` — "Atajos de Dictus" (was "Atajos de Handy" or similar)
3. `settings.advanced.autostart.description` — Replace "Handy" with "Dictus"
4. `settings.advanced.showTrayIcon.description` — Replace "Handy" with "Dictus"
5. `settings.debug.updateChecks.description` — Replace "Handy" with "Dictus"
6. `settings.about.version.description` — Replace "Handy" with "Dictus"
7. `settings.about.appDataDirectory.description` — Replace "Handy" with "Dictus"
8. `settings.about.supportDevelopment.description` — Replace "Handy" with "Dictus"
9. `settings.about.acknowledgments.whisper.details` — Replace "Handy" with "Dictus"
10. `accessibility.permissionsDescription` — Replace "Handy" with "Dictus"
11. `appLanguage.description` — Translate to equivalent of "Choose the language for the Dictus interface. Auto follows your system language." in Spanish: "Elige el idioma de la interfaz de Dictus. Auto sigue el idioma del sistema."

**French (fr/translation.json) — Key-by-key:**
Same 11 keys. Replace "Handy" with "Dictus". Add privacy note to onboarding: "Vos enregistrements restent sur votre appareil."

- `appLanguage.description` — "Choisissez la langue de l'interface Dictus. Auto suit la langue du systeme."
- `settings.general.shortcut.title` — "Raccourcis Dictus"

**Vietnamese (vi/translation.json) — Key-by-key:**
Same 11 keys. Replace "Handy" with "Dictus". Add privacy note to onboarding with appropriate Vietnamese translation.

- `appLanguage.description` — Translate to equivalent of "Choose the language for the Dictus interface. Auto follows your system language." in Vietnamese.

For each file: Find the existing "Handy" string, replace with "Dictus", adjust grammar as needed for each language. Preserve all JSON structure, indentation, and surrounding content.
</action>
<verify>
<automated>echo "es:" && grep -c "Handy" src/i18n/locales/es/translation.json; echo "fr:" && grep -c "Handy" src/i18n/locales/fr/translation.json; echo "vi:" && grep -c "Handy" src/i18n/locales/vi/translation.json; echo "---"; grep -r "Handy" src/i18n/locales/</automated>
</verify>
<acceptance_criteria> - `grep -c "Handy" src/i18n/locales/es/translation.json` returns 0 - `grep -c "Handy" src/i18n/locales/fr/translation.json` returns 0 - `grep -c "Handy" src/i18n/locales/vi/translation.json` returns 0 - Each file contains at least 11 "Dictus" references - Each file is valid JSON (no parse errors) - `grep -r "Handy" src/i18n/locales/` returns empty (zero matches across all locales)
</acceptance_criteria>
<done>All 11 Handy references replaced with Dictus-branded translations in Spanish, French, and Vietnamese. Zero "Handy" text remains in any locale file.</done>
</task>

</tasks>

<verification>
- `grep -r "Handy" src/i18n/locales/` returns empty (zero matches across all 4 locales)
- `grep -r "Dictus" src/i18n/locales/en/translation.json | wc -l` returns 11+
- Each locale file parses as valid JSON (no syntax errors)
- `bun run lint` passes
- `bun run format:check` passes
</verification>

<success_criteria>

- Zero "Handy" references remain in any of the 4 locale files (en, es, fr, vi)
- All 11 keys updated in each locale with appropriate Dictus-branded translations
- Tone matches iOS Dictus app: warm, direct, privacy-aware
- LANG-01: appLanguage.description clearly explains Auto / language choice
- All JSON files are syntactically valid
  </success_criteria>

<output>
After completion, create `.planning/phases/02-visual-rebrand/02-03-SUMMARY.md`
</output>
