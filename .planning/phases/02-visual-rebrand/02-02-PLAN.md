---
phase: 02-visual-rebrand
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/icons/DictusLogo.tsx
  - src/components/icons/DictusWaveformIcon.tsx
  - src/components/Sidebar.tsx
  - src/components/onboarding/Onboarding.tsx
  - src/components/onboarding/AccessibilityOnboarding.tsx
autonomous: true
requirements: [VISU-02, VISU-03, ONBR-01]

must_haves:
  truths:
    - "Sidebar shows Dictus logo at top, not Handy text logo"
    - "Sidebar General nav item uses Dictus waveform icon, not Handy hand"
    - "Onboarding welcome screen shows Dictus branding, not Handy logo"
    - "Accessibility onboarding shows Dictus logo, not Handy logo"
    - "No import of HandyTextLogo or HandyHand exists anywhere in src/"
  artifacts:
    - path: "src/components/icons/DictusLogo.tsx"
      provides: "3-bar waveform SVG + Dictus wordmark"
      exports: ["default"]
    - path: "src/components/icons/DictusWaveformIcon.tsx"
      provides: "Small 3-bar waveform icon for sidebar nav"
      exports: ["default"]
  key_links:
    - from: "src/components/Sidebar.tsx"
      to: "src/components/icons/DictusLogo.tsx"
      via: "import DictusLogo"
      pattern: "import DictusLogo"
    - from: "src/components/Sidebar.tsx"
      to: "src/components/icons/DictusWaveformIcon.tsx"
      via: "icon: DictusWaveformIcon in SECTIONS_CONFIG.general"
      pattern: "icon: DictusWaveformIcon"
    - from: "src/components/onboarding/Onboarding.tsx"
      to: "src/components/icons/DictusLogo.tsx"
      via: "import DictusLogo"
      pattern: "import DictusLogo"
---

<objective>
Create DictusLogo and DictusWaveformIcon components, then replace all HandyTextLogo and HandyHand usages in Sidebar and Onboarding.

Purpose: Eliminates the Handy visual identity from all main app surfaces (sidebar header, nav icons, onboarding).
Output: Two new SVG icon components, all consumers updated.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-rebrand/02-CONTEXT.md
@.planning/phases/02-visual-rebrand/02-RESEARCH.md

<interfaces>
<!-- Existing icon component pattern (from HandyHand.tsx) -->
From src/components/icons/HandyHand.tsx:
```tsx
const HandyHand = ({
  width,
  height,
}: {
  width?: number | string;
  height?: number | string;
}) => ( <svg ... /> );
export default HandyHand;
```

From src/components/Sidebar.tsx:
```tsx
import HandyTextLogo from "./icons/HandyTextLogo";
import HandyHand from "./icons/HandyHand";
// Line 37: icon: HandyHand (in SECTIONS_CONFIG.general)
// Line 97: <HandyTextLogo width={120} className="m-4" />
```

From src/components/onboarding/Onboarding.tsx:
```tsx
import HandyTextLogo from "../icons/HandyTextLogo";
// Line 94: <HandyTextLogo width={200} />
```

From src/components/onboarding/AccessibilityOnboarding.tsx:
```tsx
import HandyTextLogo from "../icons/HandyTextLogo";
// Line 311: <HandyTextLogo width={200} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DictusLogo and DictusWaveformIcon components</name>
  <files>src/components/icons/DictusLogo.tsx, src/components/icons/DictusWaveformIcon.tsx</files>
  <read_first>
    - src/components/icons/HandyTextLogo.tsx (existing pattern — SVG component with width/className props)
    - src/components/icons/HandyHand.tsx (existing pattern — SVG component with width/height props)
    - ../dictus-brand/source/logo-light.svg (light variant SVG source — bar coordinates)
    - ../dictus-brand/source/logo-dark.svg (dark variant SVG source — bar coordinates)
    - ../dictus-brand/source/appicon-light.svg (standalone waveform bars for small icon)
  </read_first>
  <action>
**DictusLogo.tsx** — Create a React SVG component that renders the 3-bar waveform alongside "Dictus" wordmark.

The component must:
- Accept `width?: number | string` and `className?: string` props (matching HandyTextLogo interface for drop-in replacement)
- Export as default
- Render an SVG with the 3-bar waveform from the brand kit and "Dictus" text
- Support light/dark via CSS classes: bars use `fill: var(--color-logo-stroke)` for outer bars, center bar uses a gradient from `#6BA3FF` to `#2563EB` (defined as SVG `<linearGradient>`)
- The "Dictus" text uses SVG `<text>` element with `font-family="-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif"`, `font-weight="600"`, `fill="currentColor"` (inherits text color from parent for light/dark)
- Use CSS classes `.logo-primary` and `.logo-stroke` already defined in App.css for consistent theming

Bar geometry from brand SVGs (viewBox origin 0 0 80 80):
- Bar 1 (left, short): x=19 y=31 width=9 height=18 rx=4.5, class="logo-stroke" opacity=0.45
- Bar 2 (center, tall): x=35.5 y=19 width=9 height=42 rx=4.5, fill=url(#dictus-bar-gradient)
- Bar 3 (right, medium): x=52 y=26 width=9 height=27 rx=4.5, class="logo-stroke" opacity=0.65

The SVG `<linearGradient id="dictus-bar-gradient">` should have:
- stop offset="0%" stop-color="#6BA3FF"
- stop offset="100%" stop-color="#2563EB"

The overall SVG viewBox should accommodate both the waveform icon and wordmark text. Use a viewBox like `0 0 200 80` — icon on the left (0-80), text on the right (~85-200). The `<text>` element positioned at approximately x=85, y=55 (vertically centered with bars).

**DictusWaveformIcon.tsx** — Create a small 3-bar waveform icon for use in sidebar nav items.

The component must:
- Accept `width?: number | string`, `height?: number | string`, and `className?: string` props (matching HandyHand interface)
- Export as default
- Render a compact SVG (viewBox="0 0 24 24") with 3 scaled-down bars
- Bars use `fill="currentColor"` so they inherit text color from the sidebar context
- Center bar slightly taller, using same proportional ratios as the logo bars

Scaled bar geometry for 24x24 viewBox:
- Bar 1 (left, short): x=4 y=9 width=4 height=6 rx=2, opacity=0.5
- Bar 2 (center, tall): x=10 y=5 width=4 height=14 rx=2
- Bar 3 (right, medium): x=16 y=7 width=4 height=10 rx=2, opacity=0.7
  </action>
  <verify>
    <automated>test -f src/components/icons/DictusLogo.tsx && test -f src/components/icons/DictusWaveformIcon.tsx && grep "export default" src/components/icons/DictusLogo.tsx && grep "export default" src/components/icons/DictusWaveformIcon.tsx && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/icons/DictusLogo.tsx exists with `export default DictusLogo`
    - DictusLogo accepts `width` and `className` props
    - DictusLogo SVG contains `<linearGradient id="dictus-bar-gradient">` with stops `#6BA3FF` and `#2563EB`
    - DictusLogo SVG contains `<text` element with "Dictus"
    - src/components/icons/DictusWaveformIcon.tsx exists with `export default DictusWaveformIcon`
    - DictusWaveformIcon accepts `width`, `height`, `className` props
    - DictusWaveformIcon SVG contains 3 `<rect` elements
  </acceptance_criteria>
  <done>DictusLogo renders 3-bar waveform + wordmark with blue gradient center bar and theme-aware colors. DictusWaveformIcon renders compact 3-bar icon suitable for 24x24 nav use.</done>
</task>

<task type="auto">
  <name>Task 2: Replace HandyTextLogo and HandyHand in Sidebar and Onboarding</name>
  <files>src/components/Sidebar.tsx, src/components/onboarding/Onboarding.tsx, src/components/onboarding/AccessibilityOnboarding.tsx</files>
  <read_first>
    - src/components/Sidebar.tsx (lines 1-10 for imports, line 37 for HandyHand in config, line 97 for HandyTextLogo usage)
    - src/components/onboarding/Onboarding.tsx (line 7 for import, line 94 for usage)
    - src/components/onboarding/AccessibilityOnboarding.tsx (line 13 for import, line 311 for usage)
  </read_first>
  <action>
**Sidebar.tsx:**
1. Remove imports on lines 4-5:
   ```tsx
   import HandyTextLogo from "./icons/HandyTextLogo";
   import HandyHand from "./icons/HandyHand";
   ```
2. Add new imports:
   ```tsx
   import DictusLogo from "./icons/DictusLogo";
   import DictusWaveformIcon from "./icons/DictusWaveformIcon";
   ```
3. In SECTIONS_CONFIG.general (line 37), change `icon: HandyHand` to `icon: DictusWaveformIcon`
4. In the JSX (line 97), change `<HandyTextLogo width={120} className="m-4" />` to `<DictusLogo width={120} className="m-4" />`

**Onboarding.tsx:**
1. Remove import on line 7:
   ```tsx
   import HandyTextLogo from "../icons/HandyTextLogo";
   ```
2. Add new import:
   ```tsx
   import DictusLogo from "../icons/DictusLogo";
   ```
3. On line 94, change `<HandyTextLogo width={200} />` to `<DictusLogo width={200} />`

**AccessibilityOnboarding.tsx:**
1. Remove import on line 13:
   ```tsx
   import HandyTextLogo from "../icons/HandyTextLogo";
   ```
2. Add new import:
   ```tsx
   import DictusLogo from "../icons/DictusLogo";
   ```
3. On line 311, change `<HandyTextLogo width={200} />` to `<DictusLogo width={200} />`

Do NOT delete HandyTextLogo.tsx or HandyHand.tsx files yet — they may be imported elsewhere. The lint step will catch any remaining unused imports.
  </action>
  <verify>
    <automated>grep -r "HandyTextLogo\|HandyHand" src/components/Sidebar.tsx src/components/onboarding/Onboarding.tsx src/components/onboarding/AccessibilityOnboarding.tsx; echo "exit: $?" && grep "DictusLogo" src/components/Sidebar.tsx src/components/onboarding/Onboarding.tsx src/components/onboarding/AccessibilityOnboarding.tsx && bun run lint</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx contains `import DictusLogo from "./icons/DictusLogo"`
    - Sidebar.tsx contains `import DictusWaveformIcon from "./icons/DictusWaveformIcon"`
    - Sidebar.tsx contains `icon: DictusWaveformIcon` in SECTIONS_CONFIG.general
    - Sidebar.tsx contains `<DictusLogo width={120}` (not HandyTextLogo)
    - Sidebar.tsx does NOT contain `HandyTextLogo` or `HandyHand`
    - Onboarding.tsx contains `import DictusLogo` and `<DictusLogo width={200}`
    - Onboarding.tsx does NOT contain `HandyTextLogo`
    - AccessibilityOnboarding.tsx contains `import DictusLogo` and `<DictusLogo width={200}`
    - AccessibilityOnboarding.tsx does NOT contain `HandyTextLogo`
    - `bun run lint` passes (no unused import errors)
  </acceptance_criteria>
  <done>All three consumer files use DictusLogo and DictusWaveformIcon. Zero references to HandyTextLogo or HandyHand remain in Sidebar or Onboarding components.</done>
</task>

</tasks>

<verification>
- `grep -r "HandyTextLogo\|HandyHand" src/components/Sidebar.tsx src/components/onboarding/` returns empty
- `grep -r "DictusLogo" src/components/Sidebar.tsx src/components/onboarding/` shows imports and usages in all 3 files
- `grep "DictusWaveformIcon" src/components/Sidebar.tsx` shows import and config usage
- `bun run lint` passes
- `bun run format:check` passes
</verification>

<success_criteria>
- DictusLogo component renders 3-bar waveform with blue gradient + "Dictus" wordmark
- DictusWaveformIcon component renders compact waveform for nav use
- Sidebar header shows DictusLogo, General nav item uses DictusWaveformIcon
- Both onboarding screens show DictusLogo
- No HandyTextLogo or HandyHand references in consumer files
- Lint and format checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-rebrand/02-02-SUMMARY.md`
</output>
