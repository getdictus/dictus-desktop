---
phase: 08-privacy-local-first-ux
plan: "10"
type: execute
wave: 3
depends_on: ["08-09"]
files_modified:
  - src/i18n/locales/ar/translation.json
  - src/i18n/locales/bg/translation.json
  - src/i18n/locales/cs/translation.json
  - src/i18n/locales/de/translation.json
  - src/i18n/locales/es/translation.json
  - src/i18n/locales/fr/translation.json
  - src/i18n/locales/he/translation.json
  - src/i18n/locales/it/translation.json
  - src/i18n/locales/ja/translation.json
  - src/i18n/locales/ko/translation.json
  - src/i18n/locales/pl/translation.json
  - src/i18n/locales/pt/translation.json
  - src/i18n/locales/ru/translation.json
  - src/i18n/locales/sv/translation.json
  - src/i18n/locales/tr/translation.json
  - src/i18n/locales/uk/translation.json
  - src/i18n/locales/vi/translation.json
  - src/i18n/locales/zh/translation.json
  - src/i18n/locales/zh-TW/translation.json
autonomous: true
gap_closure: true
requirements: ["PRIV-01", "PRIV-02", "PRIV-03"]

must_haves:
  truths:
    - "All 19 sibling locales have `settings.postProcessing.tabs.local` and `settings.postProcessing.tabs.cloud` keys with locale-appropriate values"
    - "All 19 sibling locales have REMOVED the `settings.postProcessing.cloudToggle` key block (label + description)"
    - "All 19 sibling locales have REMOVED the `settings.postProcessing.modelsAndLocalProcessing.pillars` key block (privacy/control/simplicity, each with title/body)"
    - "All 19 sibling locales have UPDATED `settings.postProcessing.modelsAndLocalProcessing.selectedModel.cloudSelectedNotice` to reference the Cloud tab (not the cloud toggle)"
    - "All 19 sibling locale JSON files remain valid JSON (no trailing commas, no syntax errors)"
    - "`bun run check:translations` exits 0 — full key-parity with `en/translation.json`"
    - "`bun run build` exits 0"
  artifacts:
    - path: "src/i18n/locales/{ar,bg,cs,de,es,fr,he,it,ja,ko,pl,pt,ru,sv,tr,uk,vi,zh,zh-TW}/translation.json"
      provides: "Each file has tabs.local + tabs.cloud added, cloudToggle block removed, modelsAndLocalProcessing.pillars block removed, selectedModel.cloudSelectedNotice updated"
      contains: "tabs"
  key_links:
    - from: "sibling locales"
      to: "en/translation.json"
      via: "scripts/check-translations.ts key-parity check"
      pattern: "bun run check:translations"
---

<objective>
Propagate the English i18n source changes from Plan 08-09 to all 19 sibling locales. This restores key-parity (broken intentionally by 08-09) and ensures `bun run check:translations` exits 0.

Three coordinated edits per locale file:

1. ADD `settings.postProcessing.tabs.local` + `settings.postProcessing.tabs.cloud` (insertion order matches English: between `api` and `modelsAndLocalProcessing`)
2. REMOVE `settings.postProcessing.cloudToggle` (the entire `{label, description}` block — was a sibling of `api`/`modelsAndLocalProcessing`/`prompts`)
3. REMOVE `settings.postProcessing.modelsAndLocalProcessing.pillars` (the entire `{privacy, control, simplicity}` block)
4. UPDATE `settings.postProcessing.modelsAndLocalProcessing.selectedModel.cloudSelectedNotice` value to reference tabs instead of toggle (locale-appropriate translation)

Purpose: Close the parity gap created by Plan 08-09 in one sweep across all 19 sibling locales (ar, bg, cs, de, es, fr, he, it, ja, ko, pl, pt, ru, sv, tr, uk, vi, zh, zh-TW). Phase 8 cannot ship until this lands because `bun run check:translations` is a CI/quality gate.

Output: 19 modified locale JSON files. No code changes. No backend changes.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-privacy-local-first-ux/08-CONTEXT.md
@.planning/phases/08-privacy-local-first-ux/08-VERIFICATION.md
@.planning/phases/08-privacy-local-first-ux/08-07-SUMMARY.md
@.planning/phases/08-privacy-local-first-ux/08-09-PLAN.md
@CLAUDE.md
@src/i18n/locales/en/translation.json
@scripts/check-translations.ts
@CONTRIBUTING_TRANSLATIONS.md

<interfaces>
<!-- Key contracts. -->

The 19 sibling locales (verified by `ls src/i18n/locales/`):
ar, bg, cs, de, es, fr, he, it, ja, ko, pl, pt, ru, sv, tr, uk, vi, zh, zh-TW

Each locale file has the SAME nested structure under `settings.postProcessing`. The keys to add/remove/update are all in the same JSON path, just with translated values.

Current state in every sibling (post-Plan-08-07):

- `settings.postProcessing.cloudToggle.label` exists (locale-translated)
- `settings.postProcessing.cloudToggle.description` exists (locale-translated)
- `settings.postProcessing.modelsAndLocalProcessing.pillars.privacy.title` exists
- `settings.postProcessing.modelsAndLocalProcessing.pillars.privacy.body` exists
- `settings.postProcessing.modelsAndLocalProcessing.pillars.control.title` exists
- `settings.postProcessing.modelsAndLocalProcessing.pillars.control.body` exists
- `settings.postProcessing.modelsAndLocalProcessing.pillars.simplicity.title` exists
- `settings.postProcessing.modelsAndLocalProcessing.pillars.simplicity.body` exists
- `settings.postProcessing.modelsAndLocalProcessing.selectedModel.cloudSelectedNotice` exists with toggle-era copy
- `settings.postProcessing.tabs` does NOT exist

Target state in every sibling (post this plan):

- `settings.postProcessing.tabs.local` exists with locale-appropriate translation of "Local"
- `settings.postProcessing.tabs.cloud` exists with locale-appropriate translation of "Cloud"
- `settings.postProcessing.cloudToggle` DELETED
- `settings.postProcessing.modelsAndLocalProcessing.pillars` DELETED
- `settings.postProcessing.modelsAndLocalProcessing.selectedModel.cloudSelectedNotice` UPDATED to refer to tab

Translation table for `tabs.local` / `tabs.cloud` (executor authoritative source):

| Locale | tabs.local | tabs.cloud |
| ------ | ---------- | ---------- |
| ar     | محلي       | سحابي      |
| bg     | Локални    | Облачни    |
| cs     | Lokální    | Cloud      |
| de     | Lokal      | Cloud      |
| es     | Local      | Nube       |
| fr     | Local      | Cloud      |
| he     | מקומי      | ענן        |
| it     | Locale     | Cloud      |
| ja     | ローカル   | クラウド   |
| ko     | 로컬       | 클라우드   |
| pl     | Lokalne    | Chmura     |
| pt     | Local      | Nuvem      |
| ru     | Локальные  | Облако     |
| sv     | Lokalt     | Moln       |
| tr     | Yerel      | Bulut      |
| uk     | Локальні   | Хмара      |
| vi     | Cục bộ     | Đám mây    |
| zh     | 本地       | 云端       |
| zh-TW  | 本地       | 雲端       |

Translation table for the updated `cloudSelectedNotice` (executor authoritative source). The English/French source value (en/translation.json) is:

> "Votre modèle sélectionné est un fournisseur cloud. Cliquez sur l'onglet « Cloud » ci-dessous pour le voir, ou choisissez un modèle local."

For each locale, the new value MUST replace the old toggle-era copy with a translation that mirrors the tab-era meaning. The KEY anchor is the phrase about clicking the "Cloud" tab. Brand-name "Cloud" (when used as the tab label) MUST match the `tabs.cloud` translation for that locale.

| Locale | New cloudSelectedNotice value                                                                                                             |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------- |
| ar     | النموذج المحدد هو مزود سحابي. انقر على علامة التبويب "سحابي" أدناه لرؤيته، أو اختر نموذجًا محليًا.                                        |
| bg     | Избраният от вас модел е облачен доставчик. Щракнете върху раздела „Облачни" по-долу, за да го видите, или изберете локален модел.        |
| cs     | Vámi vybraný model je cloudový poskytovatel. Klikněte na záložku „Cloud" níže, abyste jej viděli, nebo zvolte lokální model.              |
| de     | Dein ausgewähltes Modell ist ein Cloud-Anbieter. Klicke unten auf den Tab „Cloud", um es zu sehen, oder wähle ein lokales Modell.         |
| es     | El modelo seleccionado es un proveedor de nube. Haz clic en la pestaña «Nube» abajo para verlo, o elige un modelo local.                  |
| fr     | Votre modèle sélectionné est un fournisseur cloud. Cliquez sur l'onglet « Cloud » ci-dessous pour le voir, ou choisissez un modèle local. |
| he     | המודל שבחרת הוא ספק ענן. לחץ על הכרטיסייה "ענן" למטה כדי לראותו, או בחר במודל מקומי.                                                      |
| it     | Il modello selezionato è un fornitore cloud. Clicca sulla scheda «Cloud» qui sotto per visualizzarlo, oppure scegli un modello locale.    |
| ja     | 選択したモデルはクラウドプロバイダーです。表示するには下の「クラウド」タブをクリックするか、ローカルモデルを選択してください。            |
| ko     | 선택한 모델은 클라우드 공급자입니다. 아래의 "클라우드" 탭을 클릭하여 확인하거나 로컬 모델을 선택하세요.                                   |
| pl     | Wybrany model to dostawca chmury. Kliknij kartę „Chmura" poniżej, aby go zobaczyć, lub wybierz model lokalny.                             |
| pt     | O modelo selecionado é um provedor de nuvem. Clique na guia «Nuvem» abaixo para vê-lo ou escolha um modelo local.                         |
| ru     | Выбранная модель — облачный провайдер. Нажмите вкладку «Облако» ниже, чтобы увидеть её, или выберите локальную модель.                    |
| sv     | Din valda modell är en molnleverantör. Klicka på fliken ”Moln” nedan för att se den, eller välj en lokal modell.                          |
| tr     | Seçtiğiniz model bir bulut sağlayıcısıdır. Görüntülemek için aşağıdaki "Bulut" sekmesine tıklayın veya yerel bir model seçin.             |
| uk     | Обрана вами модель — це хмарний постачальник. Натисніть вкладку «Хмара» нижче, щоб побачити її, або оберіть локальну модель.              |
| vi     | Mô hình bạn chọn là một nhà cung cấp đám mây. Nhấp vào tab "Đám mây" bên dưới để xem, hoặc chọn một mô hình cục bộ.                       |
| zh     | 您选择的模型是云端提供商。点击下方的"云端"选项卡查看，或选择本地模型。                                                                    |
| zh-TW  | 您選擇的模型是雲端提供商。點擊下方的「雲端」分頁查看，或選擇本地模型。                                                                    |

The executor SHOULD use these exact values. They preserve:

- Brand name "Cloud" / "云端" matching each locale's `tabs.cloud` translation
- The two-clause structure (you have cloud selected → switch to tab OR choose local)
- The same quotation style convention each locale uses (« » for fr/es/it/pt; „" for de/cs/pl/bg; "" for en/uk/sv; "" for ar/he/ja/ko; "" for zh; 「」 for zh-TW)

Reference implementation approach (recommended): write a small Node.js script (use `bun` or `node` directly via Bash) that mutates each JSON file in place. Or use jq if installed. Or manual edits with the Edit tool — 19 files, 4 edits each = 76 surgical edits. Script approach is safer (atomicity per file, JSON validation).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Propagate i18n key changes to all 19 sibling locales</name>
  <files>src/i18n/locales/ar/translation.json, src/i18n/locales/bg/translation.json, src/i18n/locales/cs/translation.json, src/i18n/locales/de/translation.json, src/i18n/locales/es/translation.json, src/i18n/locales/fr/translation.json, src/i18n/locales/he/translation.json, src/i18n/locales/it/translation.json, src/i18n/locales/ja/translation.json, src/i18n/locales/ko/translation.json, src/i18n/locales/pl/translation.json, src/i18n/locales/pt/translation.json, src/i18n/locales/ru/translation.json, src/i18n/locales/sv/translation.json, src/i18n/locales/tr/translation.json, src/i18n/locales/uk/translation.json, src/i18n/locales/vi/translation.json, src/i18n/locales/zh/translation.json, src/i18n/locales/zh-TW/translation.json</files>
  <read_first>
    - src/i18n/locales/en/translation.json (post-Plan-08-09 state — the SOURCE OF TRUTH for what keys exist; the executor MUST verify English source has `tabs.local`, `tabs.cloud`, NO `cloudToggle`, NO `pillars`, and updated `cloudSelectedNotice` before starting)
    - .planning/phases/08-privacy-local-first-ux/08-VERIFICATION.md (gaps 5, 6 entries — for context on why these keys are being removed)
    - .planning/phases/08-privacy-local-first-ux/08-07-SUMMARY.md (the prior locale-propagation plan that ADDED these keys — useful for reverse-engineering the insertion order pattern)
    - CONTRIBUTING_TRANSLATIONS.md (brand-name policy — "Cloud" used as a tab label is a UI label, not a brand name; it MUST be translated per locale per the translation table above)
    - scripts/check-translations.ts (to understand how the parity check works; it traverses all leaf paths in `en/translation.json` and asserts presence in every sibling)
    - For each locale file: run `node -e "console.log(JSON.stringify(require('/Users/pierreviviere/dev/dictus-desktop/src/i18n/locales/{LOCALE}/translation.json').settings.postProcessing.modelsAndLocalProcessing.selectedModel.cloudSelectedNotice))"` to see the current value BEFORE editing — confirms the file is the version Plan 08-07 produced
  </read_first>
  <action>
    Execute this task using a SCRIPT-DRIVEN approach to ensure JSON validity and atomicity. Recommended: write a one-shot Node.js script (or run inline `node -e`) that:

    1. Reads each of the 19 sibling locale files
    2. Validates the input JSON
    3. Mutates the object:
       - Inserts `tabs: { local: <translation>, cloud: <translation> }` under `settings.postProcessing` (insertion order: between `api` and `modelsAndLocalProcessing` — this is achievable in JS by reconstructing the parent object with the new key in position, since JS preserves insertion order for string keys)
       - Deletes `settings.postProcessing.cloudToggle`
       - Deletes `settings.postProcessing.modelsAndLocalProcessing.pillars`
       - Sets `settings.postProcessing.modelsAndLocalProcessing.selectedModel.cloudSelectedNotice` to the new locale-specific value
    4. Writes the file back with 2-space indentation (matching project convention)
    5. Re-validates by parsing the written file

    Translation values to use (TAB labels):

    ```json
    {
      "ar": { "local": "محلي", "cloud": "سحابي" },
      "bg": { "local": "Локални", "cloud": "Облачни" },
      "cs": { "local": "Lokální", "cloud": "Cloud" },
      "de": { "local": "Lokal", "cloud": "Cloud" },
      "es": { "local": "Local", "cloud": "Nube" },
      "fr": { "local": "Local", "cloud": "Cloud" },
      "he": { "local": "מקומי", "cloud": "ענן" },
      "it": { "local": "Locale", "cloud": "Cloud" },
      "ja": { "local": "ローカル", "cloud": "クラウド" },
      "ko": { "local": "로컬", "cloud": "클라우드" },
      "pl": { "local": "Lokalne", "cloud": "Chmura" },
      "pt": { "local": "Local", "cloud": "Nuvem" },
      "ru": { "local": "Локальные", "cloud": "Облако" },
      "sv": { "local": "Lokalt", "cloud": "Moln" },
      "tr": { "local": "Yerel", "cloud": "Bulut" },
      "uk": { "local": "Локальні", "cloud": "Хмара" },
      "vi": { "local": "Cục bộ", "cloud": "Đám mây" },
      "zh": { "local": "本地", "cloud": "云端" },
      "zh-TW": { "local": "本地", "cloud": "雲端" }
    }
    ```

    Translation values to use (cloudSelectedNotice — copy verbatim from the table in the `<interfaces>` block above). Each locale gets the exact string from the table.

    **Insertion order trick (for `tabs` placement):** JavaScript objects preserve key insertion order for string keys. To insert `tabs` between `api` and `modelsAndLocalProcessing`, reconstruct the `settings.postProcessing` object in the right order:

    ```javascript
    const pp = settings.postProcessing;
    const { cloudToggle, ...rest } = pp; // remove cloudToggle from order
    const reordered = {};
    for (const k of Object.keys(rest)) {
      reordered[k] = rest[k];
      if (k === "api") {
        reordered.tabs = { local: TABS[locale].local, cloud: TABS[locale].cloud };
      }
    }
    settings.postProcessing = reordered;
    ```

    **JSON formatting:** Use `JSON.stringify(data, null, 2)` for 2-space indentation. Ensure the file ends with a newline (some files in the repo end with `\n`, others don't — match the existing trailing-newline state per file).

    **Per-locale failure isolation:** If a single locale fails (parse error or unexpected structure), log the error and continue with the others. Do NOT stop the batch. Surface all failures at the end.

    **Rollback safety:** This task is mechanical — if it fails, `git restore src/i18n/locales/` is the rollback. No partial-state risk if the script writes atomically per file.

    After the batch, run `bun run check:translations` and `bun run build` to confirm parity restored.

    Optional approach: instead of a script, use the Edit tool 19 × 4 = 76 times. The script approach is strongly preferred for correctness and speed.

  </action>
  <verify>
    <automated>
      # Validate all 19 locale files are valid JSON
      for L in ar bg cs de es fr he it ja ko pl pt ru sv tr uk vi zh zh-TW; do
        node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/$L/translation.json','utf8'))" || echo "INVALID: $L"
      done
      # Expect: zero "INVALID:" lines

      # tabs.local and tabs.cloud present in all 19 locales
      for L in ar bg cs de es fr he it ja ko pl pt ru sv tr uk vi zh zh-TW; do
        node -e "const t=require('/Users/pierreviviere/dev/dictus-desktop/src/i18n/locales/$L/translation.json'); if (!t.settings.postProcessing.tabs?.local || !t.settings.postProcessing.tabs?.cloud) { console.log('MISSING_TABS: $L'); process.exit(1); }" || true
      done
      # Expect: zero "MISSING_TABS:" lines

      # cloudToggle removed from all 19 locales
      for L in ar bg cs de es fr he it ja ko pl pt ru sv tr uk vi zh zh-TW; do
        node -e "const t=require('/Users/pierreviviere/dev/dictus-desktop/src/i18n/locales/$L/translation.json'); if (t.settings.postProcessing.cloudToggle !== undefined) { console.log('STILL_PRESENT_CLOUDTOGGLE: $L'); process.exit(1); }" || true
      done
      # Expect: zero "STILL_PRESENT_CLOUDTOGGLE:" lines

      # pillars removed from all 19 locales
      for L in ar bg cs de es fr he it ja ko pl pt ru sv tr uk vi zh zh-TW; do
        node -e "const t=require('/Users/pierreviviere/dev/dictus-desktop/src/i18n/locales/$L/translation.json'); if (t.settings.postProcessing.modelsAndLocalProcessing?.pillars !== undefined) { console.log('STILL_PRESENT_PILLARS: $L'); process.exit(1); }" || true
      done
      # Expect: zero "STILL_PRESENT_PILLARS:" lines

      # cloudSelectedNotice updated — contains a tab-referencing word (varies per locale; use grep for the most universal indicator: each locale's `tabs.cloud` value should appear inside the notice)
      for L in ar bg cs de es fr he it ja ko pl pt ru sv tr uk vi zh zh-TW; do
        node -e "
          const t=require('/Users/pierreviviere/dev/dictus-desktop/src/i18n/locales/$L/translation.json');
          const notice = t.settings.postProcessing.modelsAndLocalProcessing?.selectedModel?.cloudSelectedNotice;
          const cloudLabel = t.settings.postProcessing.tabs?.cloud;
          if (!notice || !cloudLabel || !notice.includes(cloudLabel)) {
            console.log('NOTICE_NOT_UPDATED: $L (notice=' + notice + ' cloudLabel=' + cloudLabel + ')');
            process.exit(1);
          }
        " || true
      done
      # Expect: zero "NOTICE_NOT_UPDATED:" lines

      # check-translations passes
      bun run check:translations
      # Expect: exit 0; "All 19 languages have complete translations!"

      bun run build
      # Expect: exit 0

      bun run lint
      # Expect: exit 0
    </automated>

  </verify>
  <acceptance_criteria>
    Each criterion below MUST pass independently. Use the bash loops in `<verify>` above to run them in batch.

    **Per-locale validity:**
    - `for L in ar bg cs de es fr he it ja ko pl pt ru sv tr uk vi zh zh-TW; do node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/$L/translation.json','utf8'))"; done` runs with no errors

    **Per-locale tabs.local presence** (one criterion per locale; collapsed for readability):
    - For each L in {ar, bg, cs, de, es, fr, he, it, ja, ko, pl, pt, ru, sv, tr, uk, vi, zh, zh-TW}: `node -e "const t=require('./src/i18n/locales/$L/translation.json'); process.exit(t.settings.postProcessing.tabs?.local ? 0 : 1)"` exits 0

    **Per-locale tabs.cloud presence** (one criterion per locale):
    - For each L in the 19 locales: `node -e "const t=require('./src/i18n/locales/$L/translation.json'); process.exit(t.settings.postProcessing.tabs?.cloud ? 0 : 1)"` exits 0

    **Per-locale cloudToggle removal** (one criterion per locale):
    - For each L in the 19 locales: `node -e "const t=require('./src/i18n/locales/$L/translation.json'); process.exit(t.settings.postProcessing.cloudToggle === undefined ? 0 : 1)"` exits 0
    - Equivalent grep verification: `! grep -q "cloudToggle" src/i18n/locales/$L/translation.json` for each L

    **Per-locale pillars removal** (one criterion per locale):
    - For each L in the 19 locales: `node -e "const t=require('./src/i18n/locales/$L/translation.json'); process.exit(t.settings.postProcessing.modelsAndLocalProcessing?.pillars === undefined ? 0 : 1)"` exits 0
    - Equivalent grep verification: `! grep -q "pillars" src/i18n/locales/$L/translation.json` for each L

    **Per-locale cloudSelectedNotice updated** (one criterion per locale):
    - For each L in the 19 locales: the new notice value contains the locale's `tabs.cloud` translation (verified via the node script in `<verify>`)

    **Full parity check:**
    - `bun run check:translations` exits 0 with output "All 19 languages have complete translations!"
    - `bun run build` exits 0
    - `bun run lint` exits 0

    **No new English keys missing:** `grep -L "tabs" src/i18n/locales/*/translation.json` returns EMPTY (every locale, including `en`, contains "tabs" — confirms English source already has it from Plan 08-09)

    **No stale `pillars` references:** `grep -l "pillars" src/i18n/locales/*/translation.json` returns EMPTY (no locale, including `en`, contains "pillars" anywhere)

    **No stale `cloudToggle` references:** `grep -l "cloudToggle" src/i18n/locales/*/translation.json` returns EMPTY

</acceptance_criteria>
<done>
All 19 sibling locale JSON files have: `tabs.local` + `tabs.cloud` added in the correct insertion position; `cloudToggle` block removed; `modelsAndLocalProcessing.pillars` block removed; `selectedModel.cloudSelectedNotice` updated to a locale-appropriate tab-referencing copy. `bun run check:translations` exits 0. Build and lint green.
</done>
</task>

</tasks>

<verification>
After Task 1 lands:

```bash
bun run check:translations
# Expect: "All 19 languages have complete translations!" exit 0

bun run lint
bun run format:check
bun run build
# All exit 0

cd src-tauri && cargo fmt -- --check && cargo clippy --all-targets -- -D warnings && cargo test --lib settings::tests
# All exit 0 — precautionary (no Rust changes in this plan)
```

Manual UAT (re-run full Phase 8 UAT in the locales the user normally tests — French is canonical):

1. Launch `bun run tauri dev` on macOS Apple Silicon.
2. Switch UI language to French in settings.
3. Open Post-processing — tabs read "Local" / "Cloud".
4. Select OpenAI from Cloud tab → switch to Local tab → notice reads "Votre modèle sélectionné est un fournisseur cloud. Cliquez sur l'onglet « Cloud » ci-dessous pour le voir, ou choisissez un modèle local." (no stale toggle reference).
5. Spot-check 2-3 other locales (e.g. de, ja, ar) — tabs labels are translated, no "cloud toggle" stale copy, no pillars block.
   </verification>

<success_criteria>

- 19 sibling locale files modified (verified by `git status` listing all 19)
- `bun run check:translations` exits 0
- `bun run build` exits 0
- `bun run lint` exits 0
- No locale file contains `cloudToggle` or `pillars` (verified by `grep -l` returning empty across all locale files)
- Every locale file contains `tabs.local` and `tabs.cloud` (verified by the per-locale node scripts)
- Every locale's `cloudSelectedNotice` value contains that locale's `tabs.cloud` translation (verified)
- 1 commit lands on the branch:
  - `chore(08-10): propagate i18n key changes to 19 sibling locales (tabs added, pillars + cloudToggle removed, cloudSelectedNotice updated)`
    </success_criteria>

<output>
After completion, create `.planning/phases/08-privacy-local-first-ux/08-10-SUMMARY.md` documenting:
- 19 files modified (list all)
- Translation table used (tabs.local, tabs.cloud per locale)
- Translation approach for cloudSelectedNotice (refer to interfaces block in plan)
- check-translations final output
- Brand-name policy note: "Cloud" is a UI tab label in this case, translated per locale (matches what Pierre would see); brand-name "Cloud" (when referring to Cloud Provider context inside the notice's quoted tab name) MUST match each locale's `tabs.cloud` value for internal consistency
- Phase 8 ship readiness: with Plans 08-08, 08-09, 08-10 all landed, all 6 gaps from VERIFICATION.md are closed; ready for re-UAT (`/gsd:execute-phase 8` with UAT checkpoint) or direct `/gsd:verify-work 8`
</output>
