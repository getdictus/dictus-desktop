---
phase: 08-privacy-local-first-ux
plan: 04
type: execute
wave: 2
depends_on:
  - "08-03"
files_modified:
  - src/i18n/locales/ar/translation.json
  - src/i18n/locales/bg/translation.json
  - src/i18n/locales/cs/translation.json
  - src/i18n/locales/de/translation.json
  - src/i18n/locales/es/translation.json
  - src/i18n/locales/fr/translation.json
  - src/i18n/locales/he/translation.json
  - src/i18n/locales/it/translation.json
  - src/i18n/locales/ja/translation.json
  - src/i18n/locales/ko/translation.json
  - src/i18n/locales/pl/translation.json
  - src/i18n/locales/pt/translation.json
  - src/i18n/locales/ru/translation.json
  - src/i18n/locales/sv/translation.json
  - src/i18n/locales/tr/translation.json
  - src/i18n/locales/uk/translation.json
  - src/i18n/locales/vi/translation.json
  - src/i18n/locales/zh/translation.json
  - src/i18n/locales/zh-TW/translation.json
autonomous: true
requirements:
  - PRIV-01
  - PRIV-02
must_haves:
  truths:
    - "All 19 sibling locale files contain the same new key paths as en/translation.json"
    - "Brand names (Ollama, Apple Intelligence, OpenAI) remain untranslated per CONTRIBUTING_TRANSLATIONS.md"
    - "Section labels and descriptions are localized for each language using English fallback only where machine translation would be unsafe for technical UI"
    - "bun run check:translations passes with full parity"
  artifacts:
    - path: "src/i18n/locales/{19 locales}/translation.json"
      provides: "i18n parity for new Phase 8 keys (sectionLocal, sectionExternal, descriptions, ollamaTip, testConnection.*, networkSurface.*, groups.postProcessing, labels.custom)"
      contains: "sectionLocal"
  key_links:
    - from: "All 20 locale files"
      to: "scripts/check-translations.ts parity check"
      via: "bun run check:translations"
      pattern: "check:translations"
---

<objective>
Copy the new English i18n keys added in Plan 03 to all 19 sibling locale files. Translate user-facing strings where safe; keep brand names and technical fallbacks in English where translation would be unsafe.

Purpose: ESLint enforces `i18next/no-literal-string`. If a key exists in `en` but is missing in another locale, i18next falls back to the key path (e.g., `settings.postProcessing.api.providers.sectionLocal`) which renders raw. Parity is required.

Output:
- 19 updated locale files
- `bun run check:translations` green
- `bun run lint` green
- `bun run build` green
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-privacy-local-first-ux/08-CONTEXT.md
@.planning/phases/08-privacy-local-first-ux/08-RESEARCH.md
@.planning/phases/08-privacy-local-first-ux/08-UI-SPEC.md
@CONTRIBUTING_TRANSLATIONS.md
@scripts/check-translations.ts
@src/i18n/locales/en/translation.json

<interfaces>
<!-- New keys added to en/translation.json by Plan 03 — must be replicated across 19 locales. -->

Keys to add (paths and English source values):

1. `settings.postProcessing.api.providers.sectionLocal` = "On your device"
2. `settings.postProcessing.api.providers.sectionExternal` = "External — data leaves this device"
3. `settings.postProcessing.api.providers.labels.custom` = "Custom (local)"
4. `settings.postProcessing.api.providers.descriptions.apple_intelligence` = "Free • on-device • macOS Apple Silicon"
5. `settings.postProcessing.api.providers.descriptions.custom` = "Ollama / LM Studio on localhost"
6. `settings.postProcessing.api.custom.ollamaTip` = "Tip: install <link>Ollama</link> and run <code>ollama serve</code> to use local models."
7. `settings.postProcessing.api.custom.testConnection.button` = "Test connection"
8. `settings.postProcessing.api.custom.testConnection.success` = "Connected — {{count}} models available"
9. `settings.postProcessing.api.custom.testConnection.errorTitle` = "Could not reach {{baseUrl}}"
10. `settings.postProcessing.api.custom.testConnection.errorDetails` = "Details"
11. `settings.about.networkSurface.title` = "Network surface"
12. `settings.about.networkSurface.description` = "Every outbound endpoint this app can contact, and how to disable each."
13. `settings.about.networkSurface.button` = "View privacy doc"
14. `settings.advanced.groups.postProcessing` = "Post Processing"

Rules from `CONTRIBUTING_TRANSLATIONS.md`:
- Brand names NOT translated: Ollama, Apple Intelligence, OpenAI, Anthropic, Groq, Cerebras, OpenRouter, Z.AI, LM Studio, macOS Apple Silicon, Dictus
- Interpolation placeholders preserved verbatim: `{{count}}`, `{{baseUrl}}`
- Custom JSX component placeholders preserved verbatim: `<link>...</link>`, `<code>...</code>`
- English fallback is ACCEPTABLE per the project convention when machine translation would be unsafe — DO NOT machine-translate technical strings if uncertain; prefer English source verbatim
- The literal string "Custom (local)" is a brand-style technical label; the parenthetical `(local)` MAY be translated where natural (e.g., French `(local)` stays, Spanish `(local)` stays, German `(lokal)` may be used) but the word "Custom" stays as-is to match the visual identity

19 locales to update (matches `ls src/i18n/locales/` minus `en`):
ar bg cs de es fr he it ja ko pl pt ru sv tr uk vi zh zh-TW
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replicate keys across 19 sibling locales</name>
  <files>src/i18n/locales/ar/translation.json, src/i18n/locales/bg/translation.json, src/i18n/locales/cs/translation.json, src/i18n/locales/de/translation.json, src/i18n/locales/es/translation.json, src/i18n/locales/fr/translation.json, src/i18n/locales/he/translation.json, src/i18n/locales/it/translation.json, src/i18n/locales/ja/translation.json, src/i18n/locales/ko/translation.json, src/i18n/locales/pl/translation.json, src/i18n/locales/pt/translation.json, src/i18n/locales/ru/translation.json, src/i18n/locales/sv/translation.json, src/i18n/locales/tr/translation.json, src/i18n/locales/uk/translation.json, src/i18n/locales/vi/translation.json, src/i18n/locales/zh/translation.json, src/i18n/locales/zh-TW/translation.json</files>
  <read_first>
    - src/i18n/locales/en/translation.json (source of truth — locate the 14 new keys added in Plan 03 inside `settings.postProcessing.api.providers`, `settings.postProcessing.api.custom`, `settings.about.networkSurface`, `settings.advanced.groups.postProcessing`)
    - CONTRIBUTING_TRANSLATIONS.md (full file — brand-name rules, interpolation rules, fallback policy)
    - src/i18n/locales/fr/translation.json (representative non-English file — shows existing translation style)
    - src/i18n/locales/de/translation.json (representative non-English file — shows existing translation style)
    - scripts/check-translations.ts (understand the parity check before running it)
  </read_first>
  <action>
    For EACH of the 19 sibling locale files, add the same 14 keys under the same nested paths as `en/translation.json`. Preserve existing JSON structure and existing keys exactly.

    Translation policy per language family:

    **Tier A — translate fully (descriptions, button labels, headings, section labels):**
    - de, fr, es, it, pt — natural translations
    - ja, ko, zh, zh-TW — natural translations
    - ru, uk, pl, cs, bg — natural translations
    - sv, tr, vi — natural translations
    - ar, he — RTL languages; translate text but keep brand names + URLs LTR (i18next/RTL handles direction)

    **Tier B — keep English fallback verbatim (if uncertain about a technical phrase):**
    - For the `ollamaTip` Trans string: the placeholders `<link>Ollama</link>` and `<code>ollama serve</code>` MUST be preserved verbatim — only translate the surrounding words.
    - For the `testConnection.success` string: keep `{{count}}` placeholder verbatim.
    - For the `testConnection.errorTitle` string: keep `{{baseUrl}}` placeholder verbatim.

    Concrete translations per locale (use these — they're vetted against the project's existing translation style; if unsure, English fallback is acceptable per CONTRIBUTING_TRANSLATIONS.md):

    For each locale, add this JSON snippet under `settings.postProcessing.api` (alongside `provider`, `appleIntelligence`, `baseUrl`, `apiKey`, `model`):
    ```json
    "providers": {
      "sectionLocal": "<see table below>",
      "sectionExternal": "<see table below>",
      "labels": {
        "custom": "<see table below>"
      },
      "descriptions": {
        "apple_intelligence": "<see table below>",
        "custom": "<see table below>"
      }
    },
    "custom": {
      "ollamaTip": "<see table below>",
      "testConnection": {
        "button": "<see table below>",
        "success": "<see table below>",
        "errorTitle": "<see table below>",
        "errorDetails": "<see table below>"
      }
    }
    ```

    And under `settings.about` (after the existing `privacy` block):
    ```json
    "networkSurface": {
      "title": "<see table below>",
      "description": "<see table below>",
      "button": "<see table below>"
    }
    ```

    And under `settings.advanced.groups`:
    ```json
    "postProcessing": "<see table below>"
    ```

    Translation table (use exact strings; brand names preserved):

    | Locale | sectionLocal | sectionExternal | labels.custom | desc.apple_intelligence | desc.custom |
    |--------|--------------|-----------------|---------------|--------------------------|-------------|
    | de | Auf deinem Gerät | Extern — Daten verlassen dieses Gerät | Custom (lokal) | Kostenlos • auf dem Gerät • macOS Apple Silicon | Ollama / LM Studio auf localhost |
    | fr | Sur votre appareil | Externe — les données quittent cet appareil | Custom (local) | Gratuit • sur l'appareil • macOS Apple Silicon | Ollama / LM Studio sur localhost |
    | es | En tu dispositivo | Externo — los datos salen de este dispositivo | Custom (local) | Gratis • en el dispositivo • macOS Apple Silicon | Ollama / LM Studio en localhost |
    | it | Sul tuo dispositivo | Esterno — i dati lasciano questo dispositivo | Custom (local) | Gratuito • sul dispositivo • macOS Apple Silicon | Ollama / LM Studio su localhost |
    | pt | No seu dispositivo | Externo — os dados saem deste dispositivo | Custom (local) | Grátis • no dispositivo • macOS Apple Silicon | Ollama / LM Studio em localhost |
    | ja | お使いのデバイスで | 外部 — データはこのデバイスから出ます | Custom (local) | 無料 • デバイス上 • macOS Apple Silicon | localhost 上の Ollama / LM Studio |
    | ko | 내 기기에서 | 외부 — 데이터가 이 기기를 떠남 | Custom (local) | 무료 • 기기 내 • macOS Apple Silicon | localhost의 Ollama / LM Studio |
    | zh | 在你的设备上 | 外部 — 数据离开此设备 | Custom (本地) | 免费 • 设备上 • macOS Apple Silicon | localhost 上的 Ollama / LM Studio |
    | zh-TW | 在你的裝置上 | 外部 — 資料離開此裝置 | Custom (本機) | 免費 • 裝置上 • macOS Apple Silicon | localhost 上的 Ollama / LM Studio |
    | ru | На вашем устройстве | Внешние — данные покидают это устройство | Custom (локально) | Бесплатно • на устройстве • macOS Apple Silicon | Ollama / LM Studio на localhost |
    | uk | На вашому пристрої | Зовнішні — дані залишають цей пристрій | Custom (локально) | Безкоштовно • на пристрої • macOS Apple Silicon | Ollama / LM Studio на localhost |
    | pl | Na twoim urządzeniu | Zewnętrzne — dane opuszczają to urządzenie | Custom (lokalnie) | Bezpłatnie • na urządzeniu • macOS Apple Silicon | Ollama / LM Studio na localhost |
    | cs | Na vašem zařízení | Externí — data opouštějí toto zařízení | Custom (lokálně) | Zdarma • v zařízení • macOS Apple Silicon | Ollama / LM Studio na localhost |
    | bg | На вашето устройство | Външни — данните напускат това устройство | Custom (локално) | Безплатно • на устройството • macOS Apple Silicon | Ollama / LM Studio на localhost |
    | sv | På din enhet | Externt — data lämnar denna enhet | Custom (lokalt) | Gratis • på enheten • macOS Apple Silicon | Ollama / LM Studio på localhost |
    | tr | Cihazında | Harici — veriler bu cihazdan çıkar | Custom (yerel) | Ücretsiz • cihazda • macOS Apple Silicon | localhost üzerinde Ollama / LM Studio |
    | vi | Trên thiết bị của bạn | Bên ngoài — dữ liệu rời khỏi thiết bị này | Custom (cục bộ) | Miễn phí • trên thiết bị • macOS Apple Silicon | Ollama / LM Studio trên localhost |
    | ar | على جهازك | خارجي — تغادر البيانات هذا الجهاز | Custom (محلي) | مجاني • على الجهاز • macOS Apple Silicon | Ollama / LM Studio على localhost |
    | he | במכשיר שלך | חיצוני — הנתונים יוצאים מהמכשיר הזה | Custom (מקומי) | חינם • במכשיר • macOS Apple Silicon | Ollama / LM Studio ב-localhost |

    | Locale | ollamaTip | testConnection.button | testConnection.success | testConnection.errorTitle | testConnection.errorDetails |
    |--------|-----------|-----------------------|------------------------|---------------------------|------------------------------|
    | de | Tipp: installiere <link>Ollama</link> und führe <code>ollama serve</code> aus, um lokale Modelle zu verwenden. | Verbindung testen | Verbunden — {{count}} Modelle verfügbar | {{baseUrl}} nicht erreichbar | Details |
    | fr | Astuce : installez <link>Ollama</link> et lancez <code>ollama serve</code> pour utiliser des modèles locaux. | Tester la connexion | Connecté — {{count}} modèles disponibles | Impossible d'atteindre {{baseUrl}} | Détails |
    | es | Consejo: instala <link>Ollama</link> y ejecuta <code>ollama serve</code> para usar modelos locales. | Probar conexión | Conectado — {{count}} modelos disponibles | No se pudo conectar con {{baseUrl}} | Detalles |
    | it | Suggerimento: installa <link>Ollama</link> ed esegui <code>ollama serve</code> per usare modelli locali. | Testa connessione | Connesso — {{count}} modelli disponibili | Impossibile raggiungere {{baseUrl}} | Dettagli |
    | pt | Dica: instale <link>Ollama</link> e execute <code>ollama serve</code> para usar modelos locais. | Testar conexão | Conectado — {{count}} modelos disponíveis | Não foi possível alcançar {{baseUrl}} | Detalhes |
    | ja | ヒント：<link>Ollama</link> をインストールして <code>ollama serve</code> を実行すると、ローカルモデルを使用できます。 | 接続をテスト | 接続済み — {{count}} モデル利用可能 | {{baseUrl}} に到達できません | 詳細 |
    | ko | 팁: <link>Ollama</link>를 설치하고 <code>ollama serve</code>를 실행하여 로컬 모델을 사용하세요. | 연결 테스트 | 연결됨 — {{count}}개 모델 사용 가능 | {{baseUrl}}에 연결할 수 없습니다 | 세부 정보 |
    | zh | 提示：安装 <link>Ollama</link> 并运行 <code>ollama serve</code> 即可使用本地模型。 | 测试连接 | 已连接 — 可用 {{count}} 个模型 | 无法连接到 {{baseUrl}} | 详细信息 |
    | zh-TW | 提示：安裝 <link>Ollama</link> 並執行 <code>ollama serve</code> 即可使用本機模型。 | 測試連線 | 已連線 — 可用 {{count}} 個模型 | 無法連線至 {{baseUrl}} | 詳細資訊 |
    | ru | Совет: установите <link>Ollama</link> и запустите <code>ollama serve</code> для использования локальных моделей. | Проверить соединение | Подключено — доступно {{count}} моделей | Не удалось подключиться к {{baseUrl}} | Подробности |
    | uk | Порада: встановіть <link>Ollama</link> та запустіть <code>ollama serve</code>, щоб використовувати локальні моделі. | Перевірити з'єднання | Підключено — доступно {{count}} моделей | Не вдалося підключитися до {{baseUrl}} | Подробиці |
    | pl | Wskazówka: zainstaluj <link>Ollama</link> i uruchom <code>ollama serve</code>, aby używać lokalnych modeli. | Testuj połączenie | Połączono — dostępnych {{count}} modeli | Nie można połączyć się z {{baseUrl}} | Szczegóły |
    | cs | Tip: nainstalujte <link>Ollama</link> a spusťte <code>ollama serve</code> pro použití lokálních modelů. | Otestovat připojení | Připojeno — dostupných {{count}} modelů | Nelze se připojit k {{baseUrl}} | Podrobnosti |
    | bg | Съвет: инсталирайте <link>Ollama</link> и стартирайте <code>ollama serve</code>, за да използвате локални модели. | Тествай връзка | Свързано — налични {{count}} модела | Не може да се достигне до {{baseUrl}} | Подробности |
    | sv | Tips: installera <link>Ollama</link> och kör <code>ollama serve</code> för att använda lokala modeller. | Testa anslutning | Ansluten — {{count}} modeller tillgängliga | Kunde inte nå {{baseUrl}} | Detaljer |
    | tr | İpucu: yerel modelleri kullanmak için <link>Ollama</link>'yı yükleyin ve <code>ollama serve</code> komutunu çalıştırın. | Bağlantıyı test et | Bağlandı — {{count}} model kullanılabilir | {{baseUrl}} adresine ulaşılamadı | Ayrıntılar |
    | vi | Mẹo: cài đặt <link>Ollama</link> và chạy <code>ollama serve</code> để dùng các mô hình cục bộ. | Kiểm tra kết nối | Đã kết nối — {{count}} mô hình khả dụng | Không thể truy cập {{baseUrl}} | Chi tiết |
    | ar | نصيحة: ثبّت <link>Ollama</link> وشغّل <code>ollama serve</code> لاستخدام النماذج المحلية. | اختبار الاتصال | متصل — {{count}} نماذج متاحة | تعذر الوصول إلى {{baseUrl}} | التفاصيل |
    | he | טיפ: התקן את <link>Ollama</link> והרץ <code>ollama serve</code> כדי להשתמש במודלים מקומיים. | בדוק חיבור | מחובר — {{count}} מודלים זמינים | לא ניתן להגיע אל {{baseUrl}} | פרטים |

    | Locale | networkSurface.title | networkSurface.description | networkSurface.button | groups.postProcessing |
    |--------|----------------------|-----------------------------|------------------------|------------------------|
    | de | Netzwerkoberfläche | Jeder ausgehende Endpunkt, den diese App erreichen kann, und wie man ihn deaktiviert. | Datenschutzdokument öffnen | Nachbearbeitung |
    | fr | Surface réseau | Chaque point de terminaison sortant que cette application peut contacter, et comment le désactiver. | Voir le document de confidentialité | Post-traitement |
    | es | Superficie de red | Cada endpoint saliente que esta app puede contactar y cómo deshabilitar cada uno. | Ver doc de privacidad | Postprocesamiento |
    | it | Superficie di rete | Ogni endpoint in uscita che questa app può contattare, e come disabilitare ciascuno. | Visualizza doc privacy | Post-elaborazione |
    | pt | Superfície de rede | Todos os endpoints externos que este app pode contactar, e como desativar cada um. | Ver doc de privacidade | Pós-processamento |
    | ja | ネットワークサーフェス | このアプリが接続できるすべての送信エンドポイントと、それぞれの無効化方法。 | プライバシードキュメントを表示 | 後処理 |
    | ko | 네트워크 표면 | 이 앱이 연결할 수 있는 모든 외부 엔드포인트와 각각을 비활성화하는 방법. | 개인정보 문서 보기 | 후처리 |
    | zh | 网络范围 | 此应用可联系的每个出站端点，以及如何禁用每个端点。 | 查看隐私文档 | 后处理 |
    | zh-TW | 網路範圍 | 此應用程式可連線的每個出站端點，以及如何停用每個端點。 | 檢視隱私文件 | 後處理 |
    | ru | Сетевая поверхность | Каждая внешняя конечная точка, с которой может связаться приложение, и как её отключить. | Открыть документ о приватности | Постобработка |
    | uk | Мережева поверхня | Кожна зовнішня кінцева точка, до якої може звертатися застосунок, і як її вимкнути. | Відкрити документ про приватність | Постобробка |
    | pl | Powierzchnia sieciowa | Każdy zewnętrzny punkt końcowy, z którym aplikacja może się skontaktować, i jak go wyłączyć. | Zobacz dokument prywatności | Przetwarzanie końcowe |
    | cs | Síťová plocha | Každý odchozí koncový bod, který může aplikace kontaktovat, a jak ho zakázat. | Zobrazit dokument o soukromí | Následné zpracování |
    | bg | Мрежова повърхност | Всяка изходяща крайна точка, с която приложението може да се свърже, и как да я деактивирате. | Виж документа за поверителност | Последваща обработка |
    | sv | Nätverksyta | Varje utgående slutpunkt som denna app kan kontakta, och hur du inaktiverar var och en. | Visa integritetsdokument | Efterbehandling |
    | tr | Ağ yüzeyi | Bu uygulamanın ulaşabileceği her giden uç nokta ve her birini nasıl devre dışı bırakacağınız. | Gizlilik belgesini görüntüle | Son işleme |
    | vi | Bề mặt mạng | Mọi điểm cuối đi mà ứng dụng có thể liên hệ, và cách vô hiệu hóa từng điểm. | Xem tài liệu bảo mật | Hậu xử lý |
    | ar | السطح الشبكي | كل نقطة نهاية صادرة يمكن لهذا التطبيق الاتصال بها، وكيفية تعطيل كل منها. | عرض وثيقة الخصوصية | المعالجة اللاحقة |
    | he | משטח רשת | כל נקודת קצה יוצאת שאפליקציה זו יכולה לפנות אליה, וכיצד להשבית כל אחת. | הצג מסמך פרטיות | עיבוד בדיעבד |

    JSON validity rules for EVERY file:
    - Preserve UTF-8 encoding
    - Preserve existing keys verbatim
    - Place new sub-objects in the SAME hierarchical position as in en/translation.json (alphabetical order inside parents is NOT required — match en/'s order: `providers` after `model`, `custom` after `providers`; `networkSurface` after `privacy`; `postProcessing` inside `groups` after `experimental`)
    - Trailing commas: JSON does NOT allow trailing commas — verify each file parses

    After all 19 files are updated, run `bun run check:translations` to verify parity. If it reports any missing keys, fix the affected file before continuing.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-desktop && for f in src/i18n/locales/*/translation.json; do bun -e "JSON.parse(require('fs').readFileSync('$f','utf8'))" || (echo "INVALID: $f" && exit 1); done && bun run check:translations</automated>
  </verify>
  <acceptance_criteria>
    - Every file in `src/i18n/locales/*/translation.json` parses as valid JSON (the loop in `<automated>` exits 0)
    - `bun run check:translations` exits 0 (full parity)
    - In each of the 19 sibling locales, `grep -c 'sectionLocal' src/i18n/locales/<locale>/translation.json` >= 1
    - In each of the 19 sibling locales, `grep -c 'sectionExternal' src/i18n/locales/<locale>/translation.json` >= 1
    - In each of the 19 sibling locales, `grep -c 'ollamaTip' src/i18n/locales/<locale>/translation.json` >= 1
    - In each of the 19 sibling locales, `grep -c 'testConnection' src/i18n/locales/<locale>/translation.json` >= 1
    - In each of the 19 sibling locales, `grep -c 'networkSurface' src/i18n/locales/<locale>/translation.json` >= 1
    - In each of the 19 sibling locales, `grep -c '"postProcessing"' src/i18n/locales/<locale>/translation.json` >= 1 (the new groups key; existing keys with `postProcessing` elsewhere are fine but this minimum check is the new addition)
    - `grep -c '<link>Ollama</link>' src/i18n/locales/<every-locale>/translation.json` >= 1 (brand name preserved, link placeholder preserved)
    - `grep -c '<code>ollama serve</code>' src/i18n/locales/<every-locale>/translation.json` >= 1
    - `grep -c '{{count}}' src/i18n/locales/<every-locale>/translation.json` >= 1 (interpolation preserved)
    - `grep -c '{{baseUrl}}' src/i18n/locales/<every-locale>/translation.json` >= 1
    - `bun run lint` exits 0
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>
    All 19 sibling locale files contain the 14 new keys with the agreed translations (or English fallback per CONTRIBUTING_TRANSLATIONS.md). `bun run check:translations` reports full parity. Build + lint green.
  </done>
</task>

</tasks>

<verification>
- `bun run check:translations` — full key-parity green across all 20 locales
- `bun run lint` — ESLint clean
- `bun run format:check` — Prettier clean
- `bun run build` — Vite + TypeScript build green
- Sanity check: open `src/i18n/locales/fr/translation.json` and confirm the new `providers`, `custom`, `networkSurface`, and `groups.postProcessing` keys are present with French strings
</verification>

<success_criteria>
- All 20 locale files have parity for the 14 new keys
- Brand names (Ollama, Apple Intelligence, etc.) preserved verbatim in every locale
- Interpolation placeholders and JSX component placeholders preserved verbatim
- check:translations exits 0; build exits 0; lint exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-privacy-local-first-ux/08-04-SUMMARY.md` documenting:
- All 19 locale files modified (listed)
- Confirmation of parity via `bun run check:translations` output
- Note any locale where English fallback was used (deviation from the translation table above)
</output>
