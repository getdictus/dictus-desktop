---
phase: 13-smart-modes-ui-translation-presets-i18n
plan: "08"
type: execute
wave: 3
depends_on: ["13-05", "13-06", "13-07"]
files_modified: []
autonomous: false
gap_closure: true
requirements: [MODE-03, MODE-04, MODE-05, MODE-06, TRANS-01, TRANS-02, MODE-02]

must_haves:
  truths:
    - "All 8 UAT gaps are confirmed closed in a running build"
  artifacts: []
  key_links: []
---

<objective>
Re-run the failed UAT tests against a live build to confirm the 8 gaps from 13-UAT.md are closed before re-closing Phase 13. This is a verification-only plan; no code is written here.

Purpose: the gaps were surfaced by live UAT, so they must be re-verified live.
Output: user confirmation (or new gap reports) for each previously-failed test.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-UAT.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-05-PLAN.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-06-PLAN.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-07-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-checkpoint automated gates</name>
  <read_first>
    - .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-05-PLAN.md (backend acceptance)
    - .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-06-PLAN.md and 13-07-PLAN.md (frontend acceptance)
  </read_first>
  <action>
    Run the full automated suite to confirm 13-05/06/07 landed clean before asking the user to verify:
    - `bun run lint`
    - `bun run check:translations`
    - `cd src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test -p dictus-desktop`
    - Confirm regenerated bindings exist: `grep -q clearSmartModeBinding src/bindings.ts && grep -q smartModeTemplates src/bindings.ts` (if missing, run `bun run tauri dev` once to regenerate, then stop it).
    If any gate fails, STOP and report which 13-05/06/07 acceptance criterion regressed instead of proceeding to the checkpoint.
  </action>
  <verify>
    <automated>bun run lint && bun run check:translations && cd src-tauri && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5 && cargo test -p dictus-desktop 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `bun run lint` exits 0
    - `bun run check:translations` exits 0
    - `cd src-tauri && cargo clippy --all-targets -- -D warnings` exits 0
    - `cd src-tauri && cargo test -p dictus-desktop` passes
    - `grep -q clearSmartModeBinding src/bindings.ts` and `grep -q smartModeTemplates src/bindings.ts` both exit 0
  </acceptance_criteria>
  <done>All automated gates green; bindings regenerated; ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-verify the 8 UAT gaps in a live build</name>
  <action>Launch the dev build and walk the user through re-running each of the 8 previously-failed UAT behaviors (see how-to-verify). Collect pass/fail per gap; record any remaining failure as a new gap.</action>
  <what-built>
    Gap-closure for the 8 UAT gaps: edit-aware card titles + "Prompt" label (13-07), keydown-commit shortcut capture + clear button + compact chip (13-06), legacy post-process shortcut removed + Clean-Up-only seed + template catalogue (13-05), template picker + single "+" + changeable translation engine (13-07).
  </what-built>
  <how-to-verify>
    Run `bun run tauri dev` (on macOS: `CMAKE_POLICY_VERSION_MINIMUM=3.5 bun run tauri dev` if cmake errors). Open Settings → Post-processing. Verify each previously-failed UAT test:

    1. GAP 1 (test 2): Edit a seeded mode (e.g. Clean Up), change its title to something else, Save. The card must immediately show the NEW title. Switch app language → a mode you did NOT rename still localizes; the one you renamed keeps your literal name.
    2. GAP 2 (test 2): In the edit form, the prompt field label reads "Prompt" (in FR: "Prompt", not "Libellé du prompt").
    3. GAP 3 (test 3): Click a card's shortcut chip and press Cmd+1 (macOS) / Ctrl+1. It must bind and STAY bound (no appear-then-disappear). Also bind a bare single key (e.g. "2") on another card — it binds.
    4. GAP 4 (test 3): A bound shortcut shows a clear (X) control; clicking it removes the binding (chip returns to "Add shortcut").
    5. GAP 5 (test 3): The old "Option"/post-process shortcut no longer triggers a hidden transcription. If you upgraded from an older settings file, the previous post-process combo now appears on the Clean Up card's chip (visible + editable), not firing invisibly.
    6. GAP 6 (test 3): The shortcut control is a compact chip on the RIGHT of the card, not a full-width bar.
    7. GAP 7 (test 4): After enabling translation, a "Change engine" control is always available; switching between the active model and TranslateGemma (and back) works and persists.
    8. GAP 8 (test 6): On a fresh profile only "Clean Up" is seeded. The "+" button shows a SINGLE plus (not "+ + New Rewrite"). Clicking "+" opens a picker of predefined templates + a "Custom" option. Delete a default, then recreate it from the picker.

    Reply "approved" if all 8 pass, or describe any that still fail (which gap #, what happened).
  </how-to-verify>
  <resume-signal>Type "approved" or describe remaining issues per gap number.</resume-signal>
</task>

</tasks>

<verification>
- All automated gates pass (Task 1)
- User confirms all 8 previously-failed behaviors now pass (Task 2)
</verification>

<success_criteria>
- The 8 UAT gaps are confirmed closed in a running build, or any remaining failures are captured for a follow-up gap-closure pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-08-SUMMARY.md`. If the user reports remaining failures, record them as new gaps for another `/gsd:plan-phase --gaps` pass.
</output>
