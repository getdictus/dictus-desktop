---
phase: 13-smart-modes-ui-translation-presets-i18n
plan: 24
type: execute
wave: 4
depends_on: [13-20, 13-21, 13-22, 13-23]
files_modified: []
requirements: [L10N-01, MODE-05, TRANS-01]
autonomous: false
gap_closure: true
tags: [gap-closure, G13, G14, G15, checkpoint, human-verify, uat]

must_haves:
  truths:
    - "On a live build under French, the Post-processing screen and model library show NO English leakage"
    - "The shortcut conflict messages render in French AND distinguish exact-duplicate from base-key collision"
    - "The Gemma 3 4B description leads on translation quality in French"
  artifacts: []
  key_links:
    - from: "live FR build"
      to: "user verification"
      via: "human-in-the-loop UAT (not a grep)"
      pattern: ""
---

<objective>
Final human verification of all three gaps on a LIVE build (the [G14] "no English leakage" check is a human-in-the-loop UAT step per the deep-work rules, not an automated grep). Automated gates (check:translations, cargo build/clippy, lint, tsc, the untranslated detector) were proven in 13-20…13-23; this plan confirms the user-observable result and closes the gaps.

Purpose: confirm [G13], [G14], [G15] are resolved as the user experiences them.
Output: pass/fail UAT result; on pass, the phase's three remaining gaps are closed.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-UAT.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-20-PLAN.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-23-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all automated gates before the live checkpoint</name>
  <files>(none — runs existing gate commands, modifies nothing)</files>
  <read_first>
    - .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-22-PLAN.md (the new check:translations:untranslated gate)
  </read_first>
  <action>
    Run the full automated gate set and confirm each is green before handing off to the user:
      1. `bun run check:translations`
      2. `bun run check:translations:untranslated`
      3. `bun run lint`
      4. `bunx tsc --noEmit`
      5. `cd src-tauri && cargo build && cargo clippy --all-targets -- -D warnings && cargo test --lib shortcut::`
    If any gate fails, STOP and report which — do not proceed to the human checkpoint with a red gate.
  </action>
  <verify>
    <automated>bun run check:translations && bun run check:translations:untranslated && bun run lint && bunx tsc --noEmit && cd src-tauri && cargo test --lib shortcut:: 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All five gate commands exit 0; `check:translations:untranslated` reports no non-allowlisted English-fallback values
  </acceptance_criteria>
  <done>All automated gates green; ready for live human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live FR build verification of [G13] + [G14] + [G15]</name>
  <files>(none — human-verify checkpoint, no files modified)</files>
  <read_first>
    - .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-UAT.md (gaps [G13][G14][G15] and their truths)
  </read_first>
  <action>
    Pause for the user to run a live French build and confirm the six checks in <how-to-verify>. This is a human-in-the-loop UAT step — the [G14] "no English leakage" check cannot be a grep. Present the steps, then wait for the resume signal. Do not auto-resolve.
  </action>
  <what-built>
    [G13] Shortcut conflict messages are now structured backend codes (SHORTCUT_CONFLICT|...) mapped to localized t() strings, with a distinct base-key-collision message.
    [G14] The whole post-processing screen + model library smartModes.*/debt keys are really translated across all 19 non-English locales (deferred English-fallback debt cleared).
    [G15] The Gemma 3 4B description now leads on translation quality.
  </what-built>
  <how-to-verify>
    Build and run the app (dev is sufficient): `bun run tauri dev` (on macOS, if cmake error: `CMAKE_POLICY_VERSION_MINIMUM=3.5 bun run tauri dev`).

    Set the app language to FRANÇAIS (Settings → app language), then:

    1. [G14] Post-processing screen — open Settings → Post-processing (Smart Modes). Confirm NO English leaks: the "Rewrite"/"Translation" section labels, the type badges, the "Add shortcut" chip text, "New Rewrite"/"New Translation" buttons, the edit-form labels (Name/Prompt/Save mode/Discard), the template picker, and the translation modal copy are ALL in French.
    2. [G14] Model library — confirm the local-model library cards (descriptions, buttons, empty states) are in French.
    3. [G14] Spot-check ONE other locale (e.g. Deutsch or Español): re-open the post-processing screen and confirm no English leakage.
    4. [G13] Conflict — exact duplicate: bind a combo (e.g. Cmd+4) to one mode, then try to bind the SAME combo to another mode. Confirm the inline red error is in FRENCH and reads as "already used by {mode name}".
    5. [G13] Conflict — base-key: with a base key already bound (e.g. Cmd Left), try to bind a combo that STARTS with it (e.g. Cmd Left + 3). Confirm the inline error is in FRENCH AND is a DISTINCT message about the start/base of the shortcut colliding (not the same sentence as step 4).
    6. [G15] Gemma description — in the model library, confirm the Gemma 3 4B card description LEADS on translation (e.g. "Excellent pour la traduction — polyvalent et multilingue"), not "excellent en français".
  </how-to-verify>
  <verify>
    <automated>MISSING — human-verify only; live FR build inspection cannot be automated (the [G14] no-English-leakage check is a visual UAT step). Automated gates run in Task 1.</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all six checks pass on the live FR build (plus the spot-check locale shows no English)
  </acceptance_criteria>
  <done>User approves all six live checks; [G13]/[G14]/[G15] confirmed resolved.</done>
  <resume-signal>Type "approved" if all six checks pass, or describe which check failed (which screen still shows English, which conflict message is wrong, etc.)</resume-signal>
</task>

</tasks>

<verification>
- All automated gates green (Task 1).
- User confirms [G13]/[G14]/[G15] resolved on a live French build with a second-locale spot check (Task 2).
</verification>

<success_criteria>
- The three remaining Phase 13 gaps are verified closed on a live build; on approval the phase can be marked complete.
</success_criteria>

<output>
After completion, create `.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-24-SUMMARY.md` recording the UAT result and updating 13-UAT.md gap statuses for [G13]/[G14]/[G15].
</output>
