---
phase: 13-smart-modes-ui-translation-presets-i18n
plan: 23
type: execute
wave: 3
depends_on: [13-21, 13-22]
files_modified:
  - src/i18n/locales/ar/translation.json
  - src/i18n/locales/bg/translation.json
  - src/i18n/locales/cs/translation.json
  - src/i18n/locales/de/translation.json
  - src/i18n/locales/es/translation.json
  - src/i18n/locales/fr/translation.json
  - src/i18n/locales/he/translation.json
  - src/i18n/locales/it/translation.json
  - src/i18n/locales/ja/translation.json
  - src/i18n/locales/ko/translation.json
  - src/i18n/locales/pl/translation.json
  - src/i18n/locales/pt/translation.json
  - src/i18n/locales/ru/translation.json
  - src/i18n/locales/sv/translation.json
  - src/i18n/locales/tr/translation.json
  - src/i18n/locales/uk/translation.json
  - src/i18n/locales/vi/translation.json
  - src/i18n/locales/zh/translation.json
  - src/i18n/locales/zh-TW/translation.json
requirements: [L10N-01, TRANS-01, TRANS-02]
autonomous: true
gap_closure: true
tags: [gap-closure, G14, G15, i18n, translation, rtl]

must_haves:
  truths:
    - "Every smartModes.* UI key in all 19 non-English locales holds a real translation, not the English fallback"
    - "The remaining whole-app debt keys (errors.boundary.*, simulateUpdaterRestart.*, translateGemma4b.description) are translated in all 19 locales"
    - "The reworded Gemma 3 4B description ([G15]) is propagated to all 19 locales, leading on translation"
    - "The two [G13] conflict-message keys are translated (not English fallback) in all 19 locales"
    - "ar/he values are JSON-safe (no inner double-quotes breaking the file) per the 13-15 precedent"
  artifacts:
    - path: "src/i18n/locales/fr/translation.json"
      provides: "real French smartModes.* + debt + Gemma + conflict translations"
      contains: "smartModes"
  key_links:
    - from: "src/i18n/locales/*/translation.json"
      to: "scripts/check-translations.ts --check-untranslated"
      via: "no non-allowlisted value equals the en source"
      pattern: "check:translations:untranslated"
---

<objective>
Close the data half of gap [G14] (and propagate [G15]). Translate, into all 19 non-English locales, every key in the 13-22 "To translate" audit list:

- the 62 `smartModes.*` UI keys (sections, card.*, picker.*, shortcut.*, kind.*, translation.* incl. modal.*, delete.*, edit.*, noModel.*) — the post-processing screen that leaked English under French
- the 2 new [G13] keys added in 13-21 (`smartModes.card.shortcutConflictBase`, `smartModes.card.shortcutBaseHint`) — currently English fallback
- the ~5 non-smartModes debt keys: `errors.boundary.title`, `errors.boundary.reload`, `settings.debug.simulateUpdaterRestart.{title,button,description}`
- the reworded Gemma 3 4B description ([G15]): `settings.postProcessing.modelsAndLocalProcessing.library.models.translateGemma4b.description` AND `...gemma3_4b.description` (the latter reworded EN in 13-22 — propagate the new "Excellent for translation — versatile and multilingual" sense)

Do NOT touch the allowlist keys (proper nouns / literals) or `smartModes.defaultModes.*` (already translated in 13-15).

This REVERSES the 13-02/13-15 "names-only / translations deferred" decision per STATE.md scope pin (commit 1135834: whole app, all 19 languages). Live-build FR verification is a separate checkpoint (13-24).

Purpose: real localization of the post-processing screen + model library across all shipped languages.
Output: 19 locale files with quality translations; `check:translations:untranslated` clean for the translated set.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-i18n-audit.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-22-PLAN.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-21-PLAN.md
@src/i18n/locales/en/translation.json

<interfaces>
<!-- Translation conventions established in-phase -->
<!-- - Interpolation placeholders {{name}}, {{base}}, {{combo}}, {{engine}}, {{model}}, {{percent}} MUST be preserved verbatim in every locale. -->
<!-- - ${output} is a LITERAL token (the transcript injection marker) — keep it literal, untranslated, in smartModes.card.promptPlaceholder / outputHint. -->
<!-- - "Gemma 3 4B", "TranslateGemma", "GGUF", "LLM" are product/proper nouns — keep verbatim. -->
<!-- - 13-15 RTL precedent: ar/he values must avoid inner ASCII double-quotes that would break JSON (use guillemets/quote-free forms, e.g. he writeAsEmail used a quote-free form). -->

[G15] EN source (reworded in 13-22) to translate per locale, leading on translation:
  gemma3_4b.description (en) = "Excellent for translation — versatile and multilingual"
  Keep consistent with translation.modal.recommendedSubtext tone (Gemma recommended for translation, [G12]/13-19).

defaultModes.* are ALREADY localized (13-15) — leave untouched.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate the smartModes.* UI block + [G13] conflict keys in all 19 locales</name>
  <read_first>
    - .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-i18n-audit.md (the exact smartModes.* key list to translate — do not invent keys, do not touch defaultModes.*)
    - src/i18n/locales/en/translation.json `smartModes` block (the English source values to translate from)
    - src/i18n/locales/fr/translation.json `smartModes.defaultModes` block (an already-translated reference showing expected FR quality/tone and the 13-15 RTL/quote conventions)
  </read_first>
  <action>
    For EACH of the 19 locales (ar, bg, cs, de, es, fr, he, it, ja, ko, pl, pt, ru, sv, tr, uk, vi, zh, zh-TW), replace the English-fallback value of every `smartModes.*` key in the audit "To translate" list (including the 2 new [G13] keys `card.shortcutConflictBase` and `card.shortcutBaseHint`) with a real, natural translation. Constraints:
      - Preserve ALL `{{...}}` placeholders verbatim ({{name}}, {{base}}, {{combo}}, {{engine}}, {{model}}, {{percent}}).
      - Keep the literal token `${output}` untranslated in `card.promptPlaceholder` and `card.outputHint`.
      - Keep product nouns verbatim: "Gemma 3 4B", "TranslateGemma", "GGUF", "LLM".
      - Match each locale's existing tone/register seen in its `smartModes.defaultModes.*` (from 13-15) and the broader `settings.*` translations.
      - RTL (ar, he): no inner ASCII double-quotes inside a JSON string value; use the language's quotation marks or a quote-free phrasing (13-15 precedent). The base-overlap message em dash is fine.
      - Do NOT modify `smartModes.defaultModes.*` (already done in 13-15) and do NOT modify any allowlist key.
    Preserve each file's key ordering and 2-space indentation; only the string VALUES change.
  </action>
  <verify>
    <automated>bun run check:translations 2>&1 | tail -3 && for f in src/i18n/locales/*/translation.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8'))" || echo "INVALID: $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - `bun run check:translations` exits 0 (all keys still present, all JSON valid)
    - Every locale file parses as valid JSON (the loop prints no "INVALID:" line)
    - Spot check FR: `python3 -c "import json;d=json.load(open('src/i18n/locales/fr/translation.json'));sm=d['smartModes'];print(sm['sections']['rewrite'], sm['card']['saveCta'])"` shows French (NOT "Rewrite"/"Save mode")
    - `${output}` still present literally: `grep -c '${output}' src/i18n/locales/fr/translation.json` ≥ 1
    - Placeholders preserved: `grep -c "{{name}}" src/i18n/locales/de/translation.json` ≥ 1
  </acceptance_criteria>
  <done>All smartModes.* UI keys + the 2 [G13] keys are really translated in all 19 locales; placeholders/${output} intact; JSON valid; check:translations green.</done>
</task>

<task type="auto">
  <name>Task 2: Translate remaining whole-app debt keys + propagate reworded Gemma description</name>
  <read_first>
    - .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-i18n-audit.md ("To translate" non-smartModes entries + the [G15] Gemma keys)
    - src/i18n/locales/en/translation.json for the en values of: errors.boundary.{title,reload}, settings.debug.simulateUpdaterRestart.{title,button,description}, library.models.gemma3_4b.description (reworded by 13-22), library.models.translateGemma4b.description
    - src/i18n/locales/fr/translation.json (reference tone for settings.* and errors.*)
  </read_first>
  <action>
    For EACH of the 19 locales, translate these remaining debt keys to real values:
      - `errors.boundary.title` ("Something went wrong.") and `errors.boundary.reload` ("Reload")
      - `settings.debug.simulateUpdaterRestart.title` / `.button` / `.description` (keep the technical reference "SHUT-03", "tauri-plugin-updater" verbatim inside the description)
      - `settings.postProcessing.modelsAndLocalProcessing.library.models.translateGemma4b.description` ("Dedicated translation model — 55 benchmarked languages") — keep "TranslateGemma"/numbers verbatim
      - `...library.models.gemma3_4b.description` — translate the NEW reworded EN ("Excellent for translation — versatile and multilingual") so each locale LEADS on translation. For FR specifically use: "Excellent pour la traduction — polyvalent et multilingue" (user's example). Keep "Gemma 3 4B" verbatim where the surrounding UI shows it; the description string itself has no model name.
    RTL (ar, he): JSON-quote-safe values (no inner ASCII double-quotes). Preserve indentation/ordering.

    Do NOT touch allowlist keys (onboarding.models.*.name, acknowledgments titles, autoSubmit options, api placeholders).
  </action>
  <verify>
    <automated>bun run check:translations 2>&1 | tail -3 && bun run check:translations:untranslated 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `bun run check:translations` exits 0
    - FR Gemma leads on translation: `python3 -c "import json;d=json.load(open('src/i18n/locales/fr/translation.json'));print(d['settings']['postProcessing']['modelsAndLocalProcessing']['library']['models']['gemma3_4b']['description'])"` outputs a string starting with "Excellent pour la traduction"
    - `python3 -c "import json;d=json.load(open('src/i18n/locales/de/translation.json'));print(d['errors']['boundary']['title'])"` is NOT "Something went wrong." (translated)
    - `bun run check:translations:untranslated` exits 0 (no non-allowlisted English-fallback values remain) — the [G14] debt is cleared by the allowlisted detector from 13-22
  </acceptance_criteria>
  <done>All remaining debt keys + the reworded Gemma description are translated in all 19 locales; the allowlisted untranslated-detector passes clean.</done>
</task>

</tasks>

<verification>
- `bun run check:translations` exits 0 (presence parity, valid JSON).
- `bun run check:translations:untranslated` exits 0 (no non-allowlisted en-fallback remains).
- FR/DE/ES spot checks show real translations; placeholders and ${output} preserved; ar/he JSON valid.
</verification>

<success_criteria>
- The whole-app deferred-translation debt for the [G14] scope is cleared with quality translations; [G15] reworded description propagated leading on translation; [G13] conflict messages localized.
</success_criteria>

<output>
After completion, create `.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-23-SUMMARY.md`
</output>
