---
phase: 13-smart-modes-ui-translation-presets-i18n
plan: 22
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-translations.ts
  - src/i18n/locales/en/translation.json
  - src-tauri/src/managers/llm.rs
  - .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-i18n-audit.md
requirements: [L10N-01, TRANS-01, TRANS-02]
autonomous: true
gap_closure: true
tags: [gap-closure, G14, G15, tooling, audit, i18n, rust]

must_haves:
  truths:
    - "A concrete, committed audit lists every i18n key whose non-English value still equals the English source across the whole app (the [G14] translation work-list)"
    - "check:translations can flag English-fallback values (not just missing keys) so deferred-translation debt cannot silently recur"
    - "The Gemma 3 4B EN-source description leads on translation quality, in BOTH the i18n source key and the llm.rs catalogue fallback string ([G15])"
  artifacts:
    - path: ".planning/phases/13-smart-modes-ui-translation-presets-i18n/13-i18n-audit.md"
      provides: "the canonical key list 13-23 must translate"
      contains: "smartModes."
    - path: "scripts/check-translations.ts"
      provides: "--check-untranslated mode flagging en==locale values"
      contains: "untranslated"
  key_links:
    - from: "scripts/check-translations.ts"
      to: "src/i18n/locales/*/translation.json"
      via: "value-equality scan against en source with an allowlist of legitimately-identical keys"
      pattern: "REFERENCE_LANG"
---

<objective>
Set up gap [G14] (and do the [G15] EN-source rework) without touching the 19 locale files yet:

1. AUDIT — produce a committed list of every i18n key whose non-English value still equals the English source. Live measurement (2026-06-08) found 91 such keys in ALL 19 locales: 62 under `smartModes.*` (the post-processing screen the user complained about), plus ~7 genuinely-translatable keys elsewhere (`errors.boundary.*`, `settings.debug.simulateUpdaterRestart.*`, `library.models.translateGemma4b.description`); the remaining ~22 are proper nouns / literals (model brand names, `Cmd+Enter`, `sk-...`, acknowledgment titles) that must STAY identical. The audit must separate "to translate" from "legitimately identical (allowlist)".

2. TOOLING — extend `check:translations` with an opt-in `--check-untranslated` mode that flags keys whose locale value equals the en value, minus an allowlist, so this debt cannot silently recur. Keep the default (presence-only) behavior unchanged so existing CI stays green.

3. [G15] EN SOURCE — reword the Gemma 3 4B description to LEAD on translation quality, in the i18n source key AND the Rust catalogue fallback. This must happen in the EN source FIRST (13-23 then propagates the reworded string to the 19 locales).

This plan is independent of the [G13] plans (13-20/13-21) and can run in the same wave. The bulk locale translation is plan 13-23 (depends on this audit + 13-21's new keys).

Purpose: a measured work-list + a guardrail + the [G15] EN baseline.
Output: 13-i18n-audit.md, an extended check script, reworded Gemma EN description.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-UAT.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-19-SUMMARY.md
@scripts/check-translations.ts

<interfaces>
<!-- check-translations.ts existing helpers to reuse -->
<!-- getAllKeyPaths(obj) -> string[][]; loadTranslationFile(lang) -> TranslationData|null; -->
<!-- hasKeyPath(obj, keyPath) -> boolean; LANGUAGES (19 non-en dirs); REFERENCE_LANG="en" -->

[G15] Gemma description lives in TWO places (both must be reworded):
- i18n source: src/i18n/locales/en/translation.json
    settings.postProcessing.modelsAndLocalProcessing.library.models.gemma3_4b.description
    = "Versatile and multilingual — strong in many languages"   (this is what RENDERS)
- Rust fallback: src-tauri/src/managers/llm.rs ~line 135
    description: "Versatile and multilingual — strong in many languages".to_string()  (defaultValue)
  LlmLibrarySection.tsx:115-118 renders t(`...gemma3_4b.description`, { defaultValue: m.description }),
  so the i18n value wins; the Rust string is the fallback — keep both in sync.

[G12]/13-19 recommendation copy to stay consistent with (translation.modal.recommendedSubtext):
  "A versatile 4B model with clean, reliable translation across languages and idioms. ~2.5 GB download."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and commit the i18n English-fallback audit</name>
  <read_first>
    - src/i18n/locales/en/translation.json (the source — 523 leaf keys) and one reference locale src/i18n/locales/fr/translation.json (to confirm which values are still English)
    - scripts/check-translations.ts (getAllKeyPaths + loadTranslationFile helpers to reuse for the scan)
  </read_first>
  <action>
    Run a one-off scan (a throwaway bun/node snippet, or `bun scripts/check-translations.ts --check-untranslated` after Task 2 — but produce the audit doc regardless) that, for every leaf key in en, counts how many of the 19 non-English locales hold a value byte-equal to the en value.

    Write `.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-i18n-audit.md` with three sections:
      ## To translate (English-fallback debt) — keys equal-to-en in ALL 19 locales, EXCLUDING the allowlist. Group by namespace. This is the authoritative list 13-23 translates. Expected ~69 keys:
         - all 62 `smartModes.*` UI keys (sections, card.*, picker.*, shortcut.*, kind.*, translation.* incl. modal.*, delete.*, edit.*, noModel.*) — note `smartModes.defaultModes.*` are ALREADY translated (13-15) and must NOT appear here
         - `errors.boundary.title`, `errors.boundary.reload`
         - `settings.debug.simulateUpdaterRestart.{title,button,description}`
         - `settings.postProcessing.modelsAndLocalProcessing.library.models.translateGemma4b.description`
      ## Allowlist (legitimately identical — DO NOT translate) — proper nouns, brand names, literals:
         - all `onboarding.models.*.name` (Whisper Large/Small/..., Parakeet V2/V3, Moonshine *, Canary *, SenseVoice, GigaAM v3, Cohere, Breeze ASR)
         - `settings.about.acknowledgments.handy.title` (Handy), `...whisper.title` (Whisper.cpp)
         - `settings.advanced.autoSubmit.options.{cmdEnter,ctrlEnter,superEnter}` (Cmd+Enter literals)
         - `settings.postProcessing.api.apiKey.placeholder` (sk-...), `...baseUrl.placeholder` (https URL)
      ## Partial (equal-to-en in SOME but not all locales) — list count only; out of [G14] scope (those locales chose to leave them; covered opportunistically by 13-23 if they fall in smartModes.*).

    Each entry in "To translate" must be the full dotted key path. Sort within each namespace.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-i18n-audit.md && grep -c "smartModes\." .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-i18n-audit.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-i18n-audit.md` exists
    - It contains a "## To translate" section listing ≥60 `smartModes.*` keys and the non-smartModes debt keys (errors.boundary.*, simulateUpdaterRestart.*, translateGemma4b.description)
    - It contains an "## Allowlist" section listing the onboarding.models.*.name + acknowledgments + autoSubmit + placeholder keys
    - `grep -c "smartModes\." 13-i18n-audit.md` returns ≥60
  </acceptance_criteria>
  <done>The audit doc enumerates the translate-set vs the keep-identical allowlist; it is the contract 13-23 executes against.</done>
</task>

<task type="auto">
  <name>Task 2: Add --check-untranslated mode to check-translations.ts with allowlist</name>
  <read_first>
    - scripts/check-translations.ts (full — getAllKeyPaths, hasKeyPath, validateTranslations, the process.exit gates; default behavior must be unchanged)
    - .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-i18n-audit.md (Task 1 output — the allowlist namespaces to hardcode)
  </read_first>
  <action>
    Add an opt-in CLI flag `--check-untranslated` (read `process.argv.includes("--check-untranslated")`). Default invocation (`bun run check:translations`, no flag) keeps the existing presence-only behavior and exit codes EXACTLY — do not change the default path.

    When the flag is present, after the presence check, additionally scan: for each non-en locale and each en leaf key whose en value is a non-empty string, flag keys where `localeValue === enValue`, EXCEPT keys matching an `UNTRANSLATED_ALLOWLIST` (an array of exact dotted paths + prefix matchers) covering the legitimately-identical set from the audit:
      - prefix `onboarding.models.` AND suffix `.name`
      - `settings.about.acknowledgments.handy.title`, `settings.about.acknowledgments.whisper.title`
      - prefix `settings.advanced.autoSubmit.options.`
      - `settings.postProcessing.api.apiKey.placeholder`, `settings.postProcessing.api.baseUrl.placeholder`
    Print a per-locale count + first 10 offending keys. Exit non-zero if any non-allowlisted English-fallback value is found (so post-13-23 this gate proves the debt is cleared). Add a one-line comment block at the top documenting the flag.

    Add a package.json script `"check:translations:untranslated": "bun scripts/check-translations.ts --check-untranslated"` (read package.json scripts block first; insert next to the existing `check:translations` entry, preserving JSON formatting).
  </action>
  <verify>
    <automated>bun run check:translations 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "check-untranslated" scripts/check-translations.ts` returns a match
    - `grep -n "UNTRANSLATED_ALLOWLIST" scripts/check-translations.ts` returns a match
    - `bun run check:translations` (no flag) still exits 0 with unchanged output (presence-only)
    - `bun run check:translations:untranslated` runs and currently exits NON-zero (the smartModes.* debt still present pre-13-23) — confirm it reports smartModes keys
  </acceptance_criteria>
  <done>The script gains an allowlisted English-fallback detector behind a flag; default behavior unchanged; new npm script added.</done>
</task>

<task type="auto">
  <name>Task 3: [G15] Reword Gemma 3 4B description (EN source) to lead on translation</name>
  <read_first>
    - src/i18n/locales/en/translation.json — `settings.postProcessing.modelsAndLocalProcessing.library.models.gemma3_4b.description` (currently "Versatile and multilingual — strong in many languages")
    - src-tauri/src/managers/llm.rs line ~135 (the `description: "Versatile and multilingual — strong in many languages".to_string()` catalogue fallback for id "gemma-3-4b")
    - .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-19-SUMMARY.md (translation recommendation copy — keep tone consistent)
  </read_first>
  <action>
    Reword to lead on translation quality (user example: "Excellent pour la traduction — polyvalent et multilingue"). Set the EN string in BOTH places to:

      "Excellent for translation — versatile and multilingual"

    1. src/i18n/locales/en/translation.json: set `...library.models.gemma3_4b.description` to the new EN string.
    2. src-tauri/src/managers/llm.rs ~135: set the `gemma-3-4b` catalogue entry `description` to the same new EN string (keeps the t() defaultValue in sync).

    Do NOT touch the 19 locale files here — 13-23 propagates the reworded string (this prevents 13-23 from translating the stale wording). Keep the em dash; no inner double-quotes (JSON-safe).
  </action>
  <verify>
    <automated>grep -rn "Excellent for translation — versatile and multilingual" src/i18n/locales/en/translation.json src-tauri/src/managers/llm.rs && cd src-tauri && cargo build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Excellent for translation — versatile and multilingual" src/i18n/locales/en/translation.json` returns the gemma3_4b key
    - `grep -n "Excellent for translation — versatile and multilingual" src-tauri/src/managers/llm.rs` returns the gemma-3-4b catalogue entry
    - `grep -n "strong in many languages" src/i18n/locales/en/translation.json src-tauri/src/managers/llm.rs` returns NOTHING (old wording gone)
    - `cd src-tauri && cargo build` exits 0
  </acceptance_criteria>
  <done>Gemma 3 4B EN description leads on translation in both the i18n source and the Rust catalogue fallback; old wording removed; build clean.</done>
</task>

</tasks>

<verification>
- 13-i18n-audit.md committed with translate-set + allowlist.
- check:translations default exits 0; --check-untranslated flags the current debt.
- Gemma EN description reworded in both source locations; cargo build clean.
</verification>

<success_criteria>
- The [G14] work-list is measured and committed, a recurrence guardrail exists, and the [G15] EN source is reworded ahead of locale propagation.
</success_criteria>

<output>
After completion, create `.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-22-SUMMARY.md`
</output>
