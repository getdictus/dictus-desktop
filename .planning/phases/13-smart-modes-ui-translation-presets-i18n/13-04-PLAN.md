---
phase: 13-smart-modes-ui-translation-presets-i18n
plan: 04
type: execute
wave: 3
depends_on: ["13-03"]
files_modified:
  - src/components/settings/post-processing/PostProcessingSettings.tsx
autonomous: false
requirements: [MODE-03, MODE-04, MODE-05, MODE-06, TRANS-01, TRANS-02]
must_haves:
  truths:
    - "The old single-prompt 'Prompts' SettingsGroup and the transcribe_with_post_process ShortcutInput are gone from the post-processing settings page"
    - "SmartModesSection renders in their place, below the Engine group"
    - "The full end-to-end flow (record -> transcribe -> Smart Mode shortcut fires -> embedded LLM responds -> output pasted) works in a running build"
  artifacts:
    - path: "src/components/settings/post-processing/PostProcessingSettings.tsx"
      provides: "SmartModesSection mounted; old prompt UI removed"
      contains: "SmartModesSection"
  key_links:
    - from: "src/components/settings/post-processing/PostProcessingSettings.tsx"
      to: "SmartModesSection"
      via: "import + render below Engine group"
      pattern: "SmartModesSection"
---

<objective>
Replace the legacy single-prompt post-processing UI with the new `SmartModesSection`: remove the `transcribe_with_post_process` ShortcutInput row and the entire "Prompts" SettingsGroup, mount `SmartModesSection` below the "Engine" group, and verify the complete end-to-end Smart Mode flow in a running build.

Purpose: This is the integration step that makes MODE-06's card list the actual surface users see (replacing the dropdown) and exercises the whole pipeline built across 13-01/02/03. The checkpoint confirms MODE-04/MODE-05/TRANS-01/TRANS-02 behaviors that only manifest in a running app.
Output: Updated `PostProcessingSettings.tsx`; verified working flow.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-CONTEXT.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-UI-SPEC.md

<interfaces>
<!-- Verified removal targets in src/components/settings/post-processing/PostProcessingSettings.tsx -->

Exported `PostProcessingSettings` component render (lines 650-711). Current structure (~693-709):
```tsx
<SettingsGroup title={t("settings.postProcessing.hotkey.title")}>   // lines 693-699 — REMOVE this whole group (only contains transcribe_with_post_process)
  <ShortcutInput shortcutId="transcribe_with_post_process" ... />
</SettingsGroup>
<SettingsGroup title={t("settings.postProcessing.api.title")}>      // 702-704 — KEEP unchanged
  <PostProcessingSettingsApi />
</SettingsGroup>
<SettingsGroup title={t("settings.postProcessing.prompts.title")}>  // 707-709 — REMOVE; replace with <SmartModesSection />
  <PostProcessingSettingsPrompts />
</SettingsGroup>
```
- `ShortcutInput` import at line 24; `PostProcessingSettingsPromptsComponent` defined at line 349; exported as `PostProcessingSettingsPrompts` at lines 624-627.
- IMPORTANT: before deleting the "Shortcuts/hotkey" group, confirm it contains ONLY the transcribe_with_post_process ShortcutInput (RESEARCH says it may be the only row). If it has other rows, remove only the transcribe_with_post_process row and keep the group. The raw `transcribe` binding lives in General settings, NOT here — do not touch it.
- The backend `transcribe_with_post_process` binding entry STAYS registered (Phase 12 migration); only its UI surface is removed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy prompt UI and mount SmartModesSection</name>
  <files>src/components/settings/post-processing/PostProcessingSettings.tsx</files>
  <read_first>
    - src/components/settings/post-processing/PostProcessingSettings.tsx (FULL file — the exported PostProcessingSettings render ~650-711, the PostProcessingSettingsPromptsComponent ~349-627, imports ~1-30)
    - src/components/settings/post-processing/SmartModesSection.tsx (the component created in 13-03 — confirm its export name and that it takes no required props)
  </read_first>
  <action>
    1. Add `import { SmartModesSection } from "./SmartModesSection";` near the other post-processing imports (~line 27, next to `LlmLibrarySection`).
    2. In the exported `PostProcessingSettings` render (~693-709):
       - REMOVE the entire "Prompts" `SettingsGroup` (the block rendering `<PostProcessingSettingsPrompts />`, ~707-709) and render `<SmartModesSection />` in its place (directly below the "Engine"/api SettingsGroup, NOT wrapped in another SettingsGroup — SmartModesSection renders its own SettingsGroups).
       - REMOVE the `transcribe_with_post_process` ShortcutInput: if the "hotkey/Shortcuts" SettingsGroup (~693-699) contains ONLY that ShortcutInput, remove the whole group; if it contains other rows, remove just the `<ShortcutInput shortcutId="transcribe_with_post_process" .../>` row.
    3. Clean up now-dead code (Workspace Hygiene): if `PostProcessingSettingsPromptsComponent` / the exported `PostProcessingSettingsPrompts` and the `ShortcutInput` import are no longer referenced ANYWHERE (grep the repo), delete them. If `PostProcessingSettingsPrompts` is imported elsewhere, keep the export but remove its usage here. Verify with grep before deleting.
    4. Do NOT remove `PostProcessingSettingsApi` or `LlmLibrarySection`.
    5. Ensure no unused-import or unused-var ESLint errors remain.
  </action>
  <verify>
    <automated>bun run lint 2>&1 | tail -10 && bun run check:translations 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "SmartModesSection" src/components/settings/post-processing/PostProcessingSettings.tsx` exits 0
    - `grep -q 'shortcutId="transcribe_with_post_process"' src/components/settings/post-processing/PostProcessingSettings.tsx` exits 1 (removed)
    - `grep -q "<PostProcessingSettingsPrompts" src/components/settings/post-processing/PostProcessingSettings.tsx` exits 1 (legacy prompt UI no longer rendered)
    - `bun run lint` exits 0 (no unused imports/vars)
    - `bun run check:translations` exits 0
  </acceptance_criteria>
  <done>The post-processing page renders SmartModesSection in place of the prompt dropdown; the transcribe_with_post_process ShortcutInput is gone; no dead-code lint errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E Smart Mode flow verification</name>
  <files>(manual verification — no files modified)</files>
  <action>Human-verify checkpoint. The executor does not edit files in this task — it builds/runs the app and the user performs the verification steps in &lt;how-to-verify&gt;. Pause for the resume signal.</action>
  <what-built>
    The complete Smart Modes UI is wired: card list replaces the prompt dropdown (MODE-06), per-mode shortcut binding with inline conflict (MODE-04/05), translation engine choice + offline translation execution (TRANS-01/02), all localized (L10N-01). This checkpoint confirms the behaviors that only manifest in a running app.
  </what-built>
  <how-to-verify>
    Build/run the app (a downloaded embedded LLM model must be present and selected as the post-processing provider):
    ```
    CMAKE_POLICY_VERSION_MINIMUM=3.5 bun run tauri dev
    ```
    Then in Settings -> Post-processing:
    1. CARD LIST (MODE-06): Confirm Smart Modes show as a vertical card list in two sections — "Rewrite" (Clean Up first) and "Translation" — NOT the old single-prompt dropdown. Seeded names are localized when you switch app language.
    2. CREATE/EDIT/DELETE (MODE-03): Click "+ New Rewrite" -> inline expanded card appears at the bottom -> fill name + prompt -> "Create mode" -> it appears as a card. Edit an existing card (Pencil) -> change name -> "Save mode" -> persists. Delete a card (Trash2) -> native confirm dialog -> confirm -> card disappears.
    3. SHORTCUT BIND + CONFLICT (MODE-04/MODE-05): Click the shortcut chip on a Rewrite card -> press a free combo -> chip shows it bound. Now click another card's chip -> press the SAME combo -> inline red text appears under the chip ("Already used by: ...") and the bind is BLOCKED (the second card stays unbound). Record audio with a bound mode's shortcut -> confirm that mode's prompt is applied to the output that gets pasted.
    4. TRANSLATION (TRANS-01/TRANS-02): Confirm the 4 Translation cards (EN/ES/FR/ZH) are greyed-out with an amber "Enable offline translation" CTA. Click "Choose translation engine" -> modal appears with both options -> pick "Use active model" (no download) -> cards become interactive. Bind a shortcut to "Translate to Spanish", record English speech, trigger it -> confirm Spanish translated text is pasted (works offline / no cloud).
    5. NO ACTIVE-MODE UI: Confirm there is no "set active" / primary post-process button anywhere — modes fire only via their own shortcuts.
  </how-to-verify>
  <verify>Manual — user confirms via resume signal.</verify>
  <done>All 5 behaviors confirmed working by the user (resume signal "approved").</done>
  <resume-signal>Type "approved" if all 5 behaviors work, or describe which step failed.</resume-signal>
</task>

</tasks>

<verification>
- `bun run lint` exits 0
- `bun run check:translations` exits 0
- `cd src-tauri && cargo test -p dictus-desktop` exits 0
- Manual E2E checkpoint approved (record -> transcribe -> Smart Mode fires -> embedded LLM -> paste; conflict blocked inline; translation runs offline)
</verification>

<success_criteria>
- The single-prompt dropdown is replaced by the Smart Modes card list (MODE-06)
- Create/edit/delete, per-mode shortcut bind with blocked inline conflict, and offline translation all verified end-to-end (MODE-03/04/05, TRANS-01/02)
</success_criteria>

<output>
After completion, create `.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-04-SUMMARY.md`
</output>
