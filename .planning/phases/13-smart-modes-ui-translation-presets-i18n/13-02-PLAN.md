---
phase: 13-smart-modes-ui-translation-presets-i18n
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/en/translation.json
  - src/i18n/locales/ar/translation.json
  - src/i18n/locales/bg/translation.json
  - src/i18n/locales/cs/translation.json
  - src/i18n/locales/de/translation.json
  - src/i18n/locales/es/translation.json
  - src/i18n/locales/fr/translation.json
  - src/i18n/locales/he/translation.json
  - src/i18n/locales/it/translation.json
  - src/i18n/locales/ja/translation.json
  - src/i18n/locales/ko/translation.json
  - src/i18n/locales/pl/translation.json
  - src/i18n/locales/pt/translation.json
  - src/i18n/locales/ru/translation.json
  - src/i18n/locales/sv/translation.json
  - src/i18n/locales/tr/translation.json
  - src/i18n/locales/uk/translation.json
  - src/i18n/locales/vi/translation.json
  - src/i18n/locales/zh/translation.json
  - src/i18n/locales/zh-TW/translation.json
autonomous: true
requirements: [L10N-01]
must_haves:
  truths:
    - "Every new Smart Modes UI / translation / default-mode-name string exists as an i18n key in en"
    - "All 19 non-English locales contain the identical key structure (English values as fallback)"
    - "bun run check:translations exits 0"
  artifacts:
    - path: "src/i18n/locales/en/translation.json"
      provides: "smartModes.* namespace source strings + translateGemma4b catalogue description"
      contains: "smartModes"
  key_links:
    - from: "src/i18n/locales/en/translation.json"
      to: "src/i18n/locales/{19 others}/translation.json"
      via: "identical key structure (check-translations enforces no missing/extra)"
      pattern: "smartModes"
---

<objective>
Add every new user-facing string for Phase 13 to the English source locale and propagate the identical key structure to all 19 other locales, so `bun run check:translations` passes with 0 errors (L10N-01).

Purpose: ESLint forbids hardcoded JSX strings; the frontend plans (13-03, 13-04) reference these exact keys. Defining them first means the UI plans can use `t('smartModes...')` without inventing keys. This plan is independent of the backend plan and runs in parallel (Wave 1).
Output: 20 updated `translation.json` files; `check:translations` green.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-CONTEXT.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-UI-SPEC.md

<notes>
- Baseline verified clean on 2026-06-03: `bun run check:translations` reported all 19 non-English locales complete. Any new failure is introduced by this plan.
- The check script (scripts/check-translations.ts) flags BOTH missing AND extra keys. Add keys to en first, then the IDENTICAL structure to all 19 others.
- 20 locale codes: ar bg cs de en es fr he it ja ko pl pt ru sv tr uk vi zh zh-TW (19 non-English).
- Real translations are deferred (Phase 11 precedent): use the English value as the placeholder in every locale. Do NOT hand-translate.
- Seeded mode names: only the SYSTEM-seeded set maps to i18n keys; user-created modes use their literal stored name (no key). The English values for default mode names are the ground truth below.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all smartModes.* keys to en/translation.json</name>
  <files>src/i18n/locales/en/translation.json</files>
  <read_first>
    - src/i18n/locales/en/translation.json (find the existing top-level key ordering and any existing `settings.postProcessing.modelsAndLocalProcessing.library.models.*` block to locate where translateGemma4b.description belongs — match the exact path used by qwen25_1b5 / gemma3_4b)
    - .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-UI-SPEC.md (Copywriting Contract table — the authoritative English copy for every key)
  </read_first>
  <action>
    Add a `smartModes` namespace (top-level, alphabetically placed if the file is ordered, otherwise appended consistently with sibling namespaces) with EXACTLY these keys and English values (verbatim from the UI-SPEC Copywriting Contract):

    ```json
    "smartModes": {
      "sections": { "rewrite": "Rewrite", "translation": "Translation" },
      "createRewrite": "+ New Rewrite",
      "createTranslation": "+ New Translation",
      "card": {
        "saveCta": "Save mode",
        "discardCta": "Discard changes",
        "createCta": "Create mode",
        "discardNewCta": "Discard",
        "addShortcut": "Add shortcut",
        "shortcutConflict": "Already used by: {{name}}",
        "targetLanguage": "Target language"
      },
      "kind": { "rewrite": "Rewrite", "translation": "Translation" },
      "translation": {
        "enableHeading": "Enable offline translation",
        "enableBody": "Translation runs fully offline through the embedded LLM. Choose an engine to get started.",
        "chooseCta": "Choose translation engine",
        "genericEngineNote": "Best-effort — quality varies by model",
        "modal": {
          "dedicatedHeading": "Download dedicated translation model (Recommended)",
          "dedicatedSubtext": "TranslateGemma 4B: ~2.5 GB download. Best translation quality across 40+ languages.",
          "downloadButton": "Download TranslateGemma",
          "useButton": "Use TranslateGemma",
          "genericHeading": "Use active processing model",
          "genericSubtext": "Uses your current local model with a built-in translation prompt. No extra download. Quality varies.",
          "useGenericButton": "Use active model",
          "footnote": "Both options run fully offline. You can change this choice later in settings."
        }
      },
      "delete": {
        "title": "Delete mode",
        "confirm": "Delete \"{{name}}\"? This will also remove its shortcut binding.",
        "ariaLabel": "Delete {{name}}"
      },
      "edit": { "ariaLabel": "Edit {{name}}" },
      "noModel": {
        "heading": "No local model selected",
        "body": "Select or download a local model above to use Smart Modes."
      },
      "defaultModes": {
        "cleanUp": "Clean Up",
        "makeFormal": "Make Formal",
        "makeCasual": "Make Casual",
        "writeAsEmail": "Write as Email",
        "bulletPoints": "Bullet Points",
        "summarize": "Summarize",
        "translateToEnglish": "Translate to English",
        "translateToSpanish": "Translate to Spanish",
        "translateToFrench": "Translate to French",
        "translateToChinese": "Translate to Chinese"
      }
    }
    ```

    Also add the TranslateGemma catalogue model description under the SAME path the other catalogue models use. First read en/translation.json to find the existing `...library.models.gemma3_4b.description` key, then add a sibling:
    ```json
    "translateGemma4b": { "description": "Dedicated translation model — 55 benchmarked languages" }
    ```
    Use the exact parent path observed (do not assume — match `qwen25_1b5` / `gemma3_4b` placement). The LlmLibrarySection MODEL_ID_TO_I18N_KEY maps `"translate-gemma-4b": "translateGemma4b"` (added in plan 13-01).

    Keep valid JSON (trailing commas removed, double quotes).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/translation.json','utf8')); console.log('valid')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const j=require('./src/i18n/locales/en/translation.json'); process.exit(j.smartModes && j.smartModes.card.saveCta==='Save mode' ? 0:1)"` exits 0
    - `grep -q "translateToChinese" src/i18n/locales/en/translation.json` exits 0
    - `grep -q "translateGemma4b" src/i18n/locales/en/translation.json` exits 0
    - en/translation.json parses as valid JSON
  </acceptance_criteria>
  <done>All ~30 smartModes.* keys plus the translateGemma4b catalogue description exist in en with the UI-SPEC English copy; file is valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate identical keys to all 19 non-English locales</name>
  <files>src/i18n/locales/ar/translation.json, src/i18n/locales/bg/translation.json, src/i18n/locales/cs/translation.json, src/i18n/locales/de/translation.json, src/i18n/locales/es/translation.json, src/i18n/locales/fr/translation.json, src/i18n/locales/he/translation.json, src/i18n/locales/it/translation.json, src/i18n/locales/ja/translation.json, src/i18n/locales/ko/translation.json, src/i18n/locales/pl/translation.json, src/i18n/locales/pt/translation.json, src/i18n/locales/ru/translation.json, src/i18n/locales/sv/translation.json, src/i18n/locales/tr/translation.json, src/i18n/locales/uk/translation.json, src/i18n/locales/vi/translation.json, src/i18n/locales/zh/translation.json, src/i18n/locales/zh-TW/translation.json</files>
  <read_first>
    - src/i18n/locales/en/translation.json (the now-updated source — the exact key structure to mirror)
    - scripts/check-translations.ts (confirm it compares full key paths and flags both missing and extra keys)
    - src/i18n/locales/fr/translation.json (one example target to confirm formatting/indentation conventions)
  </read_first>
  <action>
    For each of the 19 non-English locale files, add the SAME `smartModes` namespace and the SAME `...library.models.translateGemma4b.description` key as in en, using the ENGLISH values as placeholders (no hand-translation — consistent with the Phase 11 fallback approach noted in STATE.md line 78). The key STRUCTURE must be byte-for-byte identical to en (same nesting, same key names); only do not introduce extra or missing keys.

    The reliable way to do this is a script that reads en, extracts the new subtrees, and merges them into each locale preserving existing keys and the locale's existing formatting. Write a one-off node script (place it in scripts/ or run inline) that:
      1. Loads en/translation.json.
      2. For each non-English locale, deep-merges `en.smartModes` into the locale (locale wins only if a key already exists — but these are new, so en values fill in) and ensures the `translateGemma4b` description sits at the same path.
      3. Writes the file with 2-space indentation + trailing newline (match existing files — verify fr first).
    Do NOT leave the one-off script in the repo if it lives outside scripts/ — clean it up after (Workspace Hygiene). If placed in scripts/, it may stay only if it is generic/reusable; otherwise delete it.

    After merging, run `bun run check:translations` and fix any locale that reports missing OR extra keys until all 19 report complete.
  </action>
  <verify>
    <automated>bun run check:translations 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bun run check:translations` exits 0
    - `grep -q "smartModes" src/i18n/locales/ar/translation.json` exits 0
    - `grep -q "smartModes" src/i18n/locales/zh-TW/translation.json` exits 0
    - `grep -q "translateToChinese" src/i18n/locales/fr/translation.json` exits 0
    - All 19 non-English locale files parse as valid JSON
  </acceptance_criteria>
  <done>All 19 non-English locales carry the identical smartModes.* + translateGemma4b key structure with English-fallback values; check:translations is green.</done>
</task>

</tasks>

<verification>
- `bun run check:translations` exits 0 (L10N-01 gate)
- `bun run lint` exits 0 (no JSON or import regressions)
- en and all 19 locales parse as valid JSON
</verification>

<success_criteria>
- All Phase 13 user-facing strings are i18n keys present in all 20 locales (L10N-01)
- `bun run check:translations` passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-02-SUMMARY.md`
</output>
