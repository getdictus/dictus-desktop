---
phase: 13
slug: smart-modes-ui-translation-presets-i18n
status: approved
shadcn_initialized: false
preset: none
created: 2026-06-03
reviewed_at: 2026-06-03
---

# Phase 13 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom Tailwind + CSS variables) |
| Preset | not applicable |
| Component library | none (bespoke components in `src/components/ui/`) |
| Icon library | lucide-react (already used throughout) |
| Font | System default (CSS `font-synthesis: none`, `text-rendering: optimizeLegibility`) |

**Source:** Detected from `tailwind.config.js`, `src/App.css`, and component scan. No `components.json` found. This is not a shadcn project. Registry safety gate: not applicable.

---

## Spacing Scale

Declared values (multiples of 4 only). Project uses Tailwind spacing utilities; values below map to the Tailwind scale already in use.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px (`gap-1`, `p-1`) | Icon gaps, badge padding, chip inner spacing |
| sm | 8px (`gap-2`, `p-2`) | Inline element spacing, card internal row gaps |
| md | 16px (`p-4`, `gap-4`) | Card body padding, section gaps within a group |
| lg | 24px (`space-y-6`) | Between top-level setting groups on the page |
| xl | 32px | Not explicitly needed in this phase |
| 2xl | 48px | Not explicitly needed in this phase |
| 3xl | 64px | Not explicitly needed in this phase |

Exceptions:
- Shortcut chip: `px-2 py-1` (8px/4px) — matches existing `GlobalShortcutInput` chip style verbatim.
- Card border-radius: `rounded-xl` — matches existing `ModelCard` and `LlmLibrarySection` provider cards.
- Section header label row: `px-4` lateral padding — matches `SettingsGroup` header style (`text-xs font-medium text-mid-gray uppercase tracking-wide`).

---

## Typography

Sourced from `src/App.css` (`:root` declarations) and observed component patterns.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 15px (`:root font-size`) | 400 (`font-normal`) | 1.6 (`line-height: 24px` at 15px base) |
| Label / small / card title / section heading / page heading | 12–20px (see size column per role in component notes) | 500 (`font-medium`) | 1.2–1.5 |

**Weight declaration (exactly 2):**
- 400 — body text, prompt preview, secondary descriptive text
- 500 — all emphasized roles: labels (`text-xs font-medium`), card titles (`text-base font-medium`), section headings (`text-xs font-medium uppercase tracking-wide`), page headings (`text-xl font-medium`)

**Size reference (no new sizes introduced):**
- 12px (`text-xs`) — labels, section headings, small metadata
- 15px (`text-base`) — card titles, body text
- 20px (`text-xl`) — page heading

**Note:** This phase introduces no new type sizes. All type roles map to existing Tailwind classes already used in `PostProcessingSettings.tsx`, `LlmLibrarySection.tsx`, and `ModelCard.tsx`. Card titles previously used `font-semibold` (weight 600) in the prior draft; the contract collapses that to `font-medium` (weight 500) to stay within the 2-weight maximum.

---

## Color

Sourced from `src/App.css` `@theme` block. Light/dark adaptive via CSS variables.

| Role | Value (light) | Value (dark) | Usage |
|------|--------------|-------------|-------|
| Dominant (60%) | `#fbfbfb` | `#0a1628` | Page background, card background (`bg-background`) |
| Secondary (30%) | `#808080` at 10–20% alpha | `#808080` at 10–20% alpha | Card borders (`border-mid-gray/20`), section dividers, disabled states, secondary text |
| Accent (10%) | `#3d7eff` (`--color-accent`) / `#6ba3ff` (`--color-logo-primary`) | same | See reserved list below |
| Destructive | `red-600` / `red-500` | same | Delete button, destructive alert text |

**Accent reserved for:**
- Active/selected card border and background tint (`border-logo-primary/50 bg-logo-primary/10`) — matches ModelCard "active" state verbatim
- Active shortcut chip recording state (`border-logo-primary bg-logo-primary/30`)
- Shortcut chip hover state (`hover:border-logo-primary`)
- "Actif" badge fill (`bg-logo-primary`)
- Kind badge for **Rewrite** cards: `bg-logo-primary/10 text-logo-primary border border-logo-primary/30` (accent-tinted, matches existing badge pattern)
- Enable-translation CTA button (primary action button uses `bg-background-ui` which resolves to the accent in dark mode — follow existing `Button variant="primary"` exactly)
- Section header "recommended" badge border color (`border-logo-primary text-logo-primary`)

**Translation section color distinction (Claude's Discretion — resolved here):**
- **Rewrite cards:** Standard neutral border `border-mid-gray/20`, no tint. When active: accent tint as above.
- **Translation cards (enabled):** Same neutral default border; when active: accent tint as above.
- **Translation cards (disabled / pre-enable):** `opacity-60` applied to card content + `bg-mid-gray/5` card background. The section header label uses `text-amber-500` accent color (amber, distinct from the blue accent) to signal "action needed." The "Enable translation" CTA button uses `Button variant="primary-soft"` (blue-tinted, low-key).
- **Section header labels:** "Rewrite" in standard `text-mid-gray`; "Translation" in `text-mid-gray` when enabled, `text-amber-500` when not yet enabled — the only color difference needed to distinguish sections at a glance without redesigning cards.

---

## Component Inventory

All components are **reused from existing codebase**. No new primitives are introduced.

### Reused Without Modification

| Component | File | Used for |
|-----------|------|---------|
| `ModelCard` | `src/components/onboarding/ModelCard.tsx` | Smart Mode card visual base (border, padding, badge placement) |
| `Badge` | `src/components/ui/Badge.tsx` | Kind badge (Rewrite/Translation), shortcut "unbound" hint |
| `Button` | `src/components/ui/Button.tsx` | "+ New Rewrite", "+ New Translation", Save mode, Discard changes, Enable translation CTA |
| `Alert` | `src/components/ui/Alert.tsx` | Conflict warning (variant="error", contained), engine-choice info (variant="info") |
| `Input` | `src/components/ui/Input.tsx` | Mode name field (inline edit) |
| `Textarea` | `src/components/ui/Textarea.tsx` | Prompt field (Rewrite inline edit) |
| `SettingsGroup` | `src/components/ui/SettingsGroup.tsx` | "Rewrite" and "Translation" section wrappers |
| `SettingContainer` | `src/components/ui/SettingContainer.tsx` | Not directly used in card list; used for page-level settings |

### Reused With Adaptation

| Component | File | Adaptation |
|-----------|------|-----------|
| `GlobalShortcutInput` | `src/components/settings/GlobalShortcutInput.tsx` | Route bind through `setSmartModeBinding` instead of `updateBinding`; render `BindingResponse.error` as inline red text below the chip instead of a toast; expose as a prop-driven variant accepting `modeId` |
| `LlmLibrarySection` | `src/components/settings/post-processing/LlmLibrarySection.tsx` | No modification — TranslateGemma added to `llm.rs catalogue()` backend; frontend picks it up automatically via existing store |

### New Components (this phase)

| Component | Location | Responsibility |
|-----------|----------|---------------|
| `SmartModesSection` | `src/components/settings/post-processing/SmartModesSection.tsx` | Top-level section rendering Rewrite + Translation labeled sub-groups, create buttons, card list |
| `SmartModeCard` | `src/components/settings/post-processing/SmartModeCard.tsx` | Single card: collapsed view (name, preview, shortcut chip, kind badge, edit/delete icons) + inline expanded form |
| `TranslationEngineChoiceModal` | `src/components/settings/post-processing/TranslationEngineChoiceModal.tsx` | First-activation modal: explains choice, presents "Download TranslateGemma" vs "Use active model" options; persists global engine choice to settings |
| `SmartModeShortcutChip` | `src/components/settings/post-processing/SmartModeShortcutChip.tsx` | Inline shortcut chip on card: display-only when not editing, capture-mode when clicked; renders conflict error below the chip |

---

## Interaction Contracts

### Smart Mode Card — Collapsed State

**Always visible on each card:**
- Mode name (`text-base font-medium text-text`)
- Preview text: for Rewrite cards, first 80 characters of prompt truncated with ellipsis (`text-sm text-text/60`); for Translation cards, target language name (e.g. "Spanish") instead of prompt preview
- Kind badge: `"Rewrite"` in `bg-mid-gray/10 text-mid-gray` pill; `"Translation"` in `bg-logo-primary/10 text-logo-primary border-logo-primary/30` pill
- Shortcut chip (always visible, clickable to bind without expanding card):
  - **Bound:** displays key combo in `px-2 py-1 text-sm font-medium bg-mid-gray/10 border border-mid-gray/80 rounded-md`
  - **Unbound:** `px-2 py-1 text-xs font-medium bg-mid-gray/5 border border-mid-gray/30 text-mid-gray/60 rounded-md cursor-pointer` — text: i18n key `smartModes.card.addShortcut` ("Add shortcut")
  - **Recording:** `px-2 py-1 text-sm font-medium border border-logo-primary bg-logo-primary/30 rounded-md` — text: live key combo or "Press keys..."
  - **Conflict:** chip reverts to previous state; red inline text `text-xs text-red-400 mt-1` appears directly below the chip: "Already used by: {conflicting mode name}"
- Edit icon button (`Pencil` lucide, `text-mid-gray/50 hover:text-text`, `w-4 h-4`) — top-right of card, expands card inline
- Delete icon button (`Trash2` lucide, `text-mid-gray/50 hover:text-red-400`, `w-4 h-4`) — top-right of card, next to edit

**Card layout (collapsed):**
```
[Kind badge]  [Mode name]  [Prompt preview / Language]          [Edit] [Delete]
[Shortcut chip]
[Conflict error text if present]
```
Card: `flex flex-col gap-2 px-4 py-3 rounded-xl border-2 border-mid-gray/20 transition-all`
Hover (non-disabled): `hover:border-logo-primary/50 hover:bg-logo-primary/5`

### Smart Mode Card — Inline Expanded State

Triggered by clicking the Edit icon. Card expands in place with a vertical form below the collapsed header row.

**Rewrite card expanded form:**
- Name field: `Input` variant="compact", label `text-sm font-medium` ("Name")
- Prompt field: `Textarea`, label `text-sm font-medium` ("Prompt")
- Action row (editing existing mode): `Button variant="primary" size="md"` ("Save mode"), `Button variant="secondary" size="md"` ("Discard changes")
- Action row (creating new mode): `Button variant="primary" size="md"` ("Create mode"), `Button variant="secondary" size="md"` ("Discard")
- Shortcut chip remains visible and functional in the header row during editing

**Translation card expanded form:**
- Name field: `Input` variant="compact", label ("Name")
- Target language selector: `Select` (or `Dropdown`) showing curated language list; label ("Target language")
  - If TranslateGemma engine is active: restrict to its officially supported languages (list defined in implementation; ~40 languages)
  - If generic model path is active: show same curated list with `text-xs text-mid-gray/60` footnote "Best-effort — quality varies by model" (i18n key `smartModes.translation.genericEngineNote`)
- No prompt field (translation cards carry only a target language; this matches Phase 12 data model)
- Action row (editing existing mode): `Button variant="primary" size="md"` ("Save mode"), `Button variant="secondary" size="md"` ("Discard changes")
- Action row (creating new mode): `Button variant="primary" size="md"` ("Create mode"), `Button variant="secondary" size="md"` ("Discard")

**Expanded card border:** `border-logo-primary/40 bg-logo-primary/5` (active-tint on the card being edited)

**Validation rules:**
- Primary CTA disabled when name is empty (after trim)
- Primary CTA disabled for Rewrite when prompt is empty (after trim)
- Primary CTA disabled for Translation when no target language is selected
- No change detection required — Save always writes (matches existing prompt save pattern)

### Create New Mode

Two dedicated buttons, one per section (positioned at the bottom of each section):
- `Button variant="primary-soft" size="sm"` with `Plus` lucide icon: "+ New Rewrite" / "+ New Translation"
- Clicking inserts a new **expanded card at the bottom of that section** (above the create button)
- The new card is empty (blank name, blank prompt / no language selected)
- Kind is determined by which button was clicked — not user-selectable in the form
- "Discard" removes the unsaved card without any persistence call

### Delete Mode

- Click Trash2 icon on any card (including seeded defaults — all deletable)
- Native OS dialog via `ask()` (Tauri plugin-dialog): title `t('smartModes.delete.title')`, message `t('smartModes.delete.confirm', { name })`, kind `"warning"`
- If confirmed: call `deleteSmartMode(id)` → shortcut auto-unregistered by backend (no orphan binding)
- If cancelled: no action

### Translation Section — Disabled State (pre-enable)

When no global translation engine is configured yet, the Translation section renders:
- Section header label in `text-amber-500` (vs. "Rewrite" section label in `text-mid-gray`)
- 4 seeded translation cards rendered at `opacity-60`, non-interactive (no shortcut chip click, no edit/delete)
- Below the disabled cards, an inline CTA box:
  - Background: `bg-amber-500/5 border border-amber-500/20 rounded-xl p-4`
  - Heading: `text-sm font-medium` — i18n key `smartModes.translation.enableHeading` ("Enable offline translation")
  - Body: `text-sm text-text/60` — i18n key `smartModes.translation.enableBody` ("Translation runs fully offline through the embedded LLM. Choose an engine to get started.")
  - CTA button: `Button variant="primary-soft" size="md"` — "Choose translation engine" → opens `TranslationEngineChoiceModal`

### Translation Engine Choice Modal

Triggered by the "Choose translation engine" CTA. Uses native dialog flow or an in-page modal panel (implementation discretion — must not leave PostProcessing settings page).

**Two options presented:**

Option A — Download TranslateGemma:
- Heading: `text-base font-medium` — "Download dedicated translation model (Recommended)"
- Sub-text: `text-sm text-text/60` — "TranslateGemma 4B: ~2.5 GB download. Best translation quality across 40+ languages."
- If not yet downloaded: `Button variant="primary" size="md"` — "Download TranslateGemma"
- If already downloaded: `Button variant="primary" size="md"` — "Use TranslateGemma" (no download)
- Download shows inline progress using existing download progress bar pattern from `ModelCard`

Option B — Use active model:
- Heading: `text-base font-medium` — "Use active processing model"
- Sub-text: `text-sm text-text/60` — "Uses your current local model with a built-in translation prompt. No extra download. Quality varies." (i18n key `smartModes.translation.genericEngineDescription`)
- `Button variant="secondary" size="md"` — "Use active model"

**Footnote:** `text-xs text-mid-gray/70` — "Both options run fully offline. You can change this choice later in settings."

**On choice confirmed:** modal closes, Translation section activates (cards become interactive, shortcut chips become bindable), engine choice stored in settings.

---

## Copywriting Contract

All strings are i18n keys. English source values are listed here as the ground truth for `src/i18n/locales/en/translation.json`. Propagated to all 20 locales.

| Element | i18n Key | English Copy |
|---------|----------|-------------|
| Rewrite section label | `smartModes.sections.rewrite` | "Rewrite" |
| Translation section label | `smartModes.sections.translation` | "Translation" |
| Create Rewrite button | `smartModes.createRewrite` | "+ New Rewrite" |
| Create Translation button | `smartModes.createTranslation` | "+ New Translation" |
| Save mode button (editing existing) | `smartModes.card.saveCta` | "Save mode" |
| Discard changes button (editing existing) | `smartModes.card.discardCta` | "Discard changes" |
| Create mode button (creating new) | `smartModes.card.createCta` | "Create mode" |
| Discard button (creating new) | `smartModes.card.discardNewCta` | "Discard" |
| Add shortcut (unbound chip) | `smartModes.card.addShortcut` | "Add shortcut" |
| Shortcut recording prompt | `settings.general.shortcut.pressKeys` | "Press keys..." (reused) |
| Conflict error text | `smartModes.card.shortcutConflict` | "Already used by: {{name}}" |
| Kind badge — Rewrite | `smartModes.kind.rewrite` | "Rewrite" |
| Kind badge — Translation | `smartModes.kind.translation` | "Translation" |
| Target language label | `smartModes.card.targetLanguage` | "Target language" |
| Generic engine note | `smartModes.translation.genericEngineNote` | "Best-effort — quality varies by model" |
| Enable translation heading | `smartModes.translation.enableHeading` | "Enable offline translation" |
| Enable translation body | `smartModes.translation.enableBody` | "Translation runs fully offline through the embedded LLM. Choose an engine to get started." |
| Enable CTA button | `smartModes.translation.chooseCta` | "Choose translation engine" |
| Modal — dedicated model heading | `smartModes.translation.modal.dedicatedHeading` | "Download dedicated translation model (Recommended)" |
| Modal — dedicated model subtext | `smartModes.translation.modal.dedicatedSubtext` | "TranslateGemma 4B: ~2.5 GB download. Best translation quality across 40+ languages." |
| Modal — download button | `smartModes.translation.modal.downloadButton` | "Download TranslateGemma" |
| Modal — use downloaded button | `smartModes.translation.modal.useButton` | "Use TranslateGemma" |
| Modal — generic model heading | `smartModes.translation.modal.genericHeading` | "Use active processing model" |
| Modal — generic model subtext | `smartModes.translation.modal.genericSubtext` | "Uses your current local model with a built-in translation prompt. No extra download. Quality varies." |
| Modal — use generic button | `smartModes.translation.modal.useGenericButton` | "Use active model" |
| Modal — footnote | `smartModes.translation.modal.footnote` | "Both options run fully offline. You can change this choice later in settings." |
| Delete dialog title | `smartModes.delete.title` | "Delete mode" |
| Delete dialog confirm | `smartModes.delete.confirm` | "Delete \"{{name}}\"? This will also remove its shortcut binding." |
| No model empty state heading | `smartModes.noModel.heading` | "No local model selected" |
| No model empty state body | `smartModes.noModel.body` | "Select or download a local model above to use Smart Modes." |
| **Seeded mode names (i18n-displayed; stored key = English constant):** | | |
| Clean Up | `smartModes.defaultModes.cleanUp` | "Clean Up" |
| Make Formal | `smartModes.defaultModes.makeFormal` | "Make Formal" |
| Make Casual | `smartModes.defaultModes.makeCasual` | "Make Casual" |
| Write as Email | `smartModes.defaultModes.writeAsEmail` | "Write as Email" |
| Bullet Points | `smartModes.defaultModes.bulletPoints` | "Bullet Points" |
| Summarize | `smartModes.defaultModes.summarize` | "Summarize" |
| Translate to English | `smartModes.defaultModes.translateToEnglish` | "Translate to English" |
| Translate to Spanish | `smartModes.defaultModes.translateToSpanish` | "Translate to Spanish" |
| Translate to French | `smartModes.defaultModes.translateToFrench` | "Translate to French" |
| Translate to Chinese | `smartModes.defaultModes.translateToChinese` | "Translate to Chinese" |

**Translation preset names** follow the pattern "Translate to {language}" in English; locales use the localized language name (e.g. FR: "Traduire en espagnol").

**CTA label rationale:** "Save mode" / "Create mode" are the primary CTAs (specific verb + object noun). "Discard changes" / "Discard" are the secondary CTAs. Generic labels "Save" and "Cancel" are not used anywhere in this phase.

---

## States Inventory

Every surface must handle all applicable states before the phase is complete.

| Surface | States Required |
|---------|----------------|
| Smart Mode card (collapsed) | default, hover, active-shortcut-recording, shortcut-conflict, disabled (translation pre-enable) |
| Smart Mode card (expanded) | editing-rewrite (name+prompt), editing-translation (name+language), creating-new, saving (button loading) |
| Translation section | pre-enable (disabled cards + CTA), post-enable (cards interactive) |
| Translation engine choice modal | initial, downloading-TranslateGemma (progress), download-complete, choice-confirmed |
| Shortcut chip | unbound, bound-display, recording, conflict |
| Section create button | default, disabled (when a new card is already open in that section) |
| Delete confirmation | native OS dialog — no custom state needed |

---

## Layout Anchoring

`SmartModesSection` mounts inside `PostProcessingSettings.tsx` below the existing "API" `SettingsGroup` block, replacing the existing "Prompts" `SettingsGroup`. The page structure becomes:

```
PostProcessingSettings (max-w-3xl w-full mx-auto space-y-6)
  ├── Header (title + status badge)
  ├── SettingsGroup: "Shortcuts" — model-selection shortcut (transcribe_with_post_process REMOVED; replaced by per-mode bindings on cards)
  ├── SettingsGroup: "Engine" — ProviderPicker + LlmLibrarySection (unchanged from Phase 11/12)
  └── SmartModesSection
        ├── SettingsGroup: "Rewrite" header
        │     ├── SmartModeCard (Clean Up)
        │     ├── SmartModeCard (Make Formal)
        │     ├── ... (remaining Rewrite modes)
        │     └── Button: "+ New Rewrite"
        └── SettingsGroup: "Translation" header (amber label if pre-enable)
              ├── SmartModeCard (Translate to English) [disabled if pre-enable]
              ├── SmartModeCard (Translate to Spanish) [disabled if pre-enable]
              ├── SmartModeCard (Translate to French) [disabled if pre-enable]
              ├── SmartModeCard (Translate to Chinese) [disabled if pre-enable]
              ├── [Enable CTA box — visible only if pre-enable]
              └── Button: "+ New Translation" [disabled if pre-enable]
```

**Primary visual anchor:** The "Rewrite" section label and first card row (`SmartModeCard — Clean Up`) are the focal point of the screen. User attention flows top-to-bottom down the card list; the "Translation" section header (amber when pre-enable, neutral when active) is the secondary anchor that signals whether translation requires action.

**Note for planner:** The old `ShortcutInput shortcutId="transcribe_with_post_process"` row in PostProcessingSettings is removed in this phase (the binding is now vestigial per Phase 12 migration). The raw `transcribe` binding remains in the General settings panel (not touched here).

---

## RTL Handling

Locales `ar` and `he` are RTL. The existing app does not have explicit RTL layout rules; standard Tailwind `flex` row direction is LTR-first. For this phase:
- Card layout uses `flex`, `justify-between`, and `gap` — these reverse naturally with `dir="rtl"` on the root. No special RTL classes needed.
- Icon placement (Edit/Delete at end of row) reverses correctly.
- Shortcut chip (a key combo) is always LTR regardless of document direction — wrap with `dir="ltr"` attribute on the chip element to prevent right-to-left rendering of key combo strings.

---

## Accessibility

| Requirement | Implementation |
|-------------|---------------|
| Card keyboard navigation | `role="button"` + `tabIndex={0}` + `onKeyDown Enter/Space` — matches existing `ModelCard` pattern |
| Shortcut capture field | Announces recording state with `aria-label` that updates (e.g. "Recording shortcut, press keys...") |
| Conflict error | `role="alert"` on the conflict text element so screen readers announce it immediately on conflict |
| Delete icon button | `aria-label={t('smartModes.delete.ariaLabel', { name })}` — "Delete {mode name}" |
| Edit icon button | `aria-label={t('smartModes.edit.ariaLabel', { name })}` — "Edit {mode name}" |
| Disabled translation cards | `aria-disabled="true"` + `opacity-60`, not `disabled` attribute (divs have no disabled) |
| Section labels | Use `<h2>` or `<p>` matching `SettingsGroup` existing heading pattern |

---

## Hardcoded Translation Prompt (Generic Model Path)

When the user chooses "Use active model" for translation, the backend uses this hardcoded, non-user-editable prompt injected around the transcribed text. This is determined here as Claude's Discretion:

```
Translate the following text to {target_language}. Output only the translation, no explanation or commentary.
```

Where `{target_language}` is the full English name of the target language (e.g. "Spanish", "French"). This prompt is stored in Rust source, not in settings, and not exposed in the UI. The i18n key `smartModes.translation.genericEngineNote` informs the user that prompt quality is best-effort.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — project does not use shadcn |
| third-party | none | not applicable |

No third-party registries are used. All components are from the existing codebase or built from scratch in this phase.

---

## Pre-Population Sources

| Decision | Source |
|----------|--------|
| Card visual language (border, radius, padding, badge) | Detected from `LlmLibrarySection.tsx`, `ModelCard.tsx` — verbatim reuse mandated by 13-CONTEXT.md |
| Inline editing (no modal) | 13-CONTEXT.md §"Create / edit / delete flow" |
| No "active mode" concept | 13-CONTEXT.md §"Card list layout & grouping", §"Specifics" |
| Conflict = inline red text, bind blocked | 13-CONTEXT.md §"Shortcut binding & conflict" |
| Translation disabled by default | 13-CONTEXT.md §"Translation presets & engine" |
| Global engine choice (TranslateGemma vs generic) | 13-CONTEXT.md §"Translation presets & engine" |
| EN/ES/FR/ZH preset set | 13-CONTEXT.md + REQUIREMENTS.md TRANS-01 |
| Seeded modes i18n at display time only | 13-CONTEXT.md §"Localization" |
| 20 locales + ar/he RTL | 13-CONTEXT.md canonical refs §"i18n" |
| Tailwind + CSS variables, no shadcn | Detected from `tailwind.config.js`, `src/App.css`, absence of `components.json` |
| Color tokens | Detected from `src/App.css` `@theme` block |
| Typography scale | Detected from `src/App.css` `:root` + component class patterns |
| Button/Alert/Badge variants | Detected from `src/components/ui/Button.tsx`, `Alert.tsx`, `Badge.tsx` |
| Translation section amber accent | Claude's Discretion (resolved in this spec) |
| Hardcoded translation prompt | Claude's Discretion (resolved in this spec) |
| CTA labels "Save mode" / "Create mode" / "Discard changes" / "Discard" | Revision fix — checker Dimension 1 BLOCK (generic "Save"/"Cancel" rejected) |
| Typography collapsed to 2 weights (400 + 500) | Revision fix — checker Dimension 4 BLOCK (3 weights declared, max is 2) |
| Primary visual anchor declared | Revision recommendation — checker Dimension 2 FLAG |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
