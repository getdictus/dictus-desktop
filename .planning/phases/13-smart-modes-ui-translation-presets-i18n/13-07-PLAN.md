---
phase: 13-smart-modes-ui-translation-presets-i18n
plan: "07"
type: execute
wave: 2
depends_on: ["13-05"]
files_modified:
  - src/components/settings/post-processing/SmartModeCard.tsx
  - src/components/settings/post-processing/SmartModesSection.tsx
  - src/components/settings/post-processing/TranslationEngineChoiceModal.tsx
  - src/components/settings/post-processing/SmartModeTemplatePicker.tsx
  - src/i18n/locales/en/translation.json
  - src/i18n/locales/ar/translation.json
  - src/i18n/locales/bg/translation.json
  - src/i18n/locales/cs/translation.json
  - src/i18n/locales/de/translation.json
  - src/i18n/locales/es/translation.json
  - src/i18n/locales/fr/translation.json
  - src/i18n/locales/he/translation.json
  - src/i18n/locales/it/translation.json
  - src/i18n/locales/ja/translation.json
  - src/i18n/locales/ko/translation.json
  - src/i18n/locales/pl/translation.json
  - src/i18n/locales/pt/translation.json
  - src/i18n/locales/ru/translation.json
  - src/i18n/locales/sv/translation.json
  - src/i18n/locales/tr/translation.json
  - src/i18n/locales/uk/translation.json
  - src/i18n/locales/vi/translation.json
  - src/i18n/locales/zh/translation.json
  - src/i18n/locales/zh-TW/translation.json
autonomous: true
gap_closure: true
requirements: [MODE-03, MODE-06, TRANS-01, TRANS-02, L10N-01]

must_haves:
  truths:
    - "Editing a seeded mode's title and saving shows the new title on the card (no longer forced to the i18n default)"
    - "The prompt field is labeled 'Prompt' (not 'Prompt Label' / 'Libellé du prompt')"
    - "The '+' create button renders a single plus, not '+ + New Rewrite'"
    - "Clicking '+' opens a picker of predefined templates plus a Custom option; deleted defaults are recreatable"
    - "The translation engine choice can be changed after the first pick (active LLM <-> dedicated Gemma)"
  artifacts:
    - path: "src/components/settings/post-processing/SmartModeTemplatePicker.tsx"
      provides: "predefined-template + custom create picker"
      min_lines: 40
    - path: "src/components/settings/post-processing/SmartModeCard.tsx"
      provides: "edit-aware displayName + Prompt label fix"
      contains: "smartModes.card.promptLabel"
  key_links:
    - from: "SmartModeTemplatePicker.tsx"
      to: "commands.smartModeTemplates"
      via: "fetch template catalogue on open"
      pattern: "smartModeTemplates"
    - from: "SmartModesSection.tsx"
      to: "TranslationEngineChoiceModal"
      via: "always-available change-engine affordance"
      pattern: "setModalOpen"
---

<objective>
Close the remaining card / create-flow / translation-engine gaps from 13-UAT.md (tests 2, 4, 6).

- GAP 1 (MAJOR): editing a seeded mode's title saves but the card still shows the old (localized default) title. Root cause confirmed in SmartModeCard.tsx lines 53-57: `displayName` maps ANY seeded id through `SEEDED_MODE_ID_TO_I18N_KEY`, so an edited seeded mode always shows the translated default. Per CONTEXT.md: "User-created or user-edited modes keep their literal stored name — only the system-seeded set maps to i18n keys." Fix: only localize when the stored name still equals the seed default.
- GAP 2 (COSMETIC): the prompt field is labeled via `settings.postProcessing.prompts.promptLabel` ("Prompt Label" / FR "Libellé du prompt"). Add `smartModes.card.promptLabel` = "Prompt" and `smartModes.card.nameLabel` = "Name" and use them.
- GAP 7 (MAJOR): the translation engine choice is locked after first pick. Add an always-available "change translation engine" affordance that re-opens the modal and updates `translation_engine_choice` anytime, both directions.
- GAP 8b/c (locked decision): the "+" button currently renders a double plus ("+ + New Rewrite") and there is no template picker. Fix the double "+", and make "+" open a picker of the ~10 predefined templates (sourced from the new `smartModeTemplates` command from 13-05) plus a "Custom" option.

The template-picker UI uses CONCRETE, self-contained styling guidance given inline in Task 2 (reuse the existing TranslationEngineChoiceModal overlay style + Button/list classes) — no external design tooling is needed.
Purpose: editing works visibly, the prompt label is correct, defaults are recreatable, and the translation engine is switchable.
Output: edit-aware card naming, a new `SmartModeTemplatePicker`, a change-engine affordance, the double-"+" fix, and ~6 new i18n keys propagated to 20 locales.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-UAT.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-CONTEXT.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-UI-SPEC.md
@.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-05-PLAN.md

<interfaces>
<!-- Shapes the executor consumes. -->

From src/bindings.ts:
```ts
commands.listSmartModes(): Promise<Result<SmartMode[], string>>
commands.addSmartMode(name, kind, prompt, targetLanguage): Promise<Result<SmartMode, string>>
commands.updateSmartMode(id, name, prompt, targetLanguage): Promise<Result<null, string>>
commands.smartModeTemplates(): Promise<Result<SmartMode[], string>>   // NEW in 13-05 — the 10 predefined templates
commands.setTranslationEngineChoice(choice: TranslationEngineChoice): Promise<Result<null, string>>
type SmartMode = { id; name; kind: "rewrite"|"translation"; prompt; target_language?: { code; label } | null }
type TranslationEngineChoice = "not_chosen" | "translate_gemma" | "generic_model"
```

Seeded id -> i18n key map already in SmartModeCard.tsx (keep it):
```ts
const SEEDED_MODE_ID_TO_I18N_KEY: Record<string,string> = { mode_clean_up: "smartModes.defaultModes.cleanUp", ... }
```

Seed default English names (from settings.rs default templates — used for the edit-detection compare):
mode_clean_up="Clean Up", mode_make_formal="Make Formal", mode_make_casual="Make Casual",
mode_email="Write as Email", mode_bullet_points="Bullet Points", mode_summarize="Summarize",
mode_translate_en="Translate → English", mode_translate_es="Translate → Spanish",
mode_translate_fr="Translate → French", mode_translate_zh="Translate → Chinese"
(note the → is U+2192).

From src/components/settings/post-processing/TranslationEngineChoiceModal.tsx (overlay style to REUSE for the picker — see Task 2):
```tsx
// Modal pattern: a fixed full-screen overlay (e.g. "fixed inset-0 z-50 flex items-center justify-center bg-black/50")
// wrapping a centered card panel; gated purely on an `open` prop (early-returns null when !open).
// Reuse this same overlay + panel structure for SmartModeTemplatePicker.
```

From src/components/ui/Button.tsx:
```tsx
// variants: "primary" | "primary-soft" | "secondary"; sizes: "sm" | default. Reuse for picker rows + Custom entry.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix edit-aware title + Prompt/Name labels in SmartModeCard</name>
  <read_first>
    - src/components/settings/post-processing/SmartModeCard.tsx (entire file — displayName at 53-57; prompt label at 196; name label at 180)
    - src-tauri/src/settings.rs lines 668-755 (the exact stored default English names per seeded id — used for the edit-detection compare)
    - .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-CONTEXT.md §Localization ("only the system-seeded set maps to i18n keys; user-edited modes keep literal stored name")
    - src/i18n/locales/en/translation.json lines 723-775 (smartModes.card block to extend)
  </read_first>
  <action>
    1. EDIT-AWARE displayName (GAP 1). Add a constant mapping seeded id -> the stored default English name (the literal seed names listed in <interfaces>, including the U+2192 arrow):
    ```ts
    const SEEDED_MODE_DEFAULT_NAME: Record<string, string> = {
      mode_clean_up: "Clean Up", mode_make_formal: "Make Formal", mode_make_casual: "Make Casual",
      mode_email: "Write as Email", mode_bullet_points: "Bullet Points", mode_summarize: "Summarize",
      mode_translate_en: "Translate → English", mode_translate_es: "Translate → Spanish",
      mode_translate_fr: "Translate → French", mode_translate_zh: "Translate → Chinese",
    };
    ```
    Change `displayName` so it only localizes when the stored name is UNCHANGED from the seed default:
    ```ts
    const displayName = (() => {
      if (!mode) return "";
      const i18nKey = SEEDED_MODE_ID_TO_I18N_KEY[mode.id];
      const seedDefault = SEEDED_MODE_DEFAULT_NAME[mode.id];
      // Localize only a pristine seeded mode; once the user edits the name, show the literal stored name.
      if (i18nKey && seedDefault && mode.name === seedDefault) return t(i18nKey);
      return mode.name;
    })();
    ```
    This means: after the user edits Clean Up → "My Cleaner", `mode.name !== "Clean Up"` so the card shows "My Cleaner". A pristine Clean Up still localizes to "Nettoyer" in FR.

    2. PROMPT + NAME LABELS (GAP 2). Replace the two label keys:
       - line ~180 name label: change `t("settings.postProcessing.prompts.nameLabel", { defaultValue: "Name" })` → `t("smartModes.card.nameLabel")`.
       - line ~196 prompt label: change `t("settings.postProcessing.prompts.promptLabel", { defaultValue: "Prompt" })` → `t("smartModes.card.promptLabel")`.
       Add the keys in Task 4 (en) and propagate. en values: `smartModes.card.nameLabel` = "Name", `smartModes.card.promptLabel` = "Prompt".

    3. PLACE THE SHORTCUT CHIP COMPACTLY (GAP 6 card-side). The chip component (reworked in 13-06) now renders as an inline right-aligned cluster. Move `<SmartModeShortcutChip .../>` OUT of its own full-width row (currently rendered below the top row at line ~329-335) and INTO the top header row, right-aligned next to the edit/delete icons. The collapsed card top row becomes: `[kind badge] [name] [preview]` on the left (min-w-0, truncates) and `[shortcut chip cluster] [edit] [delete]` on the right (`shrink-0`). Use the existing `flex justify-between` row. Do not change the chip's internal markup (owned by 13-06) — only its placement. Keep edit/delete hidden when `disabled`.
  </action>
  <verify>
    <automated>bun run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "SEEDED_MODE_DEFAULT_NAME" src/components/settings/post-processing/SmartModeCard.tsx` exits 0
    - The localize-only-when-pristine check exists: `grep -q "mode.name === seedDefault" src/components/settings/post-processing/SmartModeCard.tsx` exits 0
    - `grep -q "smartModes.card.promptLabel" src/components/settings/post-processing/SmartModeCard.tsx` exits 0 and `grep -q "smartModes.card.nameLabel" src/components/settings/post-processing/SmartModeCard.tsx` exits 0
    - The old legacy label keys are gone: `grep -q "settings.postProcessing.prompts.promptLabel" src/components/settings/post-processing/SmartModeCard.tsx` exits 1
    - The shortcut chip is in the header row, not a standalone full-width row (chip placement moved): the `<SmartModeShortcutChip` JSX appears within the `justify-between` header block
    - `bun run lint` exits 0
  </acceptance_criteria>
  <done>Editing a seeded mode's name shows the new name; pristine seeded modes still localize; the prompt field is labeled "Prompt"; the chip sits compactly on the right of the card header.</done>
</task>

<task type="auto">
  <name>Task 2: Build SmartModeTemplatePicker + wire create buttons (fix double +)</name>
  <read_first>
    - src/components/settings/post-processing/SmartModesSection.tsx (entire file — the two create buttons at lines 143-153 and 214-224 with `<Plus/>` + a label that already starts with "+"; the creating-state flags)
    - src/components/settings/post-processing/TranslationEngineChoiceModal.tsx (the overlay + centered-panel structure to REUSE for the picker — `fixed inset-0` overlay, `open`-gated, centered card)
    - src/components/ui/Button.tsx (variants: primary, primary-soft, secondary; sizes — use for picker rows + Custom entry)
    - src/bindings.ts: confirm `smartModeTemplates` exists (`grep -n smartModeTemplates src/bindings.ts`; if missing, run `bun run tauri dev` once to regenerate then stop it)
    - .planning/phases/13-smart-modes-ui-translation-presets-i18n/13-CONTEXT.md §"Create / edit / delete flow" and 13-UAT.md GAP 8 (b)(c)
    - dictus-postproc-mockup.png (repo root) — visual-direction reference ONLY for compact list/picker layout. NOTE: it depicts the model-engine library, so adapt the compact-list language; do NOT copy its content.
  </read_first>
  <action>
    FIX DOUBLE "+" (GAP 8c): the i18n values `smartModes.createRewrite` = "+ New Rewrite" and `smartModes.createTranslation` = "+ New Translation" already contain a leading "+", and the buttons ALSO render a `<Plus/>` icon → "+ + New Rewrite". Resolve by removing the textual "+" from the i18n values (change to "New Rewrite" / "New Translation" in Task 4) and KEEPING the `<Plus/>` icon. (Do the i18n value change in Task 4; here just confirm the buttons keep the single `<Plus/>` icon and the label text no longer starts with "+".)

    BUILD THE PICKER (GAP 8b): create src/components/settings/post-processing/SmartModeTemplatePicker.tsx. Use CONCRETE, self-contained styling (no external design tooling):
    - VISUAL LAYOUT: reuse the TranslationEngineChoiceModal overlay structure — a `fixed inset-0 z-50 flex items-center justify-center bg-black/50` overlay wrapping a centered panel (`bg-background border border-mid-gray/20 rounded-lg p-4 max-w-md w-full max-h-[80vh] overflow-y-auto`). Inside, a title (`text-lg font-semibold`), then a vertical list (`flex flex-col gap-1`) of template rows. Each template row is a full-width left-aligned `<button>` (`w-full text-left px-3 py-2 rounded-md hover:bg-logo-primary/5 border border-transparent hover:border-logo-primary/30`) showing the template name (bold) and a one-line `text-xs text-mid-gray` preview. A "Custom" entry uses the same row style at the bottom, visually separated by a `border-t border-mid-gray/20 mt-1 pt-1`. This is an in-page popover/list of template names + a Custom entry — not a free-form design.
    - Props: `{ open: boolean; kind: "rewrite" | "translation"; existingModeIds: string[]; onClose: () => void; onCreated: () => void; onCustom: () => void; }`. Early-return `null` when `!open` (mirror the modal).
    - On open: `const r = await commands.smartModeTemplates();` filter `r.data` to the given `kind`. For each template, show name (localized via the existing SEEDED_MODE_ID_TO_I18N_KEY map — import/export it from SmartModeCard, or duplicate the map) and a one-line preview (prompt slice for rewrite, target language label for translation). Templates whose id is in `existingModeIds` get a small `text-xs text-mid-gray` "Added" hint (`t("smartModes.picker.added")`) but stay pickable (picking creates a fresh custom copy — see next bullet — so deleted-then-readded works without id collision).
    - Picking a predefined template calls `commands.addSmartMode(template.name, template.kind, template.prompt, template.target_language ?? null)` (this creates a new `mode_{timestamp}` id; it recreates the behavior/content of a deleted default without reusing the reserved seeded id). After ok → `onCreated(); onClose();`.
    - The "Custom" row calls `onClose()` then `onCustom()` (the section wires `onCustom` to the existing inline blank-card create flow). i.e. Custom = the current empty-card path.
    - Translation picker is only reachable when translation is enabled (the section already gates the translation create button on `translationEnabled`).

    WIRE IN SmartModesSection.tsx:
    - Add `pickerOpen: "rewrite" | "translation" | null` state.
    - The "+ New Rewrite" button now opens the picker: `onClick={() => setPickerOpen("rewrite")}` (remove the leading "+" from its label via i18n; keep `<Plus/>`). Same for translation.
    - Render `<SmartModeTemplatePicker open={pickerOpen === "rewrite"} kind="rewrite" existingModeIds={rewrites.map(m=>m.id)} onClose={()=>setPickerOpen(null)} onCreated={refetchModes} onCustom={() => setCreatingRewrite(true)} />` and the translation equivalent (`onCustom={() => setCreatingTranslation(true)}`).
    - Keep the existing inline blank-card create flow exactly as-is for the Custom path.
  </action>
  <verify>
    <automated>bun run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/settings/post-processing/SmartModeTemplatePicker.tsx` exists and calls `commands.smartModeTemplates` (grep exits 0)
    - The picker reuses the modal overlay style: `grep -q "fixed inset-0" src/components/settings/post-processing/SmartModeTemplatePicker.tsx` exits 0
    - The picker calls `commands.addSmartMode` for predefined templates (grep `addSmartMode` in the new file)
    - SmartModesSection opens the picker from the create buttons: `grep -q "setPickerOpen" src/components/settings/post-processing/SmartModesSection.tsx` exits 0
    - Create button labels no longer carry a textual "+": after Task 4, `grep -q '"createRewrite": "+ ' src/i18n/locales/en/translation.json` exits 1
    - The `<Plus` icon is still rendered in SmartModesSection: `grep -q "<Plus" src/components/settings/post-processing/SmartModesSection.tsx` exits 0
    - `bun run lint` exits 0
  </acceptance_criteria>
  <done>The "+" buttons render a single plus and open a template picker (an `open`-gated overlay list reusing the modal style); predefined templates are recreatable; "Custom" reaches the inline blank-card flow.</done>
</task>

<task type="auto">
  <name>Task 3: Add always-available change-translation-engine affordance</name>
  <read_first>
    - src/components/settings/post-processing/SmartModesSection.tsx (engineChoice / translationEnabled derivation at lines 90-97; the pre-enable CTA box at 180-200; the modal mount at 228-235)
    - src/components/settings/post-processing/TranslationEngineChoiceModal.tsx (entire file — it already persists the choice both ways via setTranslationEngineChoice("translate_gemma"/"generic_model"))
    - 13-UAT.md GAP 7
  </read_first>
  <action>
    The modal already supports choosing either engine; the only gap is that it is reachable ONLY in the pre-enable state. Make it always reachable so the user can switch active-LLM ↔ TranslateGemma (incl. downloading Gemma later) anytime:

    1. In SmartModesSection.tsx, when `translationEnabled` is true, render a small inline control in the Translation section header row (right-aligned next to the "Translation" `<h2>`): a `Button variant="secondary" size="sm"` (or a text link styled with existing classes) labeled `t("smartModes.translation.changeEngine")` that calls `setModalOpen(true)`. When `translationEnabled` is false, the existing CTA box already opens the modal — leave it.
    2. Show the current engine next to the control: small `text-xs text-mid-gray` reading `t("smartModes.translation.currentEngine", { engine: engineLabel })` where `engineLabel` = `t("smartModes.translation.engineGemma")` for `translate_gemma` or `t("smartModes.translation.engineGeneric")` for `generic_model`.
    3. The modal's `onChosen` already calls `refreshSettings?.()` + `refetchModes()`; confirm changing the engine updates `engineChoice` and re-derives `availableLanguages` (TranslateGemma list vs generic list) — it does, via getSetting. No modal internal change needed; it already calls `setTranslationEngineChoice` for both options. Verify the modal does not early-return when an engine is already chosen (it does not — it is purely `open`-gated). If it does, remove any such guard.
    4. New i18n keys (Task 4): `smartModes.translation.changeEngine` = "Change engine", `smartModes.translation.currentEngine` = "Using: {{engine}}", `smartModes.translation.engineGemma` = "TranslateGemma", `smartModes.translation.engineGeneric` = "Active model".
  </action>
  <verify>
    <automated>bun run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - When translation is enabled, a change-engine control opens the modal: `grep -q "smartModes.translation.changeEngine" src/components/settings/post-processing/SmartModesSection.tsx` exits 0
    - `grep -q "setModalOpen(true)" src/components/settings/post-processing/SmartModesSection.tsx` appears in BOTH the pre-enable CTA and the enabled-state control (at least 2 occurrences)
    - The modal has no "already chosen" early-return guard: `grep -q "return null" src/components/settings/post-processing/TranslationEngineChoiceModal.tsx` only matches the `if (!open)` guard
    - `bun run lint` exits 0
  </acceptance_criteria>
  <done>The translation engine can be re-chosen anytime (both directions) from an always-available control once translation is enabled.</done>
</task>

<task type="auto">
  <name>Task 4: Propagate new card/picker/translation i18n keys to 20 locales</name>
  <read_first>
    - src/i18n/locales/en/translation.json lines 723-775 (smartModes block — `card`, `createRewrite`, `createTranslation`, `translation`)
    - scripts/check-translations.ts (gate behavior)
  </read_first>
  <action>
    In src/i18n/locales/en/translation.json, under `smartModes`:
    1. Add to `smartModes.card`: `"nameLabel": "Name"`, `"promptLabel": "Prompt"`.
    2. Change `smartModes.createRewrite` from "+ New Rewrite" to "New Rewrite"; change `smartModes.createTranslation` from "+ New Translation" to "New Translation" (removes the textual "+" that caused the double plus).
    3. Add a `smartModes.picker` sub-object: `"rewriteTitle": "Add a rewrite mode"`, `"translationTitle": "Add a translation mode"`, `"custom": "Custom"`, `"customSubtext": "Create a mode with your own name and prompt"`, `"added": "Added"`.
    4. Add to `smartModes.translation`: `"changeEngine": "Change engine"`, `"currentEngine": "Using: {{engine}}"`, `"engineGemma": "TranslateGemma"`, `"engineGeneric": "Active model"`.
    Then apply the EXACT same key additions/edits to all 19 other locale files (ar, bg, cs, de, es, fr, he, it, ja, ko, pl, pt, ru, sv, tr, uk, vi, zh, zh-TW), using the English values verbatim as fallbacks (Phase 11/13 precedent). For createRewrite/createTranslation in non-English locales, also strip any leading "+ " from the existing translated value (e.g. FR "+ Nouveau ..." → "Nouveau ...") so no locale renders a double plus.
  </action>
  <verify>
    <automated>bun run check:translations 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bun run check:translations` exits 0
    - `grep -q '"promptLabel": "Prompt"' src/i18n/locales/en/translation.json` exits 0
    - No locale's createRewrite/createTranslation value starts with "+": `for f in src/i18n/locales/*/translation.json; do node -e "const j=require('./'+process.argv[1]); const s=j.smartModes; if((s.createRewrite||'').trim().startsWith('+')||(s.createTranslation||'').trim().startsWith('+')) console.log('PLUS: '+process.argv[1])" "$f"; done` prints nothing
    - All 20 locales contain `smartModes.picker.custom`: `for f in src/i18n/locales/*/translation.json; do grep -q '"custom"' "$f" || echo "MISSING: $f"; done` prints nothing
  </acceptance_criteria>
  <done>All new card/picker/translation keys exist in 20 locales, the "+" is removed from create labels in every locale, and check:translations exits 0.</done>
</task>

</tasks>

<verification>
- `bun run lint` exits 0
- `bun run check:translations` exits 0
- Manual (covered by 13-08 checkpoint): edit a seeded title → card updates; prompt label says "Prompt"; "+" opens picker with single plus; deleted default recreatable; translation engine switchable both ways
</verification>

<success_criteria>
- Editing a mode title updates the displayed card title (seeded modes localize only when pristine)
- The prompt field is labeled "Prompt"
- Create buttons render a single "+" and open a predefined-template + Custom picker; defaults are recreatable
- The translation engine choice is changeable after first pick, both directions
- All new strings localized across 20 locales
</success_criteria>

<output>
After completion, create `.planning/phases/13-smart-modes-ui-translation-presets-i18n/13-07-SUMMARY.md`
</output>
</content>
</invoke>
