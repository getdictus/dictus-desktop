---
phase: 11-llm-runtime-foundation
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src-tauri/src/commands/llm.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/actions.rs
autonomous: true
requirements: [LLM-01, LLM-04, MDL-02, MDL-03, MDL-04]
must_haves:
  truths:
    - "Frontend can call Tauri commands to list LLM models, download, cancel, delete, import custom, and set the active LLM model"
    - "LlmManager is registered in Tauri managed state and shut down on window close"
    - "Selecting the 'embedded' provider routes post-processing through LlmManager.run_inference instead of an HTTP request"
    - "Cloud providers remain opt-in; 'embedded' is NOT added to the HTTP provider list"
  artifacts:
    - path: "src-tauri/src/commands/llm.rs"
      provides: "Tauri commands for the LLM library + active-model selection"
      contains: "#[tauri::command]"
    - path: "src-tauri/src/actions.rs"
      provides: "embedded provider branch in post_process_transcription"
      contains: "\"embedded\""
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "LlmManager"
      via: "app_handle.manage()"
      pattern: "LlmManager::new"
    - from: "src-tauri/src/actions.rs"
      to: "LlmManager.run_inference"
      via: "embedded provider branch"
      pattern: "run_inference"
    - from: "src-tauri/src/lib.rs"
      to: "commands::llm"
      via: "collect_commands"
      pattern: "commands::llm::"
---

<objective>
Expose the `LlmManager` to the frontend via Tauri commands, register it in managed state with shutdown wiring, and route the "embedded" post-processing provider through `LlmManager.run_inference()` instead of the HTTP `llm_client`.

Purpose: Bridges the Plan 01 backend to the frontend (Plan 03 consumes the regenerated tauri-specta bindings) and satisfies LLM-04 (embedded as a selectable provider) at the backend layer.
Output: `commands/llm.rs` (new), command registration in `lib.rs`, manager registration + shutdown, and the embedded branch in `actions.rs`.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-llm-runtime-foundation/11-RESEARCH.md
@.planning/phases/11-llm-runtime-foundation/11-CONTEXT.md
@.planning/phases/11-llm-runtime-foundation/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (src-tauri/src/managers/llm.rs) — the commands wrap these: -->
- `LlmManager::new(app_handle: &AppHandle) -> Result<Self>`
- `LlmManager::catalogue() -> Vec<LlmModelInfo>` and instance state via `available_models`
- `async fn download_llm_model(&self, model_id: &str) -> Result<()>`
- `fn delete_llm_model(&self, model_id: &str) -> Result<()>`
- `fn cancel_download(&self, model_id: &str) -> Result<()>`
- `fn import_custom_gguf(&self, source_path: &Path) -> Result<LlmModelInfo, String>`
- `async fn load_model(&self, model_id: &str) -> Result<()>` / `fn unload_model(&self) -> Result<()>` / `fn is_model_loaded(&self) -> bool`
- `async fn run_inference(&self, prompt: String) -> Result<String>`
- `fn shutdown(&self)`
- `pub struct LlmModelInfo { id, name, description, filename, url, sha256, size_mb, is_downloaded, is_downloading, partial_size, is_custom, is_recommended }`

<!-- From src-tauri/src/commands/models.rs — command + specta pattern to mirror: -->
```rust
#[tauri::command]
#[specta::specta]
pub async fn get_available_models(model_manager: State<'_, Arc<ModelManager>>) -> Result<Vec<ModelInfo>, String> { ... }
```
commands/mod.rs lists: `pub mod audio; pub mod history; pub mod models; pub mod transcription;`

<!-- From src-tauri/src/lib.rs: -->
- Line ~209: `let model_manager = Arc::new(ModelManager::new(app_handle).expect(...));`
- Line ~222: `app_handle.manage(model_manager.clone());`
- collect_commands![...] block at line 408; models commands registered at lines 476-486
- collect_events![...] at line 514 — add LLM event payload types here for tauri-specta
- on_window_event closure at line ~660 (where shutdown for transcription happens)

<!-- From src-tauri/src/actions.rs: -->
- `async fn post_process_transcription(settings: &AppSettings, transcription: &str) -> Option<String>` (line 66)
- `const APPLE_INTELLIGENCE_PROVIDER_ID` lives in settings.rs:10 (value `"apple_intelligence"`); Apple Intelligence is special-cased at actions.rs:151
- structured-output branch at actions.rs:144; legacy branch at actions.rs:267 (`send_chat_completion`)
- `build_system_prompt(&prompt)` and `let user_content = transcription.to_string();` already exist in the structured branch
- The `AppHandle` is available in actions.rs (TranscribeAction methods take `app: &AppHandle`); `post_process_transcription` currently takes only `(settings, transcription)` — it must be given access to the AppHandle to reach managed state.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create commands/llm.rs and register the LlmManager + commands + events</name>
  <read_first>
    - src-tauri/src/commands/models.rs (command signatures, State extraction, specta attributes)
    - src-tauri/src/commands/mod.rs (module list)
    - src-tauri/src/lib.rs (manager init ~209, manage ~222, collect_commands ~408-486, collect_events ~514, on_window_event ~660)
    - src-tauri/src/managers/llm.rs (LlmManager public API from Plan 01)
  </read_first>
  <action>
    1. Add `pub mod llm;` to `src-tauri/src/commands/mod.rs`.

    2. Create `src-tauri/src/commands/llm.rs` with these `#[tauri::command] #[specta::specta]` commands (each takes `llm_manager: State<'_, Arc<LlmManager>>`, returns `Result<_, String>`, mapping anyhow errors with `.map_err(|e| e.to_string())`):
       - `get_llm_models() -> Result<Vec<LlmModelInfo>, String>` — returns current `available_models` (catalogue + custom, with download state).
       - `download_llm_model(model_id: String) -> Result<(), String>` — calls `download_llm_model`.
       - `cancel_llm_download(model_id: String) -> Result<(), String>` — calls `cancel_download`.
       - `delete_llm_model(model_id: String) -> Result<(), String>` — calls `delete_llm_model`.
       - `set_active_llm_model(app_handle: AppHandle, model_id: String) -> Result<(), String>` — calls `load_model(&model_id).await`, then persists `active_llm_model_id = Some(model_id)` via `get_settings`/`write_settings`.
       - `get_active_llm_model(app_handle: AppHandle) -> Result<Option<String>, String>` — reads `get_settings(&app_handle).active_llm_model_id`.
       - `import_custom_llm_model(app_handle: AppHandle, path: String) -> Result<LlmModelInfo, String>` — calls `import_custom_gguf(Path::new(&path))` (returns the error string directly to the frontend for the Alert).

    3. In `lib.rs`, after the ModelManager init (~line 209) add:
       `let llm_manager = Arc::new(LlmManager::new(app_handle).expect("Failed to initialize LLM manager"));`
       and after `app_handle.manage(model_manager.clone());` (~222) add `app_handle.manage(llm_manager.clone());`. Add `use managers::llm::LlmManager;` near the other manager imports (~line 32).

    4. In `lib.rs` `collect_commands![...]` (after the `commands::models::*` entries ~486) register all 7 new commands: `commands::llm::get_llm_models, commands::llm::download_llm_model, commands::llm::cancel_llm_download, commands::llm::delete_llm_model, commands::llm::set_active_llm_model, commands::llm::get_active_llm_model, commands::llm::import_custom_llm_model`.

    5. In `lib.rs` `collect_events![...]` (~line 514) add the LLM download progress payload type so tauri-specta emits a typed binding: `managers::llm::LlmDownloadProgress` (add `#[derive(... Type, tauri_specta::Event ...)]` to that struct in llm.rs if `collect_events` requires the `Event` derive — match how `HistoryUpdatePayload` is declared).

    6. In the `on_window_event` closure (~line 660) where the app handles close/shutdown, call `app.state::<Arc<LlmManager>>().shutdown();` alongside the existing transcription shutdown so the idle watcher thread stops cleanly.

    7. Regenerate bindings: the tauri-specta export runs on `cargo build`/`tauri dev`. Run `cd src-tauri && cargo build --lib` to confirm the specta macro accepts the new commands (bindings.ts is regenerated by the build script / on next dev run — Plan 03 consumes it).
  </action>
  <verify>
    <automated>cd src-tauri && cargo build --lib 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/src/commands/mod.rs` contains `pub mod llm;`
    - `commands/llm.rs` contains all 7 command fn names: `get_llm_models`, `download_llm_model`, `cancel_llm_download`, `delete_llm_model`, `set_active_llm_model`, `get_active_llm_model`, `import_custom_llm_model`
    - `commands/llm.rs` contains 7 occurrences of `#[specta::specta]`
    - `lib.rs` contains `LlmManager::new` and `app_handle.manage(llm_manager.clone())`
    - `lib.rs` collect_commands block contains all 7 `commands::llm::` entries (`grep -c "commands::llm::" src-tauri/src/lib.rs` >= 7)
    - `lib.rs` contains `LlmManager>>().shutdown()` (or equivalent shutdown call in on_window_event)
    - `cargo build --lib` exits 0
  </acceptance_criteria>
  <done>All 7 LLM commands compile and are registered; LlmManager is in managed state and shut down on window close; specta accepts the commands.</done>
</task>

<task type="auto">
  <name>Task 2: Route the "embedded" provider through LlmManager.run_inference in actions.rs</name>
  <read_first>
    - src-tauri/src/actions.rs (post_process_transcription line 66; structured branch line 144; Apple Intelligence special-case line 151; build_system_prompt; legacy branch line 267)
    - src-tauri/src/managers/llm.rs (run_inference signature, is_model_loaded, load_model)
    - .planning/phases/11-llm-runtime-foundation/11-RESEARCH.md (Pattern 7 Provider Integration; anti-pattern: do NOT add embedded to default_post_process_providers)
    - .planning/phases/11-llm-runtime-foundation/11-CONTEXT.md (embedded provider behavior: always selectable, runs the active GGUF, no silent default switch)
  </read_first>
  <action>
    1. Give `post_process_transcription` access to managed state. Change its signature to `async fn post_process_transcription(app: &AppHandle, settings: &AppSettings, transcription: &str) -> Option<String>` and update the call site(s) inside the `TranscribeAction` methods to pass the `app` handle (the methods already receive `app: &AppHandle`).

    2. Add an "embedded" branch EARLY in `post_process_transcription` — after the prompt is resolved (`build_system_prompt`) and `user_content` is available, but BEFORE the HTTP `provider.id`-based reasoning/api-key logic. Insert:
       ```rust
       if provider.id == "embedded" {
           use std::sync::Arc;
           let llm_manager = app.state::<Arc<crate::managers::llm::LlmManager>>();
           // Ensure the active model is loaded (load on demand if needed).
           if !llm_manager.is_model_loaded() {
               if let Some(active_id) = &settings.active_llm_model_id {
                   if let Err(e) = llm_manager.load_model(active_id).await {
                       error!("Embedded LLM failed to load model '{}': {}", active_id, e);
                       return None;
                   }
               } else {
                   debug!("Embedded provider selected but no active LLM model is set");
                   return None;
               }
           }
           let system_prompt = build_system_prompt(&prompt);
           let full_prompt = format!("{}\n\n{}", system_prompt, transcription);
           return match llm_manager.run_inference(full_prompt).await {
               Ok(result) => {
                   let result = strip_invisible_chars(&result);
                   if result.trim().is_empty() { None } else { Some(result) }
               }
               Err(e) => { error!("Embedded LLM inference failed: {}", e); None }
           };
       }
       ```
       Note: this branch returns before reaching the `model` / `post_process_models` and `api_key` lookups, so embedded does NOT require an HTTP `base_url`, model string, or api key.

    3. Confirm the existing `model.trim().is_empty()` early-return (actions.rs ~line 82) does NOT block embedded: place the embedded branch BEFORE that check, OR add `&& provider.id != "embedded"` to that guard. Choose placement so embedded never short-circuits on a missing `post_process_models` entry. Prefer moving the embedded branch above the `model` lookup entirely.

    4. Do NOT add "embedded" to `default_post_process_providers()` in settings.rs (RESEARCH anti-pattern — embedded has no base_url). The frontend marks it selectable via `LOCAL_PROVIDER_IDS_SET` (Plan 03).
  </action>
  <verify>
    <automated>cd src-tauri && cargo build --lib 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `actions.rs` contains `provider.id == "embedded"` and `run_inference`
    - `actions.rs` `post_process_transcription` signature includes `app: &AppHandle`
    - `actions.rs` embedded branch returns before the HTTP `api_key`/`send_chat_completion` logic (grep shows `run_inference` appears before line of `send_chat_completion`)
    - `grep -c "\"embedded\"" src-tauri/src/settings.rs` related to default providers is 0 (embedded NOT in default_post_process_providers)
    - `cargo build --lib` exits 0
  </acceptance_criteria>
  <done>The embedded provider loads the active GGUF on demand and routes post-processing through LlmManager.run_inference; it requires no HTTP config; cloud providers untouched.</done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo build --lib` exits 0
- `cd src-tauri && cargo test --lib` exits 0 (Plan 01 tests still green)
- `cd src-tauri && cargo clippy --lib 2>&1 | grep -c "^error"` returns 0
- `grep -c "commands::llm::" src-tauri/src/lib.rs` returns >= 7
</verification>

<success_criteria>
- 7 LLM commands registered and callable via tauri-specta (LLM-04, MDL-02/03/04)
- LlmManager in managed state, shut down on window close (LLM-03)
- Embedded provider branch routes to run_inference, loads active model on demand, no HTTP config required (LLM-01/04)
- Cloud providers remain opt-in; embedded not in the HTTP provider list
</success_criteria>

<output>
After completion, create `.planning/phases/11-llm-runtime-foundation/11-02-SUMMARY.md`
</output>
