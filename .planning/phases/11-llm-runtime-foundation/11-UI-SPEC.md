---
phase: 11
slug: llm-runtime-foundation
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-01
---

# Phase 11 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (no shadcn; custom Tailwind + CSS variables) |
| Preset | not applicable |
| Component library | Custom (Button, Badge, SettingContainer, SettingsGroup, Alert, Input, Dropdown from src/components/ui/) |
| Icon library | lucide-react |
| Font | System default (antialiased, 15px base, weight 400) |

**Source:** `tailwind.config.js`, `src/App.css`, `components.json` absent — confirmed manual design system.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps (`gap-1`), badge padding (`px-1`), inline chip spacing |
| sm | 8px | Compact element spacing (`gap-2`, `p-2`), card inner gaps |
| md | 16px | Default element spacing (`p-4`, `gap-4`), card padding |
| lg | 24px | Section padding (`space-y-6`), settings group gaps |
| xl | 32px | Layout gaps between major settings groups |
| 2xl | 48px | Not used in settings context (page-level only) |
| 3xl | 64px | Not used in settings context |

Exceptions:
- Drop-zone minimum hit area: 44px height (touch/pointer target accessibility floor)
- Progress bar height: 6px (`h-1.5`) — matches existing ModelCard progress bar
- Divider line: 1px (`border`) — matches `<hr className="w-full border-mid-gray/20">` in ModelCard

**Source:** Observed from `ModelsSettings.tsx`, `ModelCard.tsx`, `PostProcessingSettings.tsx` — reuse verbatim.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 (regular) | 1.5 (`leading-relaxed` on descriptions) |
| Label | 12px (`text-xs`) | 400 (regular) | 1.5 |
| Heading (section) | 14px (`text-sm`) | 500 (`font-medium`) | 1.4 |
| Heading (page) | 20px (`text-xl`) | 500 (`font-medium`) or 600 (`font-semibold`) | 1.2 |

Typography rules enforced by this phase:
- Model name in card: `text-base font-semibold` — matches existing ModelCard `<h3>`
- Model description in card: `text-sm text-text/60 leading-relaxed` — matches existing ModelCard
- Progress percentage / speed: `text-xs tabular-nums text-text/50` — matches existing ModelCard
- Section sub-headings ("Your Models", "Available Models"): `text-sm font-medium text-text/60` — matches ModelsSettings
- Badge text: `text-xs font-medium` — matches existing Badge/pill patterns
- Inline empty-state body: `text-sm text-mid-gray` — matches PostProcessingSettings empty-state pattern

**Source:** `ModelCard.tsx` lines 139, 160, 248-250; `ModelsSettings.tsx` lines 225, 341; `PostProcessingSettings.tsx` line 652.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#fbfbfb` light / `#0a1628` dark (`var(--color-background)`) | Page background, card background |
| Secondary (30%) | `mid-gray/10` to `mid-gray/20` overlays (`var(--color-mid-gray)` = `#808080`) | Card border, section divider, inactive badge fill, drop-zone background |
| Accent (10%) | `#6ba3ff` (`var(--color-logo-primary)`) | See reserved-for list below |
| Destructive | `danger-ghost` variant (implicit red — existing Button variant) | Delete model action only |

Accent (`logo-primary / #6ba3ff`) reserved for:
1. Active model card border (`border-logo-primary/50`) and background tint (`bg-logo-primary/10`)
2. "Active" badge and "Recommended" badge fill/border
3. Download progress bar fill
4. "Embedded (local)" provider selected-state highlight in ProviderPicker
5. Inline empty-state link ("download a model below") underline/hover
6. "Custom" badge outline variant (secondary/outline — inherits `logo-primary` tint)
7. Focus ring on file-picker drop-zone (`focus:ring-logo-primary`)

Secondary (`mid-gray`) reserved for:
- Card border in non-active, non-downloading state: `border-mid-gray/20`
- Drop-zone dashed border: `border-dashed border-mid-gray/40` (matches existing placeholder in PostProcessingSettings line 644)
- Metadata text (size, speed, status): `text-text/50` or `text-mid-gray`
- "Custom" model badge background: `bg-mid-gray/10 text-mid-gray`

Destructive (`danger-ghost` Button variant) reserved for:
- Cancel download button inside ModelCard progress row
- Delete model button in card footer row

**Source:** `App.css` CSS variables, `ModelCard.tsx`, `PostProcessingSettings.tsx` lines 644-663.

---

## Component Inventory

This phase exclusively reuses existing components. No new primitive components are introduced.

### Reused verbatim

| Component | Path | Usage in this phase |
|-----------|------|---------------------|
| `ModelCard` | `src/components/onboarding/ModelCard.tsx` | Each LLM library entry (catalogue + custom) |
| `SettingsGroup` | `src/components/ui/` | "Local Model Library" section wrapper |
| `SettingContainer` | `src/components/ui/` | "Add custom model" sub-section |
| `Badge` | `src/components/ui/Badge.tsx` | Active, Recommended, Custom tags on cards |
| `Button` | `src/components/ui/Button.tsx` | Download, Cancel, Delete, Add custom model |
| `Alert` | `src/components/ui/Alert.tsx` | Inline GGUF-validation error on failed import |

### New compositions (not new primitives)

| Composition | Built from | Purpose |
|-------------|------------|---------|
| `LlmLibrarySection` | `SettingsGroup` + `ModelCard`s + drop-zone div | Replaces placeholder card; anchored at top of PostProcessingSettings |
| `EmbeddedNoModelEmptyState` | Plain div + `text-sm text-mid-gray` + anchor link | Shown inside ProviderPicker row when `embedded` selected, no model present |
| `CustomGgufDropZone` | `div` with dashed border + `Button` (file picker) | Drag-and-drop target + "Add custom model" button below the library list |

### ProviderPicker integration point

Add `"embedded"` to `LOCAL_PROVIDER_IDS_SET` in `PostProcessingSettings.tsx` (line 28). The `renderRowExtras` callback for `option.value === "embedded"` renders `EmbeddedNoModelEmptyState` when no LLM is downloaded; renders nothing when an active model exists.

---

## State Inventory

Every UI state the executor must handle, mapped to its visual treatment.

### LLM Library card states (mirrors ModelCardStatus)

| State | Visual |
|-------|--------|
| `downloadable` | `border-mid-gray/20`; Download icon + size in footer; cursor pointer |
| `downloading` | Progress bar (logo-primary fill, 6px height); percentage + speed text; Cancel button (danger-ghost) |
| `verifying` | Pulsing full-width progress bar; "Verifying…" label |
| `active` | `border-logo-primary/50 bg-logo-primary/10`; Active badge; no Download button |
| `available` | `border-mid-gray/20`; no Download button; Delete button visible |

### GGUF validation states (custom import)

| State | Visual |
|-------|--------|
| Valid import | File copied; card appears in library as `available` with Custom badge |
| Invalid file (not GGUF) | `Alert variant="error" contained` inline below drop-zone: "Not a valid GGUF model file. Please select a .gguf file." |
| Drop-zone active (file dragged over) | `border-logo-primary/50 bg-logo-primary/5` (hover tint) |

### Embedded provider states (in ProviderPicker row)

| State | Visual |
|-------|--------|
| No model downloaded | `EmbeddedNoModelEmptyState` inline below provider row (text + scroll-link to library) |
| Model downloaded, not active | Row selectable; no extra content |
| Model downloading | Progress inside library card (not in provider row) — provider row shows normal state |

### Loading state (library init)

Spinner: `w-8 h-8 border-2 border-logo-primary border-t-transparent rounded-full animate-spin` centered in `py-16` — mirrors ModelsSettings loading state.

---

## Copywriting Contract

All copy below is for i18n key scaffolding in `src/i18n/locales/en/translation.json`. Localization to 20 languages happens in Phase 13 (L10N-01). Phase 11 adds English keys only.

| Element | i18n key path | English copy |
|---------|--------------|--------------|
| Library section title | `settings.postProcessing.modelsAndLocalProcessing.library.title` | "Local Model Library" (replace French placeholder) |
| Library section description | `settings.postProcessing.modelsAndLocalProcessing.library.description` | "Download and manage local GGUF models. Models run entirely on your device — no data leaves your machine." |
| Your models sub-header | `settings.postProcessing.modelsAndLocalProcessing.library.yourModels` | "Your Models" |
| Available models sub-header | `settings.postProcessing.modelsAndLocalProcessing.library.availableModels` | "Available to Download" |
| Recommended badge | `settings.postProcessing.modelsAndLocalProcessing.library.recommendedBadge` | "Recommended" |
| Custom badge | reuse `modelSelector.custom` existing key | "Custom" |
| Model role — Qwen2.5-1.5B | `settings.postProcessing.modelsAndLocalProcessing.library.models.qwen25_1b5.description` | "Fast, lightweight — runs on any machine" |
| Model role — Qwen3-4B | `settings.postProcessing.modelsAndLocalProcessing.library.models.qwen3_4b.description` | "Best quality for post-processing" |
| Model role — TranslateGemma-4B | `settings.postProcessing.modelsAndLocalProcessing.library.models.translate_gemma_4b.description` | "Translation-focused" |
| Primary CTA (download catalogue model) | reuse `modelSelector.download` or add `library.downloadModel` | "Download" |
| Primary CTA (activate model) | reuse `modelSelector.useModel` or add `library.activateModel` | "Use this model" |
| Add custom model button | `settings.postProcessing.modelsAndLocalProcessing.library.addCustomModel` | "Add custom model" |
| Drop-zone label | `settings.postProcessing.modelsAndLocalProcessing.library.dropZoneLabel` | "Drop a .gguf file here" |
| Drop-zone sub-label | `settings.postProcessing.modelsAndLocalProcessing.library.dropZoneSublabel` | "or use the button above" |
| GGUF validation error | `settings.postProcessing.modelsAndLocalProcessing.library.invalidGguf` | "Not a valid GGUF model file. Please select a .gguf file." |
| Empty state — no model, embedded selected (heading) | `settings.postProcessing.modelsAndLocalProcessing.embedded.noModelHeading` | "No local model selected" |
| Empty state — no model, embedded selected (body) | `settings.postProcessing.modelsAndLocalProcessing.embedded.noModelBody` | "Download a model below to enable embedded processing. The Qwen2.5-1.5B model is recommended for most machines." |
| Empty state — link anchor | `settings.postProcessing.modelsAndLocalProcessing.embedded.downloadNowLink` | "Download a model" |
| Embedded provider label | `settings.postProcessing.modelsAndLocalProcessing.embedded.providerLabel` | "Embedded (local)" |
| Embedded provider description | `settings.postProcessing.modelsAndLocalProcessing.embedded.providerDescription` | "Runs on your device. Requires a downloaded local model." |
| Delete model confirmation (catalogue) | reuse `settings.models.deleteConfirm` key — same pattern | "Are you sure you want to delete {{modelName}}? You will need to download it again to use it." |
| Delete model confirmation (active) | reuse `settings.models.deleteActiveConfirm` key | "{{modelName}} is your active model. Deleting it will stop embedded processing until you select a new model. Are you sure?" |
| Delete model dialog title | reuse `settings.models.deleteTitle` | "Delete Model" |
| Download progress label | reuse `modelSelector.downloading` | "Downloading {{percentage}}%" |
| Download speed label | reuse `modelSelector.downloadSpeed` | "{{speed}} MB/s" |
| Cancel download button | reuse `modelSelector.cancel` | "Cancel" |
| Verifying label | reuse `modelSelector.verifyingGeneric` | "Verifying…" |

---

## Interaction Contract

### Model card click behavior

| Card state | Click target | Result |
|------------|-------------|--------|
| `downloadable` | Anywhere on card | Triggers download |
| `available` | Anywhere on card | Sets as active LLM |
| `active` | Anywhere on card | No-op (already active) |
| `downloading` | Cancel button | Cancels download; card returns to `downloadable` |
| `active` or `available` | Delete button (footer) | Opens OS native `ask()` dialog (same pattern as transcription model delete) |

### Custom GGUF import behavior

1. User clicks "Add custom model" button → `tauri-plugin-dialog` file picker opens, filter `.gguf`
2. OR user drags a file onto the drop-zone
3. Backend validates 4-byte GGUF magic header
4. **Valid:** file copied to `$APP_DATA/models/`; card appears in library with Custom badge; status = `available`
5. **Invalid:** `Alert variant="error" contained` renders inline below drop-zone; no card created; drop-zone returns to idle state

### Embedded provider selection + no-model flow

1. User selects "Embedded (local)" in ProviderPicker
2. If no LLM is downloaded: `EmbeddedNoModelEmptyState` appears inline under the provider row
3. "Download a model" link in empty state scrolls to and highlights the `LlmLibrarySection` (same page, anchor scroll, no modal, no navigation)
4. Qwen2.5-1.5B card is visually highlighted (accent border pulse, 1 cycle) to indicate recommendation
5. Once user downloads and activates a model: empty state disappears; provider row shows normal selected state

### Active model indicator

- Only one LLM can be active at a time
- Active card: `border-logo-primary/50 bg-logo-primary/10` with Active badge
- Selecting a new model: previous active card transitions to `available` state; new card transitions to `active`
- Sort order: active model pinned to top of "Your Models" section (mirrors `ModelsSettings.tsx` sort logic)

### No-model library state (no LLMs downloaded at all)

"Your Models" sub-section is hidden. Only "Available to Download" section is shown with 3 catalogue cards. "Add custom model" button and drop-zone are always visible regardless of downloaded count.

---

## Layout Contract

The `LlmLibrarySection` replaces the placeholder card at the top of `PostProcessingSettings` (currently `~line 638`). It is a `SettingsGroup` with:

```
SettingsGroup (title = "Local Model Library")
  ├── [description paragraph — text-sm text-mid-gray]
  ├── [loading spinner OR]
  ├── "Your Models" sub-header (text-sm font-medium text-text/60) — hidden if empty
  │   └── ModelCard × N (downloaded + active models)
  ├── "Available to Download" sub-header (text-sm font-medium text-text/60)
  │   └── ModelCard × 3 (catalogue, not yet downloaded)
  └── CustomGgufDropZone
      ├── Button "Add custom model" (variant=secondary, size=md)
      └── Drop-zone div (dashed border, 44px min-height, centered label)
```

Max width: `max-w-3xl w-full mx-auto` — inherits from PostProcessingSettings outer container.

Space between ModelCards: `space-y-3` — matches ModelsSettings.

Space between sub-sections: `space-y-6` — matches PostProcessingSettings.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn |
| third-party | none | not applicable |

No shadcn, no registries. All components are custom, already in the codebase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
