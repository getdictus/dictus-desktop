---
phase: 11-llm-runtime-foundation
plan: 04
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - src-tauri/tauri.conf.json
  - src-tauri/Cargo.toml
  - .github/workflows/build.yml
autonomous: false
requirements: [LLM-01, LLM-02]
must_haves:
  truths:
    - "A macOS release build (tauri build) makes GPU acceleration work in the packaged app (.metallib bundled or confirmed embedded)"
    - "The Windows x64 build links llama-cpp-2 successfully (vulkan-shaders-gen MSVC failure resolved or Vulkan disabled for that sub-target)"
    - "End-to-end download to load to inference is confirmed on macOS, Windows, and Linux before Phase 12 opens"
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "bundle.resources entry for the macOS .metallib (if the discovered path requires it)"
      contains: "resources"
  key_links:
    - from: "src-tauri/tauri.conf.json"
      to: "llama-cpp-2 OUT_DIR .metallib"
      via: "bundle.resources"
      pattern: "metallib|ggml|resources"
---

<objective>
Close the two platform build gates carried from Phase 10 and run the cross-platform end-to-end verification that gates Phase 12: (1) discover the macOS `.metallib` paths from `llama-cpp-2`'s OUT_DIR via a real `tauri build` and bundle them if required; (2) resolve the Windows x64 `vulkan-shaders-gen` MSVC build failure; (3) confirm download to load to inference on macOS, Windows, and Linux.

Purpose: This is the ROADMAP spike gate for Phase 11. Phase 12 does not open until GPU bundling and all-platform inference are confirmed.
Output: `tauri.conf.json` (bundle.resources if needed), possibly `Cargo.toml`/CI changes for Windows x64, and a human-verified cross-platform smoke result.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-llm-runtime-foundation/11-RESEARCH.md
@.planning/phases/11-llm-runtime-foundation/11-03-SUMMARY.md

<interfaces>
<!-- src-tauri/tauri.conf.json lines 26-30: bundle.resources currently ["resources/**/*"]. Add the .metallib path(s) discovered below (or copy into src-tauri/resources/ which is already globbed). -->
<!-- src-tauri/Cargo.toml: llama-cpp-2 0.1.146 per-target features — Windows/Linux "vulkan" (lines 97,116), macOS "metal" (line 110). -->
<!-- STATE.md Blockers carried into Phase 11:
     - Windows-x64 vulkan-shaders-gen MSVC build failure (CI run 26749959299) — NOT a ggml conflict, NOT a missing Vulkan SDK; vulkan-shaders-gen ExternalProject cmake_install.cmake not generated under MSBuild on MSVC x64. Windows ARM64 builds fine.
     - .metallib bundle-resource paths unconfirmed — discover via `tauri build` (NOT `tauri dev`).
     - AMD Vulkan driver 25.11.1 crash risk (llama.cpp #17432) — monitor, not a hard blocker for this gate. -->
<!-- RESEARCH Pitfall 1 (.metallib): after backend builds, run `find target/release/build -name "*.metallib"` on macOS; add to bundle.resources if not embedded.
     RESEARCH Pitfall 2 (Win x64): options in order — (a) check if a newer llama-cpp-2 includes the upstream fix; (b) Ninja generator workaround (issue #11788); (c) fallback: disable Vulkan on Windows x64 only (CPU on that sub-target). -->
<!-- Build cmd: `bun run tauri build`; if cmake policy error: `CMAKE_POLICY_VERSION_MINIMUM=3.5 bun run tauri build`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover macOS .metallib paths and bundle them if required</name>
  <read_first>
    - src-tauri/tauri.conf.json (bundle block lines 26-30)
    - .planning/phases/11-llm-runtime-foundation/11-RESEARCH.md (Pitfall 1, Open Question 1)
  </read_first>
  <action>
    1. On macOS, run a real release build (NOT `tauri dev`): `bun run tauri build` (if a cmake policy error appears use `CMAKE_POLICY_VERSION_MINIMUM=3.5 bun run tauri build`). A full `cargo build --release` is sufficient to populate OUT_DIR if the full bundle is too slow.
    2. Discover shader artifacts: `find src-tauri/target/release/build -name "*.metallib"`. Record the exact relative path(s) found (e.g. under `target/release/build/llama-cpp-sys-2-*/out/`) in the SUMMARY.
    3. Determine whether GPU offload works in the packaged app WITHOUT bundling (llama.cpp often embeds the default Metal library in the binary). If GPU works without bundling, make NO tauri.conf.json change and record "metallib embedded, no bundling needed" in the SUMMARY.
    4. If a `.metallib` IS required at runtime, copy it into `src-tauri/resources/` (already globbed by `"resources/**/*"`) as part of the build, OR add an explicit path entry to `bundle.resources`. Because the OUT_DIR path is build-hash-specific, prefer the copy-into-`resources/` approach. Document the mechanism in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-desktop && find src-tauri/target/release/build -name "*.metallib" 2>/dev/null | head; node -e "JSON.parse(require('fs').readFileSync('src-tauri/tauri.conf.json','utf8'))" && echo conf-valid</automated>
  </verify>
  <acceptance_criteria>
    - The `find ... *.metallib` output (paths or empty) is recorded in 11-04-SUMMARY.md
    - EITHER `tauri.conf.json` bundle.resources references a metallib/ggml resource (or one is copied into src-tauri/resources/), OR the SUMMARY explicitly states the metallib is embedded and no bundling is needed
    - `node -e "JSON.parse(...tauri.conf.json...)"` exits 0 (valid JSON)
  </acceptance_criteria>
  <done>The macOS .metallib situation is resolved (bundled or confirmed embedded); tauri.conf.json valid.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the Windows x64 vulkan-shaders-gen MSVC build failure</name>
  <read_first>
    - src-tauri/Cargo.toml (llama-cpp-2 per-target features lines 97-116)
    - .github/workflows/build.yml (Windows x64 build job)
    - .planning/phases/11-llm-runtime-foundation/11-RESEARCH.md (Pitfall 2, Open Question 2)
    - .planning/STATE.md (Windows-x64 vulkan blocker, CI run 26749959299)
  </read_first>
  <action>
    Apply the FIRST option that produces a green Windows x64 build, in this priority order (per RESEARCH Pitfall 2):
    1. Check whether a newer `llama-cpp-2` (> 0.1.146) includes the upstream fix for llama.cpp issue #11788 (use ctx7 / crates.io to check the latest version). If a fixed version exists and still satisfies the Phase 10 ggml-coexistence constraint, bump the dependency version in Cargo.toml.
    2. If no version bump is available: apply the Ninja-generator workaround — install Ninja in the Windows CI job in `.github/workflows/build.yml` and set `CMAKE_GENERATOR=Ninja` (env) for the llama-cpp-sys-2 build on the Windows x64 job. Confirm llama-cpp-sys-2 honors the env.
    3. Fallback (only if 1 and 2 fail): disable Vulkan for Windows x64 ONLY (keep Windows ARM64 on Vulkan). In Cargo.toml, split the `cfg(windows)` target dependency into an x86_64 entry (no GPU features = CPU) and an aarch64 entry (vulkan). Document the CPU-only-on-Windows-x64 tradeoff in the SUMMARY and add a follow-up todo under `.planning/todos/pending/`.
    Whichever option is used, push the branch and confirm the Windows x64 CI job goes green.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-desktop && grep -n "llama-cpp-2" src-tauri/Cargo.toml; echo cargo-checked</automated>
  </verify>
  <acceptance_criteria>
    - The chosen resolution (version bump / Ninja / Vulkan-disabled-x64) is documented in 11-04-SUMMARY.md with the rationale
    - If Ninja workaround: `.github/workflows/build.yml` contains a Ninja install step and `CMAKE_GENERATOR` for the Windows job
    - If fallback used: Cargo.toml has separate x86_64-windows and aarch64-windows llama-cpp-2 entries
    - The Windows x64 CI job for this branch reports success (link to the green run in the SUMMARY)
  </acceptance_criteria>
  <done>Windows x64 links llama-cpp-2 (CI green) via the highest-priority working option; tradeoff (if any) documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cross-platform end-to-end inference smoke gate</name>
  <what-built>
    The full Phase 11 stack: LlmManager backend (download/verify/load/infer/unload + idle watcher), LLM Tauri commands, the embedded post-processing provider branch, and the real LlmLibrarySection UI. This checkpoint verifies the end-to-end flow works on all three platforms before Phase 12 opens.
  </what-built>
  <action>
    Human-only cross-platform verification — no autonomous action. The executor pauses here and presents the verification steps below. This task cannot be automated because it requires real GPU hardware per OS, interactive UI observation, and real multi-GB model downloads on each platform.
  </action>
  <how-to-verify>
    On EACH of macOS, Windows, and Linux (use `bun run tauri build` release builds — `tauri dev` does NOT replicate bundle layout for GPU resources):
    1. Open Settings -> Post-processing. Confirm the "Local Model Library" section appears at the top (placeholder gone) with 3 catalogue models showing on-disk sizes BEFORE download.
    2. Download the recommended Qwen2.5-1.5B model. Confirm live progress, then cancel mid-download and confirm it can resume on retry. Let it complete; confirm SHA256 verification succeeds.
    3. Mark the model active ("Use this model"). Confirm the active indicator.
    4. Select "Embedded (local)" as the post-processing provider. Record a short phrase with post-processing enabled; confirm the embedded LLM produces processed output (inference runs). On macOS/Windows/Linux confirm GPU offload occurs (check logs for GPU layers) with CPU fallback when no GPU.
    5. While inference runs, confirm the overlay and shortcuts stay responsive (LLM-01/03 non-blocking).
    6. Add a custom GGUF via the "Add custom model" button AND via drag-drop; confirm it appears with a Custom badge. Try a non-GGUF file; confirm the inline "Not a valid GGUF model file" error appears and no broken entry is added.
    7. Delete a downloaded model; confirm disk space is reclaimed.
    8. Wait past the LLM idle timeout; confirm the LLM unloads (logs) and the transcription model is unaffected.
  </how-to-verify>
  <verify>
    <automated>MANUAL — cross-platform human verification on macOS, Windows, Linux release builds; no automated command.</automated>
  </verify>
  <resume-signal>Type "approved" if all 3 platforms pass, or describe which platform/step failed.</resume-signal>
  <done>Download -> load -> inference -> idle-unload confirmed working on macOS, Windows, and Linux release builds; Phase 12 may open.</done>
</task>

</tasks>

<verification>
- macOS .metallib resolved (bundled or confirmed embedded); tauri.conf.json valid JSON
- Windows x64 CI job green (vulkan-shaders-gen resolved or Vulkan-disabled-x64 fallback)
- Human-verified end-to-end download -> load -> inference on macOS, Windows, Linux
</verification>

<success_criteria>
- GPU acceleration works in the packaged macOS app (LLM-02)
- Windows x64 builds and links llama-cpp-2 (LLM-01/02)
- Cross-platform end-to-end inference smoke gate passes — Phase 12 may open (LLM-01..04, MDL-01..05 observable end-to-end)
</success_criteria>

<output>
After completion, create `.planning/phases/11-llm-runtime-foundation/11-04-SUMMARY.md`
</output>
