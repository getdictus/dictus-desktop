---
phase: 11-llm-runtime-foundation
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/stores/llmModelStore.ts
  - src/components/settings/post-processing/LlmLibrarySection.tsx
  - src/components/settings/post-processing/CustomGgufDropZone.tsx
  - src/components/settings/post-processing/EmbeddedNoModelEmptyState.tsx
  - src/components/settings/post-processing/PostProcessingSettings.tsx
  - src/i18n/locales/en/translation.json
autonomous: true
requirements: [LLM-04, MDL-01, MDL-02, MDL-03, MDL-04, MDL-05]
must_haves:
  truths:
    - "The 'Bibliothèque de modèles locaux' placeholder card is replaced by a real, functional model library anchored at the top of the local-processing page"
    - "Each catalogue model shows its on-disk size before download; user can download (live progress), cancel, delete, and mark one model active"
    - "User can add a custom GGUF via the 'Add custom model' file picker or by drag-drop; invalid files show an inline error"
    - "'Embedded (local)' is selectable as a provider; when no model is downloaded, an inline empty state links to the highlighted recommended model"
  artifacts:
    - path: "src/components/settings/post-processing/LlmLibrarySection.tsx"
      provides: "Real model library (catalogue + custom) using ModelCard"
      contains: "LlmLibrarySection"
    - path: "src/stores/llmModelStore.ts"
      provides: "Zustand store wiring LLM commands + llm-* events"
      contains: "getLlmModels"
  key_links:
    - from: "src/components/settings/post-processing/PostProcessingSettings.tsx"
      to: "LlmLibrarySection"
      via: "import + render replacing placeholder"
      pattern: "LlmLibrarySection"
    - from: "src/stores/llmModelStore.ts"
      to: "llm-download-progress event"
      via: "listen()"
      pattern: "llm-download-progress"
    - from: "src/components/settings/post-processing/PostProcessingSettings.tsx"
      to: "embedded provider set"
      via: "LOCAL_PROVIDER_IDS_SET"
      pattern: "\"embedded\""
---

<objective>
Replace the placeholder "Local Model Library" card with a real, functional library (`LlmLibrarySection`) that reuses `ModelCard` for download/cancel/delete/active, add custom GGUF import (file picker + drag-drop), add "embedded" to the selectable provider set with the no-model empty state, and scaffold all English i18n keys.

Purpose: Completes the user-facing model library (MDL-05) and makes the embedded provider discoverable (LLM-04). Honors 11-UI-SPEC.md verbatim.
Output: `llmModelStore.ts`, `LlmLibrarySection.tsx`, `CustomGgufDropZone.tsx`, `EmbeddedNoModelEmptyState.tsx` (new), `PostProcessingSettings.tsx` (edits), English translation keys.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-llm-runtime-foundation/11-UI-SPEC.md
@.planning/phases/11-llm-runtime-foundation/11-CONTEXT.md
@.planning/phases/11-llm-runtime-foundation/11-02-SUMMARY.md

<interfaces>
<!-- tauri-specta bindings regenerated by Plan 02 (src/bindings.ts). Available `commands.*` (camelCase): -->
- `commands.getLlmModels(): Promise<Result<LlmModelInfo[], string>>`
- `commands.downloadLlmModel(modelId)`, `commands.cancelLlmDownload(modelId)`, `commands.deleteLlmModel(modelId)`
- `commands.setActiveLlmModel(modelId)`, `commands.getActiveLlmModel(): Promise<Result<string | null, string>>`
- `commands.importCustomLlmModel(path): Promise<Result<LlmModelInfo, string>>`
- Type `LlmModelInfo` from `@/bindings`: { id, name, description, filename, url, sha256, size_mb, is_downloaded, is_downloading, partial_size, is_custom, is_recommended }
- Backend events (use `listen` from `@tauri-apps/api/event`): `llm-download-progress` (payload `{ model_id, downloaded, total, percentage }`), `llm-verification-started`, `llm-verification-completed`, `llm-download-complete`, `llm-download-cancelled`, `llm-download-failed`, `llm-model-loaded`, `llm-model-unloaded`, `llm-deleted`, `llm-custom-model-imported`

<!-- Reuse patterns from src/stores/modelStore.ts (transcription store): Zustand `create()(immer(...))`; `listen("...")` subscriptions; `downloadingModels`/`downloadProgress`/`downloadStats` maps keyed by model id. -->

<!-- ModelCard (src/components/onboarding/ModelCard.tsx) props: -->
```ts
interface ModelCardProps {
  model: ModelInfo;            // takes ModelInfo (NOT LlmModelInfo) — adapt via mapper
  variant?: "default" | "featured";
  status?: "downloadable" | "downloading" | "verifying" | "extracting" | "switching" | "active" | "available";
  disabled?: boolean; className?: string;
  onSelect: (modelId: string) => void;
  onDownload?: (modelId: string) => void;
  onDelete?: (modelId: string) => void;
  onCancel?: (modelId: string) => void;
  downloadProgress?: number; downloadSpeed?: number; showRecommended?: boolean;
}
```

<!-- PostProcessingSettings.tsx: line 28 `const LOCAL_PROVIDER_IDS_SET = new Set(["apple_intelligence", "custom"]);` → add "embedded".
     Lines ~637-665: placeholder SettingsGroup with library.comingSoonBadge/comingSoonBody/currentBridge — replace entirely with <LlmLibrarySection />.
     Uses `const { t } = useTranslation();`. -->

<!-- i18n keys under settings.postProcessing.modelsAndLocalProcessing.library.* and .embedded.* in src/i18n/locales/en/translation.json.
     REUSE existing keys: modelSelector.custom, modelSelector.downloading ("Downloading {{percentage}}%"), modelSelector.downloadSpeed, modelSelector.verifyingGeneric, settings.models.deleteConfirm/deleteActiveConfirm/deleteTitle.
     ESLint forbids hardcoded JSX strings — every visible string MUST use t(). -->

<!-- File picker: `import { open } from "@tauri-apps/plugin-dialog";` (already a dep). Drag-drop: `getCurrentWebview().onDragDropEvent(...)` from `@tauri-apps/api/webview`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add English i18n keys and create llmModelStore.ts</name>
  <read_first>
    - src/i18n/locales/en/translation.json (library block ~line 445, modelSelector ~line 124, settings.models delete keys)
    - src/stores/modelStore.ts (Zustand + immer + listen pattern, download/cancel/delete actions, event handling)
    - src/bindings.ts (confirm generated LlmModelInfo type + commands.* names after Plan 02)
  </read_first>
  <action>
    1. In `src/i18n/locales/en/translation.json` under `settings.postProcessing.modelsAndLocalProcessing.library`, REPLACE the placeholder keys (`comingSoonBadge`, `comingSoonBody`, `currentBridge`) and set/add these EXACT English values (from 11-UI-SPEC Copywriting Contract):
       - `title`: "Local Model Library"
       - `description`: "Download and manage local GGUF models. Models run entirely on your device — no data leaves your machine."
       - `yourModels`: "Your Models"
       - `availableModels`: "Available to Download"
       - `recommendedBadge`: "Recommended"
       - `downloadModel`: "Download model"
       - `activateModel`: "Use this model"
       - `addCustomModel`: "Add custom model"
       - `dropZoneLabel`: "Drop a .gguf file here"
       - `dropZoneSublabel`: "or use the button above"
       - `invalidGguf`: "Not a valid GGUF model file. Please select a .gguf file."
       - `cancelDownload`: "Cancel download"
       - `models.qwen25_1b5.description`: "Fast, lightweight — runs on any machine"
       - `models.qwen3_4b.description`: "Best quality for post-processing"
       - `models.translate_gemma_4b.description`: "Translation-focused"
       Then add a sibling `embedded` object under `modelsAndLocalProcessing` with:
       - `noModelHeading`: "No local model selected"
       - `noModelBody`: "Download a model below to enable embedded processing. The Qwen2.5-1.5B model is recommended for most machines."
       - `downloadNowLink`: "Download a model"
       - `providerLabel`: "Embedded (local)"
       - `providerDescription`: "Runs on your device. Requires a downloaded local model."
       Keep valid JSON (no trailing commas).

    2. Create `src/stores/llmModelStore.ts` mirroring `modelStore.ts`:
       - State: `models: LlmModelInfo[]`, `activeModelId: string | null`, `downloadProgress: Record<string, { downloaded: number; total: number; percentage: number }>`, `downloadStats: Record<string, { speedMbps: number }>`, `verifyingModels: Record<string, true>`, `isLoading: boolean`.
       - Actions: `refresh()` (calls `commands.getLlmModels()` + `commands.getActiveLlmModel()`), `downloadModel(id)` (`commands.downloadLlmModel`), `cancelDownload(id)`, `deleteModel(id)`, `setActiveModel(id)` (`commands.setActiveLlmModel`), `importCustom(path)` (`commands.importCustomLlmModel`, returns `{ ok: boolean; error?: string }`).
       - `initListeners()`: subscribe with `listen` to `llm-download-progress` (update `downloadProgress`, compute speed into `downloadStats`), `llm-verification-started`/`llm-verification-completed` (toggle `verifyingModels`), `llm-download-complete`/`llm-download-cancelled`/`llm-download-failed` (clear progress + `refresh()`), `llm-deleted`/`llm-custom-model-imported`/`llm-model-loaded` (`refresh()`). Return an unlisten cleanup function.
  </action>
  <verify>
    <automated>bun run check:translations 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "comingSoonBadge" src/i18n/locales/en/translation.json` returns 0 (placeholder removed)
    - `translation.json` library block contains exact strings: "Local Model Library", "Your Models", "Available to Download", "Add custom model", "Download model", "Use this model"
    - `translation.json` contains an `embedded` object with `providerLabel` "Embedded (local)" and `noModelBody` mentioning "Qwen2.5-1.5B"
    - `llmModelStore.ts` contains `getLlmModels`, `setActiveLlmModel`, `importCustomLlmModel`, and `listen("llm-download-progress"`
    - `bun run check:translations` exits 0
  </acceptance_criteria>
  <done>English keys scaffolded (placeholder removed); llmModelStore wires all commands + llm-* events; translations check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create LlmLibrarySection + CustomGgufDropZone + EmbeddedNoModelEmptyState</name>
  <read_first>
    - .planning/phases/11-llm-runtime-foundation/11-UI-SPEC.md (Layout Contract, State Inventory, Interaction Contract; h-2 progress bar, font-medium not font-semibold notes)
    - src/components/onboarding/ModelCard.tsx (props, ModelCardStatus, status classes)
    - src/components/settings/models/ModelsSettings.tsx (getModelStatus mapping, active-first sort, ask() delete confirm via @tauri-apps/plugin-dialog)
    - src/stores/llmModelStore.ts (from Task 1)
    - src/components/ui/ (SettingsGroup, Button, Alert prop shapes)
  </read_first>
  <action>
    1. Create `src/components/settings/post-processing/LlmLibrarySection.tsx` per the 11-UI-SPEC Layout Contract:
       - Root `SettingsGroup` with `title={t("settings.postProcessing.modelsAndLocalProcessing.library.title")}` and an `id="llm-library-section"` on its outer wrapper (so the empty-state link can scroll to it).
       - First child: `<p className="text-sm text-mid-gray leading-relaxed">{t("...library.description")}</p>`.
       - Loading state: when `store.isLoading`, render the spinner `<div className="py-16 flex justify-center"><div className="w-8 h-8 border-2 border-logo-primary border-t-transparent rounded-full animate-spin" /></div>`.
       - "Your Models" sub-header `<p className="text-sm font-medium text-text/60">{t("...library.yourModels")}</p>` rendered ONLY if downloaded models exist; below it a `space-y-3` wrapper of downloaded/active `ModelCard`s with the active model sorted to top.
       - "Available to Download" sub-header (same class) + `space-y-3` wrapper of catalogue (not-downloaded) `ModelCard`s.
       - `<CustomGgufDropZone />` last.
       - Use `useLlmModelStore`; in a `useEffect` on mount call `refresh()` and `initListeners()` (cleanup unlisten on unmount).
       - Write `toModelCardModel(m: LlmModelInfo): ModelInfo` to adapt to ModelCard's `ModelInfo` prop: keep `id, name, description, filename, url, sha256, size_mb, is_downloaded, is_downloading, partial_size, is_custom, is_recommended`; fill whisper-only fields with safe defaults (`is_directory: false`, `engine_type: "Whisper"` or any valid `EngineType`, `accuracy_score: 0`, `speed_score: 0`, `supports_translation: false`, `supported_languages: []`, `supports_language_selection: false`).
       - Status mapping (mirror ModelsSettings.getModelStatus): in `downloadProgress` → "downloading"; in `verifyingModels` → "verifying"; `id === activeModelId` → "active"; downloaded & not active → "available"; else → "downloadable".
       - Handlers: `onDownload` → `store.downloadModel`; `onCancel` → `store.cancelDownload`; `onSelect` → if "downloadable" download, if "available" `store.setActiveModel`; `onDelete` → `ask()` confirm (reuse `settings.models.deleteConfirm` / `deleteActiveConfirm` / `deleteTitle` keys with the model name) then `store.deleteModel`.
       - Do NOT fork ModelCard. If ModelCard's progress bar is `h-1.5` rather than the UI-SPEC `h-2`, accept ModelCard as-is and note the deviation in the SUMMARY. Do NOT introduce a third font weight or new colors.

    2. Create `src/components/settings/post-processing/CustomGgufDropZone.tsx`:
       - "Add custom model" `Button` (variant secondary, size md) calling `open({ filters: [{ name: "GGUF", extensions: ["gguf"] }], multiple: false })`; on a returned path call `store.importCustom(path)`.
       - Drop-zone `div`: `border border-dashed border-mid-gray/40` (active drag: `border-logo-primary/50 bg-logo-primary/5`), `min-h-[44px]`, centered label `t("...library.dropZoneLabel")` + sub-label `t("...library.dropZoneSublabel")`, `focus:ring-logo-primary`. Use `getCurrentWebview().onDragDropEvent` from `@tauri-apps/api/webview` to capture dropped file paths and call `store.importCustom(path)`; toggle the active-drag classes on `enter`/`leave`.
       - On import error (`{ ok:false, error }`), render `<Alert variant="error" contained>` below the drop-zone showing `t("...library.invalidGguf")` (or the backend error string). Clear on next successful action.

    3. Create `src/components/settings/post-processing/EmbeddedNoModelEmptyState.tsx`:
       - Plain div `text-sm text-mid-gray`: heading `t("...embedded.noModelHeading")`, body `t("...embedded.noModelBody")`, and an anchor/button `t("...embedded.downloadNowLink")` (logo-primary underline/hover) that calls `document.getElementById("llm-library-section")?.scrollIntoView({ behavior: "smooth" })` and signals the recommended Qwen2.5-1.5B card to do a one-cycle accent-border pulse.
       - Respect `prefers-reduced-motion: reduce`: when reduced, skip the pulse and render a static accent border (`border-logo-primary/50`) on the recommended card only (per UI-SPEC Interaction Contract step 4).
  </action>
  <verify>
    <automated>bunx tsc --noEmit 2>&1 | grep -iE "LlmLibrarySection|CustomGgufDropZone|EmbeddedNoModel" | head; echo "tsc-checked"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist; `LlmLibrarySection.tsx` contains `id="llm-library-section"`, `SettingsGroup`, and `ModelCard`
    - `LlmLibrarySection.tsx` contains a `toModelCardModel` mapper and references `useLlmModelStore`
    - `CustomGgufDropZone.tsx` contains `open(` (file picker), `onDragDropEvent`, and `Alert`
    - `EmbeddedNoModelEmptyState.tsx` contains `scrollIntoView` and `prefers-reduced-motion`
    - `bunx tsc --noEmit` reports no errors in these 3 files (grep returns empty)
  </acceptance_criteria>
  <done>The three composition components exist, reuse ModelCard/SettingsGroup/Button/Alert verbatim, and type-check clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire LlmLibrarySection + embedded provider into PostProcessingSettings</name>
  <read_first>
    - src/components/settings/post-processing/PostProcessingSettings.tsx (line 28 LOCAL_PROVIDER_IDS_SET; placeholder SettingsGroup ~lines 637-665; provider row render / renderRowExtras for ProviderPicker)
    - src/components/settings/post-processing/EmbeddedNoModelEmptyState.tsx + LlmLibrarySection.tsx (from Task 2)
    - .planning/phases/11-llm-runtime-foundation/11-UI-SPEC.md (ProviderPicker integration point; embedded provider states)
  </read_first>
  <action>
    1. On line 28 of `PostProcessingSettings.tsx`, change `LOCAL_PROVIDER_IDS_SET` to `new Set(["apple_intelligence", "custom", "embedded"])`.

    2. Replace the entire placeholder `SettingsGroup` block (the `comingSoonBadge`/`comingSoonBody`/`currentBridge` card ~lines 637-665) with `<LlmLibrarySection />` (import it from `./LlmLibrarySection`). Keep it anchored at the SAME position (top of the local-processing area, before the Hotkey group) so MDL-05 "anchored at the top" holds.

    3. Ensure "embedded" appears as a selectable provider option in the ProviderPicker. If providers come from a fixed local list, add an `embedded` entry using `t("...embedded.providerLabel")` / `t("...embedded.providerDescription")`. In the picker's `renderRowExtras` (or equivalent per-option render hook), when `option.value === "embedded"` AND no LLM model is active/downloaded, render `<EmbeddedNoModelEmptyState />` inline under the row; render nothing when an active model exists. Use `useLlmModelStore` to read the active/downloaded state.

    4. Confirm no hardcoded JSX strings are introduced (all new visible text uses `t()`), so ESLint passes.
  </action>
  <verify>
    <automated>bun run lint 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "LOCAL_PROVIDER_IDS_SET" src/components/settings/post-processing/PostProcessingSettings.tsx` shows the set includes `"embedded"`
    - `grep -c "comingSoonBadge\|comingSoonBody\|currentBridge" src/components/settings/post-processing/PostProcessingSettings.tsx` returns 0 (placeholder removed)
    - `PostProcessingSettings.tsx` contains `<LlmLibrarySection` and `EmbeddedNoModelEmptyState`
    - `bun run lint` exits 0 (no hardcoded-string violations)
    - `bunx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Placeholder replaced by the real library at the top of the page; "embedded" is selectable with the no-model empty state wired into the ProviderPicker; lint clean.</done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` exits 0
- `bun run lint` exits 0 (i18n enforcement passes — no hardcoded JSX strings)
- `bun run check:translations` exits 0
- Placeholder coming-soon card no longer present anywhere in PostProcessingSettings.tsx
</verification>

<success_criteria>
- Real model library replaces the placeholder, anchored at top (MDL-05)
- Catalogue shows pre-download sizes; download/cancel/delete/active all functional via the store (MDL-01/02/03)
- Custom GGUF import via file picker + drag-drop with inline invalid-file error (MDL-04)
- "Embedded (local)" selectable with no-model empty state linking to the highlighted recommended model (LLM-04)
- All new strings localized via i18next (English scaffolded; 20-locale propagation is Phase 13)
</success_criteria>

<output>
After completion, create `.planning/phases/11-llm-runtime-foundation/11-03-SUMMARY.md`
</output>
