---
phase: 11-llm-runtime-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/managers/llm.rs
  - src-tauri/src/managers/mod.rs
  - src-tauri/src/settings.rs
autonomous: true
requirements: [LLM-01, LLM-02, LLM-03, MDL-01, MDL-02, MDL-03, MDL-04]
must_haves:
  truths:
    - "The 3 catalogue models (Qwen2.5-1.5B, Qwen3-4B, TranslateGemma-4B) are defined with HuggingFace CDN URLs, SHA256 hashes, and on-disk sizes available before download"
    - "A GGUF model file passes header validation only if its first 4 bytes are the GGUF magic; non-GGUF files are rejected"
    - "A downloaded LLM file can be deleted and its file removed from disk"
    - "The loaded LLM unloads after its own idle timeout via an independent watcher thread"
    - "Inference dispatches on a blocking thread (spawn_blocking) so it never blocks the async executor"
  artifacts:
    - path: "src-tauri/src/managers/llm.rs"
      provides: "LlmManager: catalogue, download/verify/delete, GGUF validation, model load/infer/unload, idle watcher"
      min_lines: 300
      contains: "pub struct LlmManager"
    - path: "src-tauri/src/settings.rs"
      provides: "active_llm_model_id and llm_unload_timeout fields on AppSettings"
      contains: "active_llm_model_id"
  key_links:
    - from: "src-tauri/src/managers/llm.rs"
      to: "huggingface.co CDN"
      via: "catalogue url field"
      pattern: "huggingface\\.co/.*resolve/main"
    - from: "src-tauri/src/managers/llm.rs"
      to: "GGUF magic bytes"
      via: "validate_gguf_header"
      pattern: "b\"GGUF\""
    - from: "src-tauri/src/managers/llm.rs"
      to: "background thread"
      via: "spawn_blocking inference"
      pattern: "spawn_blocking"
---

<objective>
Create the `LlmManager` Rust backend that mirrors `ModelManager` (download/verify/delete) and `TranscriptionManager` (load/unload/idle-watcher) for in-process GGUF inference via `llama-cpp-2`. Also add the two new `AppSettings` fields the manager needs.

Purpose: This is the engine foundation for Phase 11. Plans 02 (commands/provider wiring) and 03 (frontend) build directly on the types and methods defined here.
Output: `src-tauri/src/managers/llm.rs` (new), `managers/mod.rs` (register module), `settings.rs` (2 new fields), all with passing unit tests.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-llm-runtime-foundation/11-RESEARCH.md
@.planning/phases/11-llm-runtime-foundation/11-CONTEXT.md

<interfaces>
<!-- Extracted from src-tauri/src/managers/model.rs — the LlmManager mirrors these. -->

ModelInfo struct (model.rs:32) — LlmModelInfo is a slimmed version:
```rust
pub struct ModelInfo {
    pub id: String, pub name: String, pub description: String, pub filename: String,
    pub url: Option<String>, pub sha256: Option<String>, pub size_mb: u64,
    pub is_downloaded: bool, pub is_downloading: bool, pub partial_size: u64,
    pub is_custom: bool, pub is_recommended: bool, /* ...whisper-only fields omitted... */
}
```

DownloadProgress (model.rs:56):
```rust
pub struct DownloadProgress { pub model_id: String, pub downloaded: u64, pub total: u64, pub percentage: f64 }
```

DownloadCleanup RAII guard (model.rs:66) — copy this pattern for LLM downloads.

Reusable functions to clone verbatim from model.rs (single-file, no tar.gz extraction):
- `download_model()` (model.rs:987) — range-request resume, DownloadCleanup, 10/sec event throttle (model.rs:1122)
- `verify_sha256()` (model.rs:941), `compute_sha256()` (model.rs:973) — copy verbatim
- `delete_model()` (model.rs:1327) — use the non-directory (file) path only
- `cancel_download()` (model.rs:1442) — cancel-flag pattern, identical

App data dir: `crate::portable::app_data_dir(app_handle)` then join `"models"` — SAME `$APP_DATA/models/` directory ModelManager uses.

From src-tauri/src/managers/transcription.rs:88-160 — idle watcher template:
- `last_activity: Arc<AtomicU64>` reset via `touch_activity()`
- watcher thread: `thread::spawn`, `thread::sleep(Duration::from_secs(10))`, check `shutdown_signal.load(Ordering::Relaxed)`, read timeout from `get_settings(&app_handle)`, compare `now_ms.saturating_sub(last) > limit_seconds*1000`, call `unload_model()`
- `shutdown_signal: Arc<AtomicBool>` set true in `shutdown()`

From src-tauri/src/settings.rs:
- `pub enum ModelUnloadTimeout` (line 119) with `#[default] Min5`, method `to_seconds() -> Option<u64>`
- `pub struct AppSettings` (line 308); existing fields `selected_model: String` (323), `model_unload_timeout: ModelUnloadTimeout` (345)
- `get_settings(&app_handle)` and `write_settings(...)` are the accessors (model.rs imports them at line 1)

Crate name for tests: lib crate is `dictus_app_lib`, package is `dictus`. Run with `cargo test --lib`.

llama-cpp-2 0.1.146 API (RESEARCH.md Code Examples, verified against docs.rs):
```rust
use llama_cpp_2::llama_backend::LlamaBackend;
use llama_cpp_2::model::{AddBos, LlamaModel, Special};
use llama_cpp_2::model::params::LlamaModelParams;
use llama_cpp_2::context::params::LlamaContextParams;
let backend = LlamaBackend::init()?;                          // once at startup
let model_params = LlamaModelParams::default().with_n_gpu_layers(i32::MAX); // GPU if avail, CPU fallback
let model = LlamaModel::load_from_file(&backend, model_path, &model_params)?;
// context per inference call inside spawn_blocking:
let ctx_params = LlamaContextParams::default().with_n_ctx(NonZeroU32::new(2048)).with_n_threads(num_cpus::get() as u32 / 2);
let mut ctx = model.new_context(&backend, ctx_params)?;
```
`num_cpus` and `tempfile` are already in Cargo.toml. llama-cpp-2 feature flags (metal/vulkan per-target) already configured — no Cargo.toml changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add LLM settings fields and define LlmModelInfo + catalogue with passing serde/catalogue tests</name>
  <read_first>
    - src-tauri/src/settings.rs (AppSettings struct ~line 308, ModelUnloadTimeout enum ~line 119, default_settings ~line 748)
    - src-tauri/src/managers/model.rs (ModelInfo struct lines 32-53 — mirror the field pattern)
    - src-tauri/src/managers/mod.rs (module list to add `pub mod llm;`)
    - .planning/phases/11-llm-runtime-foundation/11-RESEARCH.md (Confirmed Catalogue Metadata table — copy exact values)
  </read_first>
  <behavior>
    - Test `test_catalogue_has_three_models`: `LlmManager::catalogue()` returns exactly 3 entries.
    - Test `test_catalogue_sizes_nonzero`: every catalogue entry has `size_mb > 0`.
    - Test `test_catalogue_urls_are_huggingface`: every entry's `url` contains `"huggingface.co"` and `"resolve/main"`, and NO entry's url contains `"blob.handy.computer"`.
    - Test `test_catalogue_recommended_is_qwen25`: exactly one entry has `is_recommended == true` and its id is `"qwen2.5-1.5b"`.
    - Test `test_settings_serde_defaults_llm_fields`: deserializing a JSON object WITHOUT `active_llm_model_id`/`llm_unload_timeout` yields `active_llm_model_id == None` and `llm_unload_timeout == ModelUnloadTimeout::Min5`.
  </behavior>
  <action>
    1. In `src-tauri/src/managers/mod.rs` add `pub mod llm;` to the module list (alongside `pub mod model;`).

    2. In `src-tauri/src/settings.rs`, add two fields to `AppSettings` (each MUST have `#[serde(default)]` for backward compatibility with existing settings_store.json):
       ```rust
       #[serde(default)]
       pub active_llm_model_id: Option<String>,
       #[serde(default)]
       pub llm_unload_timeout: ModelUnloadTimeout,
       ```
       Also add them to the `default_settings()` constructor (near `selected_model`/`model_unload_timeout` ~line 748): `active_llm_model_id: None,` and `llm_unload_timeout: ModelUnloadTimeout::default(),`.

    3. Create `src-tauri/src/managers/llm.rs`. Define `LlmModelInfo` (mirror ModelInfo, drop whisper-only fields):
       ```rust
       #[derive(Debug, Clone, Serialize, Deserialize, Type)]
       pub struct LlmModelInfo {
           pub id: String,
           pub name: String,
           pub description: String,
           pub filename: String,
           pub url: Option<String>,        // None for custom models
           pub sha256: Option<String>,     // None for custom (skip verification)
           pub size_mb: u64,               // shown BEFORE download (MDL-01)
           pub is_downloaded: bool,
           pub is_downloading: bool,
           pub partial_size: u64,
           pub is_custom: bool,
           pub is_recommended: bool,
       }
       ```

    4. Add `LlmManager::catalogue() -> Vec<LlmModelInfo>` returning EXACTLY these 3 entries (values copied verbatim from RESEARCH.md — do NOT abbreviate URLs or hashes):
       - id `"qwen2.5-1.5b"`, name `"Qwen2.5 1.5B"`, description `"Fast, lightweight — runs on any machine"`, filename `"qwen2.5-1.5b-instruct-q4_k_m.gguf"`, url `Some("https://huggingface.co/Qwen/Qwen2.5-1.5B-Instruct-GGUF/resolve/main/qwen2.5-1.5b-instruct-q4_k_m.gguf")`, sha256 `Some("6a1a2eb6d15622bf3c96857206351ba97e1af16c30d7a74ee38970e434e9407e")`, size_mb `1120`, is_recommended `true`, is_custom `false`.
       - id `"qwen3-4b"`, name `"Qwen3 4B"`, description `"Best quality for post-processing"`, filename `"Qwen3-4B-Q4_K_M.gguf"`, url `Some("https://huggingface.co/Qwen/Qwen3-4B-GGUF/resolve/main/Qwen3-4B-Q4_K_M.gguf")`, sha256 `Some("7485fe6f11af29433bc51cab58009521f205840f5b4ae3a32fa7f92e8534fdf5")`, size_mb `2500`, is_recommended `false`, is_custom `false`.
       - id `"translategemma-4b"`, name `"TranslateGemma 4B"`, description `"Translation-focused"`, filename `"translategemma-4b-it-Q4_K_M.gguf"`, url `Some("https://huggingface.co/bullerwins/translategemma-4b-it-GGUF/resolve/main/translategemma-4b-it-Q4_K_M.gguf")`, sha256 `Some("7f7357c14abd9da4eb200b38b05da502cd6e10d7e1d403fbc9f78c19f3209b72")`, size_mb `2490`, is_recommended `false`, is_custom `false`.
       For all three: `is_downloaded: false, is_downloading: false, partial_size: 0`.

    5. Add a `#[cfg(test)] mod tests` block at the bottom with the 5 tests from <behavior>. For the settings serde test, deserialize a minimal JSON string (no llm fields) into `AppSettings` (or a small focused struct mirroring the two fields if full AppSettings is hard to construct) and assert the defaults.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib -- managers::llm 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/src/managers/mod.rs` contains `pub mod llm;`
    - `grep -c "serde(default)" src-tauri/src/settings.rs` increased by at least 2 and file contains `pub active_llm_model_id: Option<String>` and `pub llm_unload_timeout: ModelUnloadTimeout`
    - `llm.rs` contains `pub struct LlmModelInfo` and `pub fn catalogue`
    - `llm.rs` contains all 3 SHA256 strings: `6a1a2eb6d15622bf3c96857206351ba97e1af16c30d7a74ee38970e434e9407e`, `7485fe6f11af29433bc51cab58009521f205840f5b4ae3a32fa7f92e8534fdf5`, `7f7357c14abd9da4eb200b38b05da502cd6e10d7e1d403fbc9f78c19f3209b72`
    - `grep -c "blob.handy.computer" src-tauri/src/managers/llm.rs` returns 0
    - `cargo test --lib -- managers::llm` exits 0 with the 5 named tests passing
  </acceptance_criteria>
  <done>AppSettings has both LLM fields with serde(default); catalogue returns 3 HF-CDN models; all 5 unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement download/verify/delete pipeline + GGUF header validation (cloned from ModelManager)</name>
  <read_first>
    - src-tauri/src/managers/model.rs (download_model line 987, DownloadCleanup line 66, verify_sha256 line 941, compute_sha256 line 973, delete_model line 1327, cancel_download line 1442, 10/sec throttle line ~1122)
    - src-tauri/src/managers/llm.rs (current state from Task 1)
  </read_first>
  <behavior>
    - Test `test_gguf_validation_accepts_valid`: write a temp file whose first 4 bytes are `b"GGUF"` followed by garbage; `validate_gguf_header()` returns `Ok`.
    - Test `test_gguf_validation_rejects_non_gguf`: write a temp file starting with `b"ABCD"`; `validate_gguf_header()` returns `Err` containing the string `"GGUF"`.
    - Test `test_gguf_validation_rejects_too_small`: write a 2-byte file; `validate_gguf_header()` returns `Err`.
    - Test `test_compute_sha256_matches_known`: write known bytes, assert computed hash equals the precomputed expected hash.
    - Test `test_delete_removes_file`: create a temp `.gguf` in a temp models_dir, call the delete helper, assert the file no longer exists.
  </behavior>
  <action>
    1. Add the `DownloadCleanup` RAII guard to `llm.rs` (copy from model.rs:66-86, swap `ModelInfo` → `LlmModelInfo`).

    2. Add `LlmManager` struct fields needed for downloads (copy ModelManager's: `app_handle: AppHandle`, `models_dir: PathBuf`, `available_models: Mutex<HashMap<String, LlmModelInfo>>`, `cancel_flags: Arc<Mutex<HashMap<String, Arc<AtomicBool>>>>`). Add `LlmManager::new(app_handle: &AppHandle) -> Result<Self>` that builds `models_dir` via `crate::portable::app_data_dir(app_handle)?.join("models")`, seeds `available_models` from `catalogue()`, marks `is_downloaded`/`partial_size` by scanning the dir for each filename, and discovers custom `.gguf` files (any `.gguf` whose filename is not a catalogue filename) as `is_custom: true` entries.

    3. Add `pub fn validate_gguf_header(path: &Path) -> Result<(), String>` exactly as RESEARCH.md Code Examples (read first 4 bytes, compare to `b"GGUF"`, return `Err("Not a valid GGUF model file".to_string())` on mismatch and `Err("File too small to be a GGUF model".to_string())` on short read).

    4. Copy `verify_sha256` and `compute_sha256` verbatim from model.rs (they are independent of model type).

    5. Add `pub async fn download_llm_model(&self, model_id: &str) -> Result<()>` — clone model.rs `download_model` but SKIP all tar.gz/Archive extraction logic (GGUF is a single flat file). Keep: Range-header resume, `DownloadCleanup`, the 100ms / 10-per-sec progress throttle, and SHA256 verification after completion. Emit events named with the `llm-` prefix: `llm-download-progress` (payload struct `LlmDownloadProgress { model_id, downloaded, total, percentage }`), `llm-verification-started`, `llm-verification-completed`, `llm-download-complete`, `llm-download-cancelled`, `llm-download-failed`.

    6. Add `pub fn delete_llm_model(&self, model_id: &str) -> Result<()>` (file-path branch of model.rs delete_model; remove the single `.gguf` file, update `available_models` state, emit `llm-deleted`) and `pub fn cancel_download(&self, model_id: &str) -> Result<()>` (cancel-flag pattern from model.rs:1442).

    7. Add `pub fn import_custom_gguf(&self, source_path: &Path) -> Result<LlmModelInfo, String>`: call `validate_gguf_header(source_path)`; on Ok copy the file into `self.models_dir.join(filename)`, insert an `is_custom: true` `LlmModelInfo` into `available_models`, emit `llm-custom-model-imported`, and return the info. On validation Err return the error string.

    8. Add the 5 tests from <behavior> to the test module (use `tempfile::TempDir`).
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib -- managers::llm 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `llm.rs` contains `pub fn validate_gguf_header` and the literal `b"GGUF"`
    - `llm.rs` contains `pub async fn download_llm_model`, `pub fn delete_llm_model`, `pub fn cancel_download`, `pub fn import_custom_gguf`
    - `llm.rs` contains all six event names: `llm-download-progress`, `llm-verification-started`, `llm-download-complete`, `llm-download-cancelled`, `llm-deleted`, `llm-custom-model-imported`
    - `llm.rs` contains NO `tar::Archive` / `GzDecoder` usage (GGUF is single-file): `grep -c "GzDecoder\|tar::Archive" src-tauri/src/managers/llm.rs` returns 0
    - `cargo test --lib -- managers::llm` exits 0 with the 5 new tests passing (10 total with Task 1)
  </acceptance_criteria>
  <done>Download (resume + SHA256 + throttled progress, no extraction), delete, cancel, custom GGUF import with header validation all implemented; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement model load/inference/unload + independent idle watcher (LLM-01/02/03)</name>
  <read_first>
    - src-tauri/src/managers/transcription.rs (idle watcher lines 88-160, touch_activity, unload_model, shutdown_signal, try_start_loading/LoadingGuard pattern)
    - src-tauri/src/managers/llm.rs (current state from Tasks 1-2)
    - .planning/phases/11-llm-runtime-foundation/11-RESEARCH.md (Patterns 2-4, Code Examples for inference; Pitfall 4 LlamaContext lifetime, Pitfall 5 double-load)
  </read_first>
  <behavior>
    - Test `test_model_params_gpu_layers`: a helper `gpu_model_params()` (or equivalent) constructs `LlamaModelParams` with `n_gpu_layers == i32::MAX`; assert via the helper's documented return or a wrapper bool that GPU offload is requested. (If the param is not introspectable, assert the code path compiles by calling the helper.)
    - Test `test_touch_activity_updates_last_activity`: after `touch_activity()`, `last_activity` atomic is >= the value captured before the call.
    - Test `test_idle_watcher_unload_logic`: a pure helper `should_unload(idle_ms, limit_seconds) -> bool` returns true when `idle_ms > limit_seconds*1000` and false otherwise; assert both cases.
  </behavior>
  <action>
    1. Add `llama-cpp-2` imports and the runtime fields to `LlmManager`: `backend: Arc<LlamaBackend>` (init once in `new()` via `LlamaBackend::init()`), `loaded_model: Arc<Mutex<Option<LoadedLlmModel>>>`, `active_model_id: Arc<Mutex<Option<String>>>`, `last_activity: Arc<AtomicU64>`, `shutdown_signal: Arc<AtomicBool>`, `watcher_handle: Arc<Mutex<Option<thread::JoinHandle<()>>>>`, `is_loading: Arc<Mutex<bool>>`, `loading_condvar: Arc<Condvar>`. Define `struct LoadedLlmModel { model: Arc<LlamaModel>, id: String }`.

    2. Implement `gpu_model_params()` helper returning `LlamaModelParams::default().with_n_gpu_layers(i32::MAX)` (GPU if available, graceful CPU fallback per RESEARCH Pattern 2). Do NOT add per-platform detection beyond the existing Cargo feature flags.

    3. Implement `pub async fn load_model(&self, model_id: &str) -> Result<()>`: use a `try_start_loading()` guard (copy the `Arc<Mutex<bool>>` + `Condvar` LoadingGuard pattern from transcription.rs to prevent double-load, RESEARCH Pitfall 5); resolve the file path in `models_dir`; `LlamaModel::load_from_file(&self.backend, &path, &gpu_model_params())` inside `spawn_blocking`; store `Arc<LlamaModel>` in `loaded_model`; set `active_model_id`; emit `llm-model-loaded`.

    4. Implement `pub fn unload_model(&self) -> Result<()>`: drop the loaded model (`*loaded_model.lock() = None`), emit `llm-model-unloaded`. Add `pub fn is_model_loaded(&self) -> bool` and `touch_activity()` (`last_activity.store(now_ms, Ordering::Relaxed)`), and a `now_ms()` helper (copy from transcription.rs).

    5. Implement `pub async fn run_inference(&self, prompt: String) -> Result<String>` per RESEARCH Code Examples: clone the `Arc<LlamaModel>` from `loaded_model` (Err if none), run the tokenize→decode→greedy-sample→detokenize loop inside `tokio::task::spawn_blocking`, creating a fresh `LlamaContext` per call (`with_n_ctx(NonZeroU32::new(2048))`, `with_n_threads(num_cpus::get() as u32 / 2)`, `max_tokens` default 512). Call `self.touch_activity()` after a successful run. Return the generated `String`.

    6. Implement the independent idle watcher: in `new()` spawn a watcher thread mirroring transcription.rs:88-160 but reading `get_settings(&app_handle).llm_unload_timeout` (NOT `model_unload_timeout`) and calling `self.unload_model()`. Use the manager's OWN `shutdown_signal`. Extract the unload decision into a pure helper `fn should_unload(idle_ms: u64, limit_seconds: u64) -> bool { idle_ms > limit_seconds * 1000 }` so it is unit-testable. Add `pub fn shutdown(&self)` that sets `shutdown_signal` to true.

    7. Add the 3 tests from <behavior>.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test --lib -- managers::llm 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `llm.rs` contains `with_n_gpu_layers(i32::MAX)`
    - `llm.rs` contains `pub async fn load_model`, `pub fn unload_model`, `pub async fn run_inference`, `pub fn shutdown`, `pub fn touch_activity`
    - `llm.rs` contains `spawn_blocking` (inference runs on a blocking thread — LLM-01)
    - `llm.rs` contains `llm_unload_timeout` (watcher reads the LLM-specific timeout, independent of transcription — LLM-03)
    - `llm.rs` contains `fn should_unload(` and `LlamaBackend::init`
    - `cargo test --lib -- managers::llm` exits 0; `cargo build --lib` exits 0 (llama-cpp-2 links)
  </acceptance_criteria>
  <done>LlmManager loads a GGUF with GPU params + CPU fallback, runs inference on a blocking thread, and unloads via its own idle watcher; backend compiles and all unit tests pass.</done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo test --lib -- managers::llm` exits 0 (all unit tests across the 3 tasks)
- `cd src-tauri && cargo build --lib` exits 0 (llama-cpp-2 + transcribe-rs coexist — proven in Phase 10)
- `cd src-tauri && cargo clippy --lib 2>&1 | grep -c "^error"` returns 0
</verification>

<success_criteria>
- LlmManager defines the 3-model HF-CDN catalogue with sizes available pre-download (MDL-01)
- Download with resume + SHA256 + throttled progress + cancel; delete; custom GGUF import with header validation (MDL-02/03/04)
- load/run_inference (background thread) / unload with independent idle watcher (LLM-01/02/03)
- AppSettings has `active_llm_model_id` and `llm_unload_timeout`, both serde-default backward-compatible
- All unit tests green; backend compiles
</success_criteria>

<output>
After completion, create `.planning/phases/11-llm-runtime-foundation/11-01-SUMMARY.md`
</output>
