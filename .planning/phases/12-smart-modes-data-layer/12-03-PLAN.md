---
phase: 12-smart-modes-data-layer
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src-tauri/src/transcription_coordinator.rs
  - src-tauri/src/actions.rs
  - src-tauri/src/shortcut/tauri_impl.rs
  - src-tauri/src/shortcut/handy_keys.rs
  - src-tauri/src/commands/history.rs
autonomous: true
requirements: [MODE-04]
must_haves:
  truths:
    - "is_transcribe_binding returns true for any smart_mode_ prefixed id"
    - "A smart_mode_{id} shortcut routes through the transcription pipeline and applies that mode's prompt"
    - "Rewrite-kind modes run the existing prompt path; Translation-kind modes route but return a stubbed None (engine wired Phase 13)"
    - "Bound smart modes are registered at app init in both tauri_impl and handy_keys init paths"
    - "Existing transcribe / transcribe_with_post_process behavior is unchanged (mode_id_override = None)"
  artifacts:
    - path: "src-tauri/src/transcription_coordinator.rs"
      provides: "is_transcribe_binding smart_mode_ prefix + start/stop dispatch for smart_mode_ ids"
      contains: "starts_with(\"smart_mode_\")"
    - path: "src-tauri/src/actions.rs"
      provides: "process_transcription_output mode_id_override param + SmartModeAction + kind branch"
      contains: "mode_id_override"
  key_links:
    - from: "transcription_coordinator start/stop"
      to: "SmartModeAction"
      via: "smart_mode_ prefix branch builds action from binding_id"
      pattern: "SmartModeAction"
    - from: "SmartModeAction"
      to: "process_transcription_output"
      via: "passes mode_id override; branches on SmartModeKind"
      pattern: "mode_id_override"
---

<objective>
Wire `smart_mode_{id}` shortcuts end-to-end through the routing pipeline: extend `is_transcribe_binding` to match the prefix, dispatch `smart_mode_` ids to a new `SmartModeAction` in the coordinator's `start`/`stop`, give `process_transcription_output` a per-mode override that branches on `SmartModeKind` (Rewrite path live, Translation path stubbed for Phase 13), and register bound smart modes at init in both keyboard implementations.

Purpose: Completes the routing half of MODE-04 — triggering a mode's shortcut applies that mode's prompt. Translation modes route correctly but their engine execution is deferred to Phase 13 (per CONTEXT.md phase boundary).
Output: Updated coordinator, actions, both init loops, and the history-replay call site. `cargo test` green on the routing unit tests.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-smart-modes-data-layer/12-CONTEXT.md
@.planning/phases/12-smart-modes-data-layer/12-RESEARCH.md
@.planning/phases/12-smart-modes-data-layer/12-VALIDATION.md

<interfaces>
<!-- Types this plan CONSUMES from Plan 12-01 (settings.rs). -->
SmartMode { id, name, kind: SmartModeKind, prompt, target_language }
SmartModeKind::Rewrite  -> existing active-model prompt path
SmartModeKind::Translation -> stubbed (return None, log) for Phase 13
AppSettings.smart_modes: Vec<SmartMode>
AppSettings.smart_mode_active_id: Option<String>

<!-- Refactored signature this plan CREATES. -->
process_transcription_output(app, transcription, post_process: bool, mode_id_override: Option<&str>) -> ProcessedTranscription
</interfaces>

<existing_code>
- transcription_coordinator.rs:40-42 — `pub fn is_transcribe_binding(id: &str) -> bool { id == "transcribe" || id == "transcribe_with_post_process" }`
- transcription_coordinator.rs:161-175 — `fn start(app, stage, binding_id, hotkey_string)`: does `ACTION_MAP.get(binding_id)` then `action.start(...)`; warns + returns if absent
- transcription_coordinator.rs:177-184 — `fn stop(...)`: same `ACTION_MAP.get` + `action.stop(...)`
- actions.rs:41-50 — `ShortcutAction` trait (`start`/`stop`) + `struct TranscribeAction { post_process: bool }`
- actions.rs:66-70 — `async fn post_process_transcription(app, settings, transcription) -> Option<String>` (the active-mode prompt path; currently reads `post_process_selected_prompt_id` + `post_process_prompts`)
- actions.rs:421-465 — `ProcessedTranscription { final_text, post_processed_text, post_process_prompt }` + `process_transcription_output(app, transcription, post_process) -> ProcessedTranscription`; post_process branch at 441-455 reads selected prompt
- actions.rs:660-665 — call site in `TranscribeAction::stop` async task: `process_transcription_output(&ah, &transcription, post_process).await`
- actions.rs:778-799 — `ACTION_MAP` Lazy<HashMap> static (cannot hold dynamic smart_mode ids)
- commands/history.rs:1, 97 — `use crate::actions::process_transcription_output;` and call `process_transcription_output(&app, &transcription, entry.post_process_requested).await`
- shortcut/tauri_impl.rs:17-40 — `init_shortcuts`: iterates `default_bindings` only; skips cancel + disabled post-process
- shortcut/handy_keys.rs:425-457 — `init_shortcuts`: iterates `default_bindings` only; same skip logic
</existing_code>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend is_transcribe_binding and refactor process_transcription_output</name>
  <files>src-tauri/src/transcription_coordinator.rs, src-tauri/src/actions.rs, src-tauri/src/commands/history.rs</files>
  <read_first>
    - src-tauri/src/transcription_coordinator.rs:40-42 (is_transcribe_binding current body)
    - src-tauri/src/actions.rs:421-465 (ProcessedTranscription + process_transcription_output)
    - src-tauri/src/actions.rs:660-665 (TranscribeAction::stop call site)
    - src-tauri/src/commands/history.rs:90-100 (history replay call site)
    - src-tauri/src/settings.rs (SmartMode/SmartModeKind from Plan 12-01)
  </read_first>
  <behavior>
    - `is_transcribe_binding("smart_mode_abc")` returns `true`; `is_transcribe_binding("transcribe")` and `("transcribe_with_post_process")` still return `true`; `is_transcribe_binding("cancel")` returns `false`.
    - `process_transcription_output(.., mode_id_override = Some("mode_x"))` for a Rewrite mode uses mode_x's prompt (not the active-mode prompt).
    - `process_transcription_output(.., mode_id_override = Some(translation_mode_id))` for a Translation mode returns a `ProcessedTranscription` with `post_processed_text == None` (stub path, no panic).
    - All existing callers pass `None` → behavior identical to before.
  </behavior>
  <action>
    1. In `src-tauri/src/transcription_coordinator.rs:40-42`, change `is_transcribe_binding` to:
       `pub fn is_transcribe_binding(id: &str) -> bool { id == "transcribe" || id == "transcribe_with_post_process" || id.starts_with("smart_mode_") }`

    2. In `src-tauri/src/actions.rs`, change the signature of `process_transcription_output` (line 427) to add a fourth param:
       `pub(crate) async fn process_transcription_output(app: &AppHandle, transcription: &str, post_process: bool, mode_id_override: Option<&str>) -> ProcessedTranscription`.
       Inside, when `post_process` is true: if `mode_id_override` is `Some(id)`, look up the mode in `settings.smart_modes.iter().find(|m| m.id == id)`. Branch on `mode.kind`:
         - `SmartModeKind::Rewrite` → call a new helper `post_process_with_prompt(app, &settings, &final_text, &mode.prompt).await` (factor the existing embedded/HTTP prompt-execution body of `post_process_transcription` so it can take an explicit prompt string instead of resolving the active selected prompt; see step 3). Set `post_processed_text`/`post_process_prompt` from the result + `mode.prompt`.
         - `SmartModeKind::Translation` → `log::warn!("Translation Smart Mode '{}' triggered but the translation engine is not yet wired (Phase 13)", id);` leave `post_processed_text = None` and `post_process_prompt = None` (stub). Do not call inference.
       When `mode_id_override` is `None`, keep the EXACT existing behavior (call `post_process_transcription(app, &settings, &final_text)` and read the active selected prompt for `post_process_prompt`, as today at actions.rs:441-455).

    3. Refactor `post_process_transcription` (actions.rs:66) so its prompt-execution body is reusable with an explicit prompt. Extract `async fn post_process_with_prompt(app: &AppHandle, settings: &AppSettings, transcription: &str, prompt: &str) -> Option<String>` containing everything from "resolve provider/embedded → build_system_prompt(prompt) → run inference → strip + return" — i.e. the current body MINUS the selected-prompt resolution (lines ~78-98 embedded branch and ~165-186 HTTP branch that look up `post_process_selected_prompt_id`). Then `post_process_transcription` becomes: resolve the active selected prompt text from settings, then delegate to `post_process_with_prompt(app, settings, transcription, &resolved_prompt)`. This keeps existing behavior identical while exposing the explicit-prompt path for `mode_id_override`.

    4. Update the two existing call sites to pass `None`:
       - `src-tauri/src/actions.rs:660-665`: `process_transcription_output(&ah, &transcription, post_process, None).await`
       - `src-tauri/src/commands/history.rs:97`: `process_transcription_output(&app, &transcription, entry.post_process_requested, None).await`

    5. Add unit tests in `actions.rs` `#[cfg(test)]`:
       - `mode_routing_rewrite_uses_correct_prompt`: build an `AppSettings` (via `get_default_settings()`) with a known Rewrite mode, assert that the resolution path selects that mode's prompt (test the selection logic — e.g. a small pure helper `fn resolve_mode_prompt(settings: &AppSettings, mode_id: &str) -> Option<(&SmartMode, String)>` factored out and unit-tested) rather than running inference (inference needs a model). Assert it returns the override mode's prompt, not the active mode's.
       - `mode_routing_translation_returns_none`: assert that for a Translation-kind mode, `resolve_mode_prompt` signals the translation/stub branch (returns `None` or a `Translation` marker) so `process_transcription_output` will not run inference.
    Factor whatever minimal pure helper makes these testable without a Tauri runtime or a loaded model.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test actions:: 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "starts_with(\"smart_mode_\")" src-tauri/src/transcription_coordinator.rs` succeeds
    - `grep -q "mode_id_override: Option<&str>" src-tauri/src/actions.rs` succeeds
    - `grep -q "post_process_with_prompt" src-tauri/src/actions.rs` succeeds
    - `grep -c "process_transcription_output(.*None)" src-tauri/src/actions.rs src-tauri/src/commands/history.rs` returns at least 2 (both legacy callers pass None)
    - `grep -q "not yet wired (Phase 13)" src-tauri/src/actions.rs` succeeds (translation stub)
    - Tests `mode_routing_rewrite_uses_correct_prompt` and `mode_routing_translation_returns_none` pass
    - `cd src-tauri && cargo build` exits 0
  </acceptance_criteria>
  <done>is_transcribe_binding matches the prefix; process_transcription_output takes a mode override and branches on kind; legacy callers pass None; routing helpers unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SmartModeAction + coordinator start/stop dispatch</name>
  <files>src-tauri/src/actions.rs, src-tauri/src/transcription_coordinator.rs</files>
  <read_first>
    - src-tauri/src/transcription_coordinator.rs:161-184 (start/stop ACTION_MAP lookup)
    - src-tauri/src/actions.rs:41-50 (ShortcutAction trait + TranscribeAction)
    - src-tauri/src/actions.rs:467-575 (TranscribeAction::start / ::stop — recording + process_transcription_output flow to mirror)
  </read_first>
  <behavior>
    - A `binding_id` of form `smart_mode_{id}` produces a `SmartModeAction { mode_id: "{id}" }` whose `start`/`stop` drive the same record→transcribe flow as `TranscribeAction` with `post_process = true`, passing `mode_id_override = Some(mode_id)` into `process_transcription_output`.
    - Non-prefixed binding ids still resolve via `ACTION_MAP` (unchanged).
    - `is_transcribe_binding_smart_mode_prefix` unit test (in coordinator) confirms prefix routing recognition.
  </behavior>
  <action>
    1. In `src-tauri/src/actions.rs`, add `pub(crate) struct SmartModeAction { pub mode_id: String }` and `impl ShortcutAction for SmartModeAction`. Implement `start` IDENTICALLY to `TranscribeAction::start` (recording start, overlays, audio feedback — it does not depend on post_process). Implement `stop` by mirroring `TranscribeAction::stop` (actions.rs:570-end) but: always treat `post_process = true`, and replace the `process_transcription_output(&ah, &transcription, post_process, None)` call with `process_transcription_output(&ah, &transcription, true, Some(&mode_id)).await` where `mode_id = self.mode_id.clone()` is moved into the async task. To avoid duplicating the large `stop` body, factor the shared async transcription flow of `TranscribeAction::stop` into a free helper `fn spawn_transcription_task(app: &AppHandle, binding_id: String, post_process: bool, mode_id_override: Option<String>)` and have both `TranscribeAction::stop` and `SmartModeAction::stop` call it (TranscribeAction passes `None`, SmartModeAction passes `Some(self.mode_id.clone())`). Keep `start` shared too if practical, otherwise duplicate the small `start` body.

    2. In `src-tauri/src/transcription_coordinator.rs`, change `start` (161-166) and `stop` (177-182) so that for `smart_mode_`-prefixed ids they build the action dynamically instead of `ACTION_MAP.get`:
       ```
       let action: Arc<dyn ShortcutAction> = if binding_id.starts_with("smart_mode_") {
           let mode_id = binding_id.strip_prefix("smart_mode_").unwrap().to_string();
           Arc::new(crate::actions::SmartModeAction { mode_id })
       } else {
           let Some(a) = ACTION_MAP.get(binding_id) else {
               warn!("No action in ACTION_MAP for '{binding_id}'");
               return;
           };
           Arc::clone(a)
       };
       action.start(app, binding_id, hotkey_string); // (stop() calls action.stop)
       ```
       Apply the analogous change to `stop`.

    3. Add `#[cfg(test)]` test `is_transcribe_binding_smart_mode_prefix` in `transcription_coordinator.rs` asserting `is_transcribe_binding("smart_mode_mode_abc")` is true and `is_transcribe_binding("cancel")` is false.
  </action>
  <verify>
    <automated>cd src-tauri && cargo test transcription_coordinator:: 2>&1 | tail -15 && cargo build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "struct SmartModeAction" src-tauri/src/actions.rs` succeeds
    - `grep -q "impl ShortcutAction for SmartModeAction" src-tauri/src/actions.rs` succeeds
    - `grep -q "SmartModeAction { mode_id }" src-tauri/src/transcription_coordinator.rs` succeeds
    - `grep -q "Some(&mode_id)\|Some(self.mode_id" src-tauri/src/actions.rs` succeeds (override threaded through)
    - Test `is_transcribe_binding_smart_mode_prefix` passes
    - `cd src-tauri && cargo build` exits 0
  </acceptance_criteria>
  <done>SmartModeAction drives the record→transcribe→mode-prompt flow; coordinator dispatches smart_mode_ ids to it; prefix test green.</done>
</task>

<task type="auto">
  <name>Task 3: Register bound smart modes at init (both keyboard impls)</name>
  <files>src-tauri/src/shortcut/tauri_impl.rs, src-tauri/src/shortcut/handy_keys.rs</files>
  <read_first>
    - src-tauri/src/shortcut/tauri_impl.rs:17-40 (init_shortcuts default-bindings loop + register_shortcut)
    - src-tauri/src/shortcut/handy_keys.rs:425-457 (init_shortcuts default-bindings loop + state.register)
    - src-tauri/src/settings.rs (smart_modes field; ShortcutBinding shape)
  </read_first>
  <action>
    Both `init_shortcuts` implementations currently iterate only `default_bindings`, so `smart_mode_{id}` entries (which live in `user_settings.bindings`, not in defaults) are never registered. Add a second loop in BOTH files, after the existing default-bindings loop, that registers bound smart modes:

    1. `src-tauri/src/shortcut/tauri_impl.rs` (after the loop ending at line 39, before the closing brace of `init_shortcuts`):
       ```
       for mode in &user_settings.smart_modes {
           let binding_key = format!("smart_mode_{}", mode.id);
           if let Some(binding) = user_settings.bindings.get(&binding_key) {
               if !binding.current_binding.trim().is_empty() {
                   if let Err(e) = register_shortcut(app, binding.clone()) {
                       error!("Failed to register smart mode shortcut {} during init: {}", binding_key, e);
                   }
               }
           }
       }
       ```
       (`user_settings` and `register_shortcut` are already in scope in this function.)

    2. `src-tauri/src/shortcut/handy_keys.rs` (after the default-bindings loop, before `app.manage(state)` at ~452): same loop, but register via `state.register(&binding)` instead of `register_shortcut(app, ...)`, matching the existing handy_keys pattern:
       ```
       for mode in &user_settings.smart_modes {
           let binding_key = format!("smart_mode_{}", mode.id);
           if let Some(binding) = user_settings.bindings.get(&binding_key) {
               if !binding.current_binding.trim().is_empty() {
                   if let Err(e) = state.register(binding) {
                       error!("Failed to register handy-keys smart mode shortcut {} during init: {}", binding_key, e);
                   }
               }
           }
       }
       ```
       Verify `state.register` takes `&ShortcutBinding` (it does, per handy_keys.rs:447 usage `state.register(&binding)`); pass `binding` (already a `&ShortcutBinding` from `.get()`).

    Only modes whose binding has a non-empty `current_binding` are registered — defaults ship unbound, so this loop registers only the migrated active mode (which inherited the transferred combo) and any user-bound modes. No surprise global shortcuts.
  </action>
  <verify>
    <automated>cd src-tauri && cargo build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "for mode in &user_settings.smart_modes" src-tauri/src/shortcut/tauri_impl.rs` succeeds
    - `grep -q "for mode in &user_settings.smart_modes" src-tauri/src/shortcut/handy_keys.rs` succeeds
    - `grep -q "smart mode shortcut" src-tauri/src/shortcut/tauri_impl.rs` succeeds
    - tauri_impl loop uses `register_shortcut(app, binding.clone())`; handy_keys loop uses `state.register(binding)` (grep both)
    - `cd src-tauri && cargo build` exits 0
  </acceptance_criteria>
  <done>Both init paths register bound smart modes from user_settings.bindings; unbound defaults are skipped.</done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo test actions:: transcription_coordinator::` — routing + prefix tests green
- `cd src-tauri && cargo build` exits 0 (both init loops + SmartModeAction compile)
- `cd src-tauri && cargo clippy --all-targets -- -D warnings` clean on new code
- Manual-Only (per VALIDATION.md): live shortcut firing requires a running app + real keypress — bind a mode, press the combo, confirm transcription routes through that mode's prompt
</verification>

<success_criteria>
- `is_transcribe_binding` recognizes the `smart_mode_` prefix
- `SmartModeAction` exists and drives the record→transcribe→mode-prompt flow; coordinator dispatches prefixed ids to it
- `process_transcription_output` accepts `mode_id_override` and branches on `SmartModeKind` (Rewrite live, Translation stubbed for Phase 13); legacy callers pass `None`
- Both `init_shortcuts` impls register bound smart modes; unbound defaults skipped
- MODE-04 routing half satisfied; Translation execution correctly deferred to Phase 13
</success_criteria>

<output>
After completion, create `.planning/phases/12-smart-modes-data-layer/12-03-SUMMARY.md`
</output>
