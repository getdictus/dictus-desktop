---
phase: 05-upstream-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/upstream-sync.yml
  - .github/upstream-sha.txt
  - .planning/phases/05-upstream-sync/scripts/verify-sync.sh
autonomous: false
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-05
must_haves:
  truths:
    - "Weekly cron at Monday 08:00 UTC fires .github/workflows/upstream-sync.yml"
    - "Workflow exits early when upstream-sha.txt equals upstream/main HEAD (idempotent)"
    - "Workflow creates one GitHub issue labeled upstream-sync with commit list when SHA differs"
    - "verify-sync.sh fails with a non-zero exit when Dictus identity regresses in the tree"
    - "upstream-sha.txt is seeded with 39e855d so the first run reports the 4 known commits"
    - "upstream-sync label exists in the GitHub repo before the first workflow run"
  artifacts:
    - path: .github/workflows/upstream-sync.yml
      provides: "Weekly cron detection action using actions/checkout@v4 + actions/github-script@v7"
      contains: "cron: '0 8 * * 1'"
    - path: .github/upstream-sha.txt
      provides: "Idempotency state file — stored upstream SHA"
      contains: "39e855d"
    - path: .planning/phases/05-upstream-sync/scripts/verify-sync.sh
      provides: "SYNC-05 post-merge identity + build assertions"
      contains: "productName.*Dictus"
  key_links:
    - from: .github/workflows/upstream-sync.yml
      to: .github/upstream-sha.txt
      via: "cat .github/upstream-sha.txt in compare step"
      pattern: "cat .github/upstream-sha.txt"
    - from: .github/workflows/upstream-sync.yml
      to: "github.rest.issues.create"
      via: "actions/github-script@v7 call with labels: ['upstream-sync']"
      pattern: "labels: \\['upstream-sync'\\]"
    - from: .planning/phases/05-upstream-sync/scripts/verify-sync.sh
      to: src-tauri/tauri.conf.json
      via: "grep / jq identity assertions"
      pattern: "productName.*Dictus"
---

<objective>
Build the detection infrastructure for upstream sync: a weekly GitHub Actions workflow that reads `.github/upstream-sha.txt`, fetches `upstream/main`, and opens exactly one `upstream-sync`-labeled issue when the SHA differs. Also seed the idempotency file with the merge-base `39e855d` so the first run reports the 4 known post-v0.8.2 commits, and create the `verify-sync.sh` Wave 0 validator that Plan 03 will use for SYNC-05 post-merge checks. Finally, gate on a manual checkpoint that the `upstream-sync` label exists in the repo (required — `actions/github-script` throws 422 without it).

Purpose: SYNC-01 (detection), SYNC-02 (idempotency), and the verification substrate for SYNC-05. No code merge happens here — detection and tooling only.
Output: Workflow YAML + SHA seed + verify script + confirmed label.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-upstream-sync/05-CONTEXT.md
@.planning/phases/05-upstream-sync/05-RESEARCH.md
@.planning/phases/05-upstream-sync/05-VALIDATION.md
@.github/workflows/release.yml
@.planning/phases/04-updater-infrastructure/scripts/validate.sh

<interfaces>
<!-- Key patterns the executor must replicate. Extracted from codebase. -->

Pattern — actions/github-script@v7 usage (from .github/workflows/release.yml):
```yaml
- uses: actions/github-script@v7
  with:
    github-token: ${{ secrets.GITHUB_TOKEN }}
    script: |
      await github.rest.{resource}.{method}({...})
```

Pattern — check()/FAIL helper (from .planning/phases/04-updater-infrastructure/scripts/validate.sh):
```bash
#!/usr/bin/env bash
set -euo pipefail
FAIL=0
check() {
  local label="$1"; shift
  if eval "$@" >/dev/null 2>&1; then
    echo "  ok  $label"
  else
    echo "  FAIL $label"
    echo "       cmd: $*"
    FAIL=1
  fi
}
# ...checks...
[ $FAIL -eq 0 ] && echo "All checks passed." || exit 1
```

Known SHAs (from RESEARCH.md, verified via `git log 39e855d..upstream/main --stat`):
- Fork point: `85a8ed77`
- Merge-base / v0.8.2: `39e855d` (seed value for upstream-sha.txt)
- Post-v0.8.2 upstream commits (4): `c1697b2`, `84d88f9`, `30b57c4`, `fdc8cb7`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create upstream-sha.txt seed and verify-sync.sh validator (Wave 0 artifacts)</name>
  <files>.github/upstream-sha.txt, .planning/phases/05-upstream-sync/scripts/verify-sync.sh</files>
  <read_first>
    - .planning/phases/04-updater-infrastructure/scripts/validate.sh (replicate the check()/FAIL helper exactly)
    - .planning/phases/05-upstream-sync/05-RESEARCH.md §"Post-Merge Identity Verification Script" and §"Wave 0 Gaps" (exact assertions required)
    - src-tauri/tauri.conf.json (confirm current productName, identifier, version, updater endpoint values)
    - src-tauri/src/llm_client.rs (confirm current `Dictus/1.0` User-Agent and `X-Title` values before asserting on them)
    - .planning/phases/05-upstream-sync/05-VALIDATION.md §"Wave 0 Requirements"
  </read_first>
  <action>
    Create `.github/upstream-sha.txt` containing exactly the merge-base SHA `39e855d` followed by a single trailing newline. Nothing else — no comments, no prefix. This seeds the detection action so its first run reports the 4 known post-v0.8.2 commits as "new."

    Then create `.planning/phases/05-upstream-sync/scripts/verify-sync.sh` with executable permission (`chmod +x`). Start with this exact header:

    ```bash
    #!/usr/bin/env bash
    # Phase 5 validator — SYNC-05 post-merge identity + build assertions.
    # Run after merging upstream/main to verify Dictus identity has not regressed.
    set -euo pipefail

    FAIL=0
    check() {
      local label="$1"; shift
      if eval "$@" >/dev/null 2>&1; then
        echo "  ok  $label"
      else
        echo "  FAIL $label"
        echo "       cmd: $*"
        FAIL=1
      fi
    }

    command -v jq >/dev/null 2>&1 || { echo "jq required — brew install jq"; exit 2; }

    echo "Phase 5 — Upstream Sync identity + build assertions"
    echo
    ```

    Then add these checks verbatim (each via the `check()` helper):

    1. `SYNC-05a productName is Dictus` → `grep -q '"productName": "Dictus"' src-tauri/tauri.conf.json`
    2. `SYNC-05b identifier is com.dictus.desktop` → `grep -q '"identifier": "com.dictus.desktop"' src-tauri/tauri.conf.json`
    3. `SYNC-05c updater endpoint is getdictus/dictus-desktop` → `jq -e '.plugins.updater.endpoints[0] | test(\"getdictus/dictus-desktop\")' src-tauri/tauri.conf.json`
    4. `SYNC-05d no Handy in en i18n (except attribution)` → `! grep -E '"[^"]*":[[:space:]]*"[^"]*Handy[^"]*"' src/i18n/locales/en/translation.json | grep -v -i 'based on\|attribution\|credit'` — simpler form: `! grep '"Handy"' src/i18n/locales/en/translation.json`
    5. `SYNC-05e llm_client User-Agent is Dictus/1.0` → `grep -q 'Dictus/1.0' src-tauri/src/llm_client.rs`
    6. `SYNC-05f llm_client X-Title is Dictus` → `grep -q '"X-Title".*"Dictus"' src-tauri/src/llm_client.rs` (fallback: `grep -q '"Dictus"' src-tauri/src/llm_client.rs`)
    7. `SYNC-05g no handy.computer reference in actions.rs` → `! grep 'handy\\.computer' src-tauri/src/actions.rs`
    8. `SYNC-05h no handy.computer reference in llm_client.rs` → `! grep 'handy\\.computer' src-tauri/src/llm_client.rs`
    9. `SYNC-05i no handy.log comment in actions.rs (Pitfall 6)` → `! grep -i 'handy\\.log' src-tauri/src/actions.rs`
    10. `SYNC-05j cargo build succeeds` → `cargo build --manifest-path src-tauri/Cargo.toml 2>&1`

    Finish with:
    ```bash
    echo
    if [ $FAIL -eq 0 ]; then
      echo "All Phase 5 checks passed."
      exit 0
    else
      echo "One or more Phase 5 checks FAILED."
      exit 1
    fi
    ```

    IMPORTANT: On the current (pre-merge) codebase, checks 1–9 should all pass; check 10 (cargo build) may be slow but should succeed. This script is intentionally designed to be green NOW and stay green AFTER the merge. Plan 03 uses it to gate merge resolution.

    Run `chmod +x .planning/phases/05-upstream-sync/scripts/verify-sync.sh` after writing.
  </action>
  <verify>
    <automated>test -f .github/upstream-sha.txt && grep -q '^39e855d' .github/upstream-sha.txt && test -x .planning/phases/05-upstream-sync/scripts/verify-sync.sh && bash .planning/phases/05-upstream-sync/scripts/verify-sync.sh</automated>
  </verify>
  <acceptance_criteria>
    - `.github/upstream-sha.txt` exists and first line starts with `39e855d` (verify: `head -c7 .github/upstream-sha.txt` outputs `39e855d`)
    - `.github/upstream-sha.txt` file size is between 7 and 42 bytes (SHA + newline; not accidentally containing prose)
    - `.planning/phases/05-upstream-sync/scripts/verify-sync.sh` exists and is executable (`test -x` returns 0)
    - Script contains literal string `check "SYNC-05a productName is Dictus"` (grep confirms)
    - Script contains literal string `check "SYNC-05i no handy.log comment in actions.rs (Pitfall 6)"` (grep confirms Pitfall 6 codified)
    - Script contains literal string `Dictus/1.0` (User-Agent assertion present)
    - Running the script on current codebase exits 0 with `All Phase 5 checks passed.` in output
    - Script starts with `#!/usr/bin/env bash` and has `set -euo pipefail`
  </acceptance_criteria>
  <done>
    Wave 0 artifacts in place: upstream-sha.txt seeded, verify-sync.sh passes on current tree, both committed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create upstream-sync.yml detection workflow (SYNC-01, SYNC-02)</name>
  <files>.github/workflows/upstream-sync.yml</files>
  <read_first>
    - .github/workflows/release.yml (reuse the actions/github-script@v7 + permissions pattern)
    - .planning/phases/05-upstream-sync/05-RESEARCH.md §"Pattern 1: SHA-Based Idempotent Detection" (the workflow skeleton to follow)
    - .planning/phases/05-upstream-sync/05-CONTEXT.md §"Detection action design" (locked decisions)
    - .github/upstream-sha.txt (the file the workflow reads; must exist from Task 1)
  </read_first>
  <action>
    Create `.github/workflows/upstream-sync.yml` implementing the locked design. Use this exact top section:

    ```yaml
    name: Upstream Sync Check

    on:
      schedule:
        - cron: '0 8 * * 1'   # Monday 08:00 UTC
      workflow_dispatch:

    permissions:
      contents: read
      issues: write

    jobs:
      check-upstream:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
            with:
              fetch-depth: 0

          - name: Fetch upstream
            run: |
              if ! git remote get-url upstream >/dev/null 2>&1; then
                git remote add upstream https://github.com/cjpais/Handy.git
              fi
              git fetch upstream main --no-tags

          - name: Compare upstream SHA
            id: compare
            run: |
              STORED_SHA=$(cat .github/upstream-sha.txt 2>/dev/null | tr -d '[:space:]' || echo "")
              CURRENT_SHA=$(git rev-parse upstream/main)
              echo "stored=${STORED_SHA}" >> $GITHUB_OUTPUT
              echo "current=${CURRENT_SHA}" >> $GITHUB_OUTPUT
              if [ "$STORED_SHA" = "$CURRENT_SHA" ]; then
                echo "changed=false" >> $GITHUB_OUTPUT
                echo "No upstream changes since last sync (${CURRENT_SHA:0:8}). Exiting."
              else
                echo "changed=true" >> $GITHUB_OUTPUT
                echo "Upstream changed: stored=${STORED_SHA:0:8} current=${CURRENT_SHA:0:8}"
              fi

          - name: Build commit list
            id: commits
            if: steps.compare.outputs.changed == 'true'
            run: |
              STORED="${{ steps.compare.outputs.stored }}"
              CURRENT="${{ steps.compare.outputs.current }}"
              if [ -z "$STORED" ]; then
                STORED="85a8ed77"
              fi
              COUNT=$(git log ${STORED}..upstream/main --oneline | wc -l | tr -d ' ')
              COMMIT_LIST=$(git log ${STORED}..upstream/main --oneline | head -20)
              echo "count=${COUNT}" >> $GITHUB_OUTPUT
              {
                echo "commit_list<<EOF"
                echo "$COMMIT_LIST"
                echo "EOF"
              } >> $GITHUB_OUTPUT

          - name: Create tracking issue
            if: steps.compare.outputs.changed == 'true'
            uses: actions/github-script@v7
            with:
              github-token: ${{ secrets.GITHUB_TOKEN }}
              script: |
                const count = '${{ steps.commits.outputs.count }}';
                const commitList = `${{ steps.commits.outputs.commit_list }}`;
                const storedSha = '${{ steps.compare.outputs.stored }}'.substring(0, 8);
                const currentSha = '${{ steps.compare.outputs.current }}'.substring(0, 8);
                const compareUrl = `https://github.com/cjpais/Handy/compare/${storedSha}...${currentSha}`;

                await github.rest.issues.create({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  title: `Upstream: ${count} new commits on cjpais/Handy`,
                  labels: ['upstream-sync'],
                  body: [
                    `## Upstream Sync Available`,
                    ``,
                    `**${count} new commit(s)** on [\`cjpais/Handy\`](https://github.com/cjpais/Handy) since last sync (\`${storedSha}\`).`,
                    ``,
                    `**Compare:** ${compareUrl}`,
                    ``,
                    `### Commits`,
                    `\`\`\``,
                    commitList,
                    `\`\`\``,
                    ``,
                    `Follow \`UPSTREAM.md\` to merge.`,
                  ].join('\n')
                });
    ```

    CRITICAL requirements that must be preserved verbatim:
    - `cron: '0 8 * * 1'` — Monday 08:00 UTC (quoted; YAML needs quotes on cron strings starting with `0`)
    - `workflow_dispatch:` enabled for manual testing
    - `permissions: issues: write` block present (Pitfall 5 — without this the action 403s)
    - `labels: ['upstream-sync']` array (Pitfall 3 — label must already exist in repo; see Task 3)
    - The workflow does NOT update `.github/upstream-sha.txt` — that file is only updated as part of the merge commit in Plan 03 (Pitfall 2 — updating too early makes issues disappear before work is done)
    - Title format: `Upstream: ${count} new commits on cjpais/Handy`
    - `git remote add upstream` wrapped in `if ! git remote get-url upstream` to survive re-runs where the remote already exists
    - First-run fallback: if stored SHA is empty, fall back to fork point `85a8ed77` for the commit range

    Do NOT add auto-commit, auto-merge, auto-PR logic. This workflow is detection-only.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/upstream-sync.yml'))" && grep -q "cron: '0 8 \* \* 1'" .github/workflows/upstream-sync.yml && grep -q "issues: write" .github/workflows/upstream-sync.yml && grep -q "labels: \['upstream-sync'\]" .github/workflows/upstream-sync.yml && grep -q "actions/github-script@v7" .github/workflows/upstream-sync.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/upstream-sync.yml` exists and parses as valid YAML (`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/upstream-sync.yml'))"` exits 0)
    - File contains literal `cron: '0 8 * * 1'` (Monday 08:00 UTC, quoted)
    - File contains `workflow_dispatch:` trigger
    - File contains `permissions:` block with `issues: write` and `contents: read`
    - File contains `uses: actions/checkout@v4` with `fetch-depth: 0`
    - File contains `uses: actions/github-script@v7`
    - File contains `labels: ['upstream-sync']` (exact array syntax)
    - File contains title template `Upstream: ${count} new commits on cjpais/Handy`
    - File contains the idempotency guard `if [ "$STORED_SHA" = "$CURRENT_SHA" ]`
    - File does NOT contain `git commit` or `git push` in any step (detection-only — grep: `! grep -E '^\s*git (commit|push)' .github/workflows/upstream-sync.yml`)
    - File does NOT contain `upstream-sha.txt` write/echo-redirect (`! grep -E '>[[:space:]]*\.github/upstream-sha\.txt' .github/workflows/upstream-sync.yml`)
    - File contains fork-point fallback `STORED="85a8ed77"` for first-run case
  </acceptance_criteria>
  <done>
    Detection workflow YAML is valid, wired with correct cron + permissions + label + title, and is detection-only (no auto-commit/merge).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify upstream-sync label exists in GitHub repo (Pitfall 3 pre-flight)</name>
  <files>GitHub repository labels at https://github.com/getdictus/dictus-desktop/labels</files>
  <read_first>
    - .planning/phases/05-upstream-sync/05-RESEARCH.md §"Pitfall 3: `upstream-sync` Label Not Created Before First Run"
  </read_first>
  <action>
    Pierre performs a one-time manual pre-flight in the GitHub UI. Claude cannot do this via API because it requires an authenticated browser session in Pierre's GitHub account and the label does not yet exist (so the REST API would 422 if the workflow ran first).

    **Manual steps for Pierre:**

    1. Open https://github.com/getdictus/dictus-desktop/labels in a browser
    2. Check if a label named exactly `upstream-sync` exists. If yes, skip to step 4.
    3. If missing, click **"New label"**:
       - Label name: `upstream-sync` (exact, case-sensitive, no spaces)
       - Description: `Tracks new upstream Handy commits detected by weekly sync action`
       - Color: any (suggest `#fbca04` orange or `#1d76db` blue for visibility)
       - Click **"Create label"**
    4. (Optional but recommended) Smoke test the workflow end-to-end:
       - Go to Actions → **"Upstream Sync Check"** → **"Run workflow"** → branch `main` → **Run**
       - Wait for completion (should be ~20-40s)
       - Expected outcome: one new GitHub issue titled `Upstream: 4 new commits on cjpais/Handy` with the `upstream-sync` label attached. Body contains a compare URL `https://github.com/cjpais/Handy/compare/39e855d...<current>` and lists the 4 commit SHAs `c1697b2`, `84d88f9`, `30b57c4`, `fdc8cb7` (prefix-length may vary).
    5. (Optional) Verify no 422/403 errors in the Actions log — success confirms permissions + label are both correct.

    **Note on SYNC-02 idempotency:** True idempotency is achieved only after Plan 03 lands on main (the merge commit updates `.github/upstream-sha.txt` to the new upstream HEAD). From that point, every subsequent workflow run sees `changed=false` and exits without creating an issue. Running the workflow twice BEFORE Plan 03 lands would create two duplicate issues — that is expected behavior (stored SHA is still `39e855d`, still differs from current upstream HEAD). The label pre-flight smoke test is validating that the action executes cleanly, not multi-run idempotency.
  </action>
  <verify>
    <automated>MISSING — manual verification only. Pierre confirms label exists by opening https://github.com/getdictus/dictus-desktop/labels in a browser. Optional smoke test confirmed by issue creation in GitHub UI.</automated>
  </verify>
  <done>
    `upstream-sync` label exists in the repo (verified by Pierre in the labels UI). Optionally: first `workflow_dispatch` run of `upstream-sync.yml` created exactly one labeled issue with correct title, compare URL, and 4-commit body.
  </done>
</task>

</tasks>

<verification>
- `.github/upstream-sha.txt` exists, contains `39e855d`
- `.planning/phases/05-upstream-sync/scripts/verify-sync.sh` is executable and green on current codebase
- `.github/workflows/upstream-sync.yml` parses as valid YAML
- Workflow contains: weekly cron, workflow_dispatch, `issues: write` permission, `upstream-sync` label, github-script@v7
- Workflow does NOT auto-commit/push/merge
- `upstream-sync` label exists in repo (verified via checkpoint)
- Optional: first manual run created exactly one labeled issue reporting the 4-commit delta
</verification>

<success_criteria>
- SYNC-01: workflow exists, creates issue on first manual run with correct title/labels (verified via Task 3 optional smoke test)
- SYNC-02: workflow only creates an issue when stored SHA differs from upstream HEAD — confirmed by code inspection of the `changed` output gate
- SYNC-05 substrate: verify-sync.sh passes on current codebase (will be re-run in Plan 03 after merge)
- All quality gate items from the plan checklist green
</success_criteria>

<output>
After completion, create `.planning/phases/05-upstream-sync/05-01-SUMMARY.md` with:
- Files created: .github/upstream-sha.txt, .github/workflows/upstream-sync.yml, .planning/phases/05-upstream-sync/scripts/verify-sync.sh
- Label status: created / already existed
- Optional smoke test result (issue URL if one was created)
- Any deviations from the exact YAML template (and why)
</output>
