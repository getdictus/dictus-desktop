---
phase: 03-documentation-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
  - BUILD.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "README describes Dictus Desktop as a fork of Handy with its own identity and vision"
    - "README contains no Handy references outside the Acknowledgments section"
    - "CLAUDE.md describes Dictus Desktop architecture with no Handy branding text"
    - "BUILD.md clone URL points to getdictus/dictus-desktop"
    - "Developer docs note the binary name discrepancy (handy binary, V2 rename planned)"
  artifacts:
    - path: "README.md"
      provides: "Dictus Desktop README with fork attribution, vision, quick start, architecture, license"
      contains: "Dictus Desktop is a fork of Handy"
    - path: "CLAUDE.md"
      provides: "Developer guide with Dictus Desktop references"
      contains: "Dictus Desktop"
    - path: "BUILD.md"
      provides: "Build instructions with Dictus clone URL"
      contains: "getdictus/dictus-desktop"
  key_links:
    - from: "README.md"
      to: "BUILD.md"
      via: "link in Development Setup section"
      pattern: "BUILD\\.md"
    - from: "README.md"
      to: "getdictus.com"
      via: "installation link"
      pattern: "getdictus\\.com"
---

<objective>
Rewrite README.md as a Dictus Desktop product page and update CLAUDE.md and BUILD.md to remove Handy branding.

Purpose: The repository's public face and developer docs must present Dictus Desktop as a first-class product, not a copy of the Handy README.
Output: Three updated markdown files — README.md (full rewrite), CLAUDE.md (targeted edits), BUILD.md (targeted edits)
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-documentation-and-cleanup/03-CONTEXT.md
@.planning/phases/03-documentation-and-cleanup/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md as Dictus Desktop product page</name>
  <files>README.md</files>
  <read_first>
    - README.md (current 456-line Handy README — see full structure to understand what to keep/remove)
    - .planning/phases/03-documentation-and-cleanup/03-CONTEXT.md (locked decisions on README positioning)
  </read_first>
  <action>
Replace the entire README.md with a Dictus Desktop README. The new README must follow this structure:

**Header:**

```
# Dictus Desktop

**Privacy-first speech-to-text. Your voice stays on your device.**
```

**Introduction (fork-first per locked decision):**

- Lead sentence: "Dictus Desktop is a fork of [Handy](https://github.com/cjpais/Handy) — a free, open-source speech-to-text application that works completely offline."
- Follow with Dictus identity and vision: local-first, privacy-focused, cross-platform (macOS, Windows, Linux)
- Mention Dictus ecosystem: also available on iOS and Android at [getdictus.com](https://getdictus.com)

**How It Works section:**

- Keep the existing 4-step flow (Press, Speak, Release, Get) — rewrite to say "Dictus" not "Handy"
- Keep the technical details: VAD with Silero, Whisper models, Parakeet V3, GPU acceleration
- Keep platform support line: macOS, Windows, Linux

**Quick Start section:**

- Installation: "Download from [getdictus.com](https://getdictus.com) (desktop downloads coming soon) or build from source using [BUILD.md](BUILD.md)"
- Do NOT include Homebrew cask or winget (those are Handy packages)
- Do NOT include blob.handy.computer model download URLs (app handles downloads internally)
- Keep: grant permissions, configure shortcuts, start transcribing

**Development Setup section:**

- Brief: "See [BUILD.md](BUILD.md) for detailed build instructions"
- No inline build commands — BUILD.md has them

**Architecture section:**

- Keep the existing architecture summary (Tauri, React+TS, Rust backend, core libraries list)
- Replace "Handy" with "Dictus Desktop" in all text
- Keep Debug Mode subsection (Cmd+Shift+D / Ctrl+Shift+D)
- Keep CLI Parameters subsection — keep `handy` binary name in code examples (it IS the binary name), add note: "> **Note:** The binary is currently named `handy` (a V2 rename is planned). Use `handy` in all CLI commands until the rename is complete."
- Keep the macOS tip about /Applications path but update to `/Applications/Dictus.app/Contents/MacOS/handy`

**Known Issues section:**

- Keep: Whisper model crashes, Wayland support, Linux notes (text input tools table, libgtk-layer-shell, overlay disabled, WEBKIT env var, Wayland keyboard shortcuts for GNOME/KDE/Sway/Hyprland)
- Replace "Handy" with "Dictus" or "Dictus Desktop" in all prose text
- Keep `handy` in CLI/binary examples (it IS the binary name)
- Update shortcut names from "Toggle Handy Transcription" to "Toggle Dictus Transcription" in GNOME/KDE examples
- Keep Platform Support subsection (macOS, Windows, Linux)
- Keep System Requirements subsection (Whisper + Parakeet)
- Replace "running Handy on your own machine" with "running Dictus Desktop on your machine"
- Update issues link to `https://github.com/getdictus/dictus-desktop/issues`

**REMOVE entirely:**

- "Why Handy?" section (replaced by the introduction)
- Integrations section (Raycast extension is Handy-specific)
- Verify Release Signatures section (Handy signing, not applicable)
- Troubleshooting > Manual Model Installation section (blob.handy.computer URLs, app handles downloads)
- Custom Whisper Models section (too detailed for README, users can discover in-app)
- Roadmap & Active Development section (Handy-specific roadmap)
- Sponsors section (Handy sponsors)
- Related Projects section (Handy CLI, handy.computer)

**How to Contribute section:**

- Update issues link to `https://github.com/getdictus/dictus-desktop/issues`
- Update contact to `hello@getdictus.com`
- Add community link: Telegram `https://t.me/getdictus`

**Contact/Community section:**

- GitHub Issues: `https://github.com/getdictus/dictus-desktop/issues`
- Website: `https://getdictus.com`
- Email: `hello@getdictus.com`
- Telegram: `https://t.me/getdictus`

**License section:**

- "MIT License - see [LICENSE](LICENSE) file for details."

**Acknowledgments section (dedicated, at the end):**

- First item: "**Handy** by [cjpais](https://github.com/cjpais/Handy) — the original project Dictus Desktop is forked from"
- Keep: Whisper by OpenAI, whisper.cpp and ggml, Silero VAD, Tauri team
- Remove: "Community contributors helping make Handy better"
  </action>
  <verify>
  <automated>grep -ic "handy" README.md | xargs -I{} echo "Total Handy mentions: {}" && echo "---" && grep -in "handy" README.md | grep -v "Acknowledgments\|binary.*named\|handy --\|/handy\|pkill.*handy\|Note.\*handy\|MacOS/handy"</automated>
  </verify>
  <acceptance_criteria> - README.md line 1 contains `# Dictus Desktop` - README.md contains the string `Dictus Desktop is a fork of` - README.md contains `getdictus.com` - README.md contains `hello@getdictus.com` - README.md contains `t.me/getdictus` - README.md contains `github.com/getdictus/dictus-desktop` - `grep -i "handy" README.md` returns ONLY lines in Acknowledgments section, binary name references in CLI examples, or the V2 rename note - README.md does NOT contain `blob.handy.computer` - README.md does NOT contain `handy.computer/donate` - README.md does NOT contain `Raycast` - README.md does NOT contain `Sponsors` - README.md does NOT contain `cjpais/Handy/releases` - README.md contains `BUILD.md` link - README.md contains `## Acknowledgments` section
  </acceptance_criteria>
  <done>README.md presents Dictus Desktop as a first-class product with fork attribution in Acknowledgments only, no blob.handy.computer URLs, no Handy-specific sections (sponsors, Raycast, signatures, roadmap)</done>
  </task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md and BUILD.md to remove Handy branding</name>
  <files>CLAUDE.md, BUILD.md</files>
  <read_first>
    - CLAUDE.md (current developer guide — 3 Handy references at lines ~32, ~37, ~124)
    - BUILD.md (current build instructions — Handy clone URL at line 55, binary path references)
    - .planning/phases/03-documentation-and-cleanup/03-RESEARCH.md (exact line numbers and replacement values)
  </read_first>
  <action>
**CLAUDE.md edits (3 targeted replacements + 1 note addition):**

1. Line containing `Handy is a cross-platform desktop speech-to-text app` — change to:
   `Dictus Desktop is a cross-platform desktop speech-to-text app built with Tauri 2.x (Rust backend + React/TypeScript frontend).`

2. Line containing `Handy supports command-line parameters` — change to:
   `Dictus Desktop supports command-line parameters on all platforms for integration with scripts, window managers, and autostart configurations.`

3. The VAD model curl line `curl -o src-tauri/resources/models/silero_vad_v4.onnx https://blob.handy.computer/silero_vad_v4.onnx` — keep the URL as-is (it is the only working source) but add a comment line directly after:

   ```
   # Note: This URL is from the upstream Handy CDN. A Dictus-owned CDN migration is planned (V2 INFR-01).
   ```

4. Any other instance of standalone "Handy" in prose text (not in code paths like `handy_keys` or binary name `handy`) — replace with "Dictus Desktop" or "Dictus" as appropriate for the sentence.

5. Search the entire file for remaining "Handy" references. The word "handy" in code identifiers (handy_keys, /handy, binary name) must stay — only branded prose "Handy" changes to "Dictus Desktop".

**BUILD.md edits (clone URL + branding text):**

1. Title line: `# Build Instructions` — keep as-is (generic title is fine)

2. Line 4: Change `build Handy from source` to `build Dictus Desktop from source`

3. Lines 55-56 clone section — replace:

   ```bash
   git clone git@github.com:getdictus/dictus-desktop.git
   cd dictus-desktop
   ```

4. Line 81 (raw binary reference): Change `The raw binary (\`src-tauri/target/release/handy\`)`— keep the`handy` binary name (it IS the binary name), but change surrounding prose text from "Handy" to "Dictus Desktop" where it appears as a product name.

5. Lines 86-93 (deb bundle paths): Keep `handy` in binary paths (`/usr/bin/handy`, `src-tauri/target/release/handy`) — these are filesystem paths. Change `Handy` product name references in the .deb filename pattern and .desktop file path to match the current build output. Check: `Handy_*_amd64.deb` and `Handy.desktop` may still be the actual output filenames from Tauri — if so, keep them accurate with a note.

6. Add a note after the Linux Install section heading:

   ```
   > **Note:** The binary is named `handy` and Tauri bundle filenames use `Handy` — a full rename is planned for V2 (TECH-03).
   ```

7. Lines referencing `Handy` in the Troubleshooting AppImage section — replace product name "Handy" with "Dictus Desktop" in prose, keep `Handy` in actual file paths/bundle names that Tauri generates (e.g., `Handy_*_amd64.AppImage`, `Handy.AppDir`).
   </action>
   <verify>
   <automated>echo "=== CLAUDE.md ===" && grep -n "Dictus Desktop" CLAUDE.md | head -5 && echo "---" && grep -in "handy" CLAUDE.md | grep -v "handy*keys\|/handy\|handy --\|pkill.*handy\|binary.*handy\|handy\.computer.\*CDN\|INFR-01" && echo "=== BUILD.md ===" && grep -n "getdictus" BUILD.md && echo "---" && grep -in "handy" BUILD.md | grep -v "/handy\|handy --\|Handy*\*\|Handy\.AppDir\|Handy\.desktop\|binary.\*handy\|TECH-03\|V2"</automated>
   </verify>
   <acceptance_criteria> - CLAUDE.md contains `Dictus Desktop is a cross-platform desktop speech-to-text app` - CLAUDE.md contains `Dictus Desktop supports command-line parameters` - CLAUDE.md contains the comment `# Note: This URL is from the upstream Handy CDN` - CLAUDE.md still contains `blob.handy.computer` (the URL is kept, not broken) - `grep -i "handy" CLAUDE.md` returns only: CDN URL line, CDN note line, code identifiers (handy_keys, binary paths) - BUILD.md contains `getdictus/dictus-desktop.git` - BUILD.md contains `cd dictus-desktop` - BUILD.md contains `build Dictus Desktop from source` - BUILD.md contains a V2 rename note mentioning TECH-03 - `grep -i "handy" BUILD.md` returns only: binary path references, Tauri bundle filenames, V2 rename note
   </acceptance_criteria>
   <done>CLAUDE.md and BUILD.md use Dictus Desktop branding in all prose text, with accurate binary/path references preserved and V2 rename notes added</done>
   </task>

</tasks>

<verification>
- `grep -ic "handy" README.md` returns a small number (only Acknowledgments + CLI binary references)
- `grep -c "Dictus" README.md` returns a significant number (product name throughout)
- `grep -c "Dictus Desktop" CLAUDE.md` returns at least 2
- `grep "getdictus" BUILD.md` returns the clone URL
- `bun run format:check` passes (markdown formatting)
</verification>

<success_criteria>

- README.md is a complete Dictus Desktop product page with fork attribution in Acknowledgments
- No blob.handy.computer URLs in README
- No Handy-branded prose in CLAUDE.md or BUILD.md (only technical path references)
- All three files pass format checks
  </success_criteria>

<output>
After completion, create `.planning/phases/03-documentation-and-cleanup/03-01-SUMMARY.md`
</output>
