---
phase: 06-brand-icon-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/scripts/verify-sync.sh
  - .planning/phases/05-upstream-sync/scripts/verify-sync.sh
  - UPSTREAM.md
autonomous: true
requirements: [BRAND-04, ICON-02, SYNC-06]
must_haves:
  truths:
    - "verify-sync.sh lives at .github/scripts/verify-sync.sh and is executable"
    - "verify-sync.sh is missing from .planning/phases/05-upstream-sync/scripts/"
    - "verify-sync.sh contains 15 assertions: 11 existing SYNC-05* + 4 new (BRAND-01a, BRAND-02a, BRAND-03a, ICON-02a)"
    - "verify-sync.sh fails early with 'imagemagick required' message when identify is missing"
    - "UPSTREAM.md path references point to .github/scripts/verify-sync.sh"
    - "Running bash .github/scripts/verify-sync.sh exercises all 15 checks"
    - "The existing SYNC-05d awk acknowledgments-skip logic is preserved verbatim"
  artifacts:
    - path: ".github/scripts/verify-sync.sh"
      provides: "Relocated identity validator with 15 assertions"
      contains: "BRAND-01a"
    - path: "UPSTREAM.md"
      provides: "Runbook with updated script paths"
      contains: ".github/scripts/verify-sync.sh"
  key_links:
    - from: ".github/scripts/verify-sync.sh"
      to: "src-tauri/src/ + src/components/ + src-tauri/icons/icon.ico"
      via: "grep + identify assertions"
      pattern: "check \"(BRAND|ICON)-.*a"
    - from: "UPSTREAM.md"
      to: ".github/scripts/verify-sync.sh"
      via: "documentation reference"
      pattern: "\\.github/scripts/verify-sync\\.sh"
---

<objective>
Relocate verify-sync.sh from its temporary home under .planning/phases/05-upstream-sync/scripts/ to its permanent home at .github/scripts/verify-sync.sh (SYNC-06), extend it with four new assertions that guard the Brand & Icon polish surfaces (BRAND-01a, BRAND-02a, BRAND-03a, ICON-02a), add an imagemagick dev-dep check, and update all UPSTREAM.md path references.

Purpose: The verify-sync.sh script is the primary automated verification surface for all other Plan 06 tasks (BRAND-01/02/03, ICON-01/02/03/04). It must be in place at its final path BEFORE those plans run so their per-task `bash .github/scripts/verify-sync.sh` sampling works. Plan 06-01 is Wave 0/1 infrastructure for Plans 06-02/03/04.

Output: Working verify-sync.sh at .github/scripts/ with 15 checks, UPSTREAM.md paths updated, all existing SYNC-05* checks still green, the 4 new BRAND/ICON checks expected to fail until downstream plans land.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-brand-icon-polish/06-CONTEXT.md
@.planning/phases/06-brand-icon-polish/06-RESEARCH.md
@.planning/phases/06-brand-icon-polish/06-VALIDATION.md
@.planning/phases/05-upstream-sync/scripts/verify-sync.sh
@UPSTREAM.md

<interfaces>
<!-- Existing verify-sync.sh structure (as of 2026-04-15) -->
<!-- 11 existing assertions use this pattern: -->

```bash
check "<label>" "<shell expr>"
```

Where `check` is defined as:
```bash
FAIL=0
check() {
  local label="$1"; shift
  if eval "$@" >/dev/null 2>&1; then
    echo "  ok  $label"
  else
    echo "  FAIL $label"
    echo "       cmd: $*"
    FAIL=1
  fi
}
```

Existing dep check at line 18:
```bash
command -v jq >/dev/null 2>&1 || { echo "jq required — brew install jq"; exit 2; }
```

Existing header echo:
```bash
echo "Phase 5 — Upstream Sync identity + build assertions"
```

Existing final message:
```bash
if [ $FAIL -eq 0 ]; then
  echo "All Phase 5 checks passed."
  exit 0
else
  echo "One or more Phase 5 checks FAILED."
  exit 1
fi
```

UPSTREAM.md references to rewrite (from grep -n 'verify-sync\|05-upstream-sync' UPSTREAM.md):
- line 216: `bash .planning/phases/05-upstream-sync/scripts/verify-sync.sh`
- line 239: `"Follows UPSTREAM.md. All SYNC-05 post-merge checks green locally. See .planning/phases/05-upstream-sync/ for context."`
- line 254: `| Skip `verify-sync.sh` before pushing | ...`  (no path — leave as-is)
- line 284: `- .planning/phases/05-upstream-sync/scripts/verify-sync.sh — post-merge identity gate`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate verify-sync.sh to .github/scripts/ (SYNC-06)</name>
  <files>
    - .github/scripts/verify-sync.sh (new location)
    - .planning/phases/05-upstream-sync/scripts/verify-sync.sh (removed)
  </files>
  <read_first>
    - .planning/phases/05-upstream-sync/scripts/verify-sync.sh (current content — must be preserved byte-for-byte on move)
    - .planning/phases/06-brand-icon-polish/06-CONTEXT.md §"verify-sync.sh — relocation + brand assertions"
    - .planning/phases/06-brand-icon-polish/06-RESEARCH.md §"Pattern 4: verify-sync.sh Move + Extension"
  </read_first>
  <action>
    Perform a content-preserving move of the script using `git mv`:

    1. Create the target directory: `mkdir -p .github/scripts`
    2. Run: `git mv .planning/phases/05-upstream-sync/scripts/verify-sync.sh .github/scripts/verify-sync.sh`
    3. Verify the script is still executable: `chmod +x .github/scripts/verify-sync.sh`
    4. If `.planning/phases/05-upstream-sync/scripts/` is now empty, remove the empty directory: `rmdir .planning/phases/05-upstream-sync/scripts/ 2>/dev/null || true` (do NOT force-remove if other files exist).
    5. Run the script from repo root to confirm it still works AT ITS OLD ASSERTION SET (all 11 SYNC-05* checks must be green):
       `bash .github/scripts/verify-sync.sh`
    6. Confirm exit code 0. Do NOT add new assertions in this task — that is Task 2's scope.

    Rationale: `git mv` preserves file history. Content must not change in this task so diff review in Task 2 is limited to the new assertions only.

    Do NOT:
    - Modify any existing `check` line in this task (exact content preservation)
    - Change the shebang, `set -euo pipefail`, or the `check()` helper definition
    - Touch UPSTREAM.md yet (Task 3 handles documentation updates)
  </action>
  <verify>
    <automated>test -x .github/scripts/verify-sync.sh && test ! -f .planning/phases/05-upstream-sync/scripts/verify-sync.sh && bash .github/scripts/verify-sync.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/scripts/verify-sync.sh` exists and is executable (`test -x .github/scripts/verify-sync.sh` returns 0)
    - File `.planning/phases/05-upstream-sync/scripts/verify-sync.sh` does NOT exist (`test ! -f` returns 0)
    - `git log --follow --oneline .github/scripts/verify-sync.sh` shows prior Phase 5 commits (history preserved through `git mv`)
    - First line of `.github/scripts/verify-sync.sh` is `#!/usr/bin/env bash`
    - Running `bash .github/scripts/verify-sync.sh` from repo root exits 0 (all 11 existing SYNC-05* checks pass at this point since no source changes were made)
    - Grep for `check "SYNC-05` in `.github/scripts/verify-sync.sh` returns exactly 11 lines (SYNC-05a through SYNC-05k)
    - `grep -c 'check "BRAND-' .github/scripts/verify-sync.sh` returns 0 (new assertions not yet added)
  </acceptance_criteria>
  <done>Script moved to final home, content byte-for-byte identical to pre-move, existing 11 checks all green, old path no longer exists.</done>
</task>

<task type="auto">
  <name>Task 2: Extend verify-sync.sh with BRAND/ICON assertions + imagemagick dep check</name>
  <files>
    - .github/scripts/verify-sync.sh
  </files>
  <read_first>
    - .github/scripts/verify-sync.sh (current state after Task 1 move — must see the `jq` dep-check line and the `check "SYNC-05k"` line to know where to insert)
    - .planning/phases/06-brand-icon-polish/06-CONTEXT.md §"verify-sync.sh — relocation + brand assertions" §"Roadmap ripple"
    - .planning/phases/06-brand-icon-polish/06-RESEARCH.md §"Pattern 4: verify-sync.sh Move + Extension" (contains exact check strings)
    - .planning/phases/06-brand-icon-polish/06-VALIDATION.md (contract: each BRAND-01/02/03 + ICON-02 requirement maps to a verify-sync assertion)
  </read_first>
  <action>
    Edit `.github/scripts/verify-sync.sh` to add four new assertions and one new dependency check. Perform the following exact edits:

    **Edit 1 — Add imagemagick dep check after the existing jq check:**

    Find this line (around line 18):
    ```
    command -v jq >/dev/null 2>&1 || { echo "jq required — brew install jq"; exit 2; }
    ```

    Add this new line immediately after:
    ```
    command -v identify >/dev/null 2>&1 || { echo "imagemagick required — brew install imagemagick"; exit 2; }
    ```

    **Edit 2 — Update the header echo (and only the header, not the final message):**

    Change:
    ```
    echo "Phase 5 — Upstream Sync identity + build assertions"
    ```
    To:
    ```
    echo "Phase 5+6 — Upstream Sync + Brand/Icon identity assertions"
    ```

    **Edit 3 — Append four new `check` lines BEFORE the final `echo` / `if [ $FAIL -eq 0 ]` block:**

    The final block of the script currently looks like:
    ```bash
    check "SYNC-05k upstream-sha.txt is full 40-char SHA" \
      "[ \$(tr -d '[:space:]' < .github/upstream-sha.txt | wc -c) -eq 40 ]"

    echo
    if [ $FAIL -eq 0 ]; then
    ```

    Insert these four new checks BETWEEN the `SYNC-05k` check and the blank `echo` line. Preserve the existing `check` pattern exactly (double quotes around label, double quotes around shell expr, backslash-escaped inner quotes):

    ```bash
    check "BRAND-01a no handy- filename prefix in src-tauri (except handy_keys crate)" \
      "! grep -rn 'handy-' src-tauri/src/ | grep -v handy_keys | grep -q ."

    check "BRAND-02a no legacy Handy Portable Mode string in portable.rs" \
      "! grep -q '\"Handy Portable Mode\"' src-tauri/src/portable.rs"

    check "BRAND-03a no hardcoded %APPDATA%/handy path in DebugPaths.tsx" \
      "! grep -q '%APPDATA%/handy' src/components/settings/debug/DebugPaths.tsx"

    check "ICON-02a icon.ico contains all 6 required layer sizes (16,24,32,48,64,256)" \
      "[ \$(identify -format '%w\n' src-tauri/icons/icon.ico 2>/dev/null | sort -u | grep -cE '^(16|24|32|48|64|256)$') -eq 6 ]"
    ```

    **Edit 4 — Update the final pass/fail message to reflect phase coverage:**

    Change:
    ```
    echo "All Phase 5 checks passed."
    ```
    To:
    ```
    echo "All Phase 5+6 checks passed."
    ```

    And change:
    ```
    echo "One or more Phase 5 checks FAILED."
    ```
    To:
    ```
    echo "One or more Phase 5+6 checks FAILED."
    ```

    **IMPORTANT — what to PRESERVE verbatim:**
    - `set -euo pipefail` (line 4)
    - The entire `FAIL=0` + `check()` helper definition (lines 6-15)
    - Every existing `check "SYNC-05*"` line — no edits to labels, commands, or the SYNC-05d awk-acknowledgments-skip expression
    - The exit codes (0 pass, 1 fail, 2 missing-dep)

    **Running the script now will FAIL** — expected. The BRAND-01a/02a/03a checks will fail because Plans 06-02 and 06-03 haven't replaced the `handy-` strings yet. That is correct and desired. The script structure must be valid bash (no syntax errors).

    Do NOT:
    - Invent additional assertions beyond the four specified
    - Change any SYNC-05* line
    - Modify the SYNC-05d awk expression in any way
    - Add SYNC-07 updater pubkey/endpoint assertions (Phase 9 owns those per CONTEXT.md)
  </action>
  <verify>
    <automated>bash -n .github/scripts/verify-sync.sh && grep -c '^check ' .github/scripts/verify-sync.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n .github/scripts/verify-sync.sh` exits 0 (script is syntactically valid bash)
    - `grep -c '^check ' .github/scripts/verify-sync.sh` returns exactly 15
    - `grep -c 'check "SYNC-05' .github/scripts/verify-sync.sh` returns exactly 11 (existing checks preserved)
    - `grep -q 'check "BRAND-01a' .github/scripts/verify-sync.sh` returns 0
    - `grep -q 'check "BRAND-02a' .github/scripts/verify-sync.sh` returns 0
    - `grep -q 'check "BRAND-03a' .github/scripts/verify-sync.sh` returns 0
    - `grep -q 'check "ICON-02a' .github/scripts/verify-sync.sh` returns 0
    - `grep -q "command -v identify" .github/scripts/verify-sync.sh` returns 0
    - `grep -q "imagemagick required" .github/scripts/verify-sync.sh` returns 0
    - `grep -q "Phase 5+6" .github/scripts/verify-sync.sh` returns 0
    - SYNC-05d awk expression still present: `grep -q "awk '/\"acknowledgments\"/" .github/scripts/verify-sync.sh` returns 0
    - Running `bash .github/scripts/verify-sync.sh` exits with a non-zero status (at least one new BRAND-0*a check fails because source hasn't been updated yet) AND stdout contains the strings `"BRAND-01a"`, `"BRAND-02a"`, `"BRAND-03a"`, `"ICON-02a"` — confirming the new checks actually run
    - If ICON-02a passes right away (ICO already has all 6 layers per RESEARCH.md Pitfall 2), that is expected and acceptable
  </acceptance_criteria>
  <done>15 assertions present, script syntactically valid, imagemagick dep check added, new BRAND/ICON checks execute when script runs (some expected to fail pending other plans).</done>
</task>

<task type="auto">
  <name>Task 3: Update UPSTREAM.md path references to new script location</name>
  <files>
    - UPSTREAM.md
  </files>
  <read_first>
    - UPSTREAM.md (must see current state around lines 216, 239, 284)
    - .planning/phases/06-brand-icon-polish/06-CONTEXT.md §"verify-sync.sh — relocation + brand assertions" ("UPSTREAM.md updates" bullet)
  </read_first>
  <action>
    Update all stale path references inside `UPSTREAM.md` so they point to the new location `.github/scripts/verify-sync.sh`.

    Perform exactly these string replacements across the whole file:

    1. Replace every occurrence of the literal string:
       `.planning/phases/05-upstream-sync/scripts/verify-sync.sh`
       with:
       `.github/scripts/verify-sync.sh`

    2. Replace the remaining context-reference on line 239 (which points to the phase directory, not the script):
       Original: `See .planning/phases/05-upstream-sync/ for context.`
       Updated: `See .planning/phases/05-upstream-sync/ for historical context; script now lives at .github/scripts/verify-sync.sh.`

    Known occurrences to address (from `grep -n 'verify-sync\|05-upstream-sync' UPSTREAM.md`):
    - Line 216: `bash .planning/phases/05-upstream-sync/scripts/verify-sync.sh` → `bash .github/scripts/verify-sync.sh`
    - Line 239: the "Follows UPSTREAM.md..." body string — apply replacement #2 above
    - Line 284: `- .planning/phases/05-upstream-sync/scripts/verify-sync.sh — post-merge identity gate` → `- .github/scripts/verify-sync.sh — post-merge identity gate`
    - Line 254 (mention of `verify-sync.sh` without a path) — LEAVE AS-IS; it references the script by name only.

    After editing, run this sanity check to confirm no stale path references remain:
    `grep -n '05-upstream-sync/scripts/verify-sync' UPSTREAM.md` should return 0 matches.

    Note for the execute-plan orchestrator: This plan completes SYNC-06 ahead of its original Phase 9 schedule. After this phase ships, run `/gsd:roadmap-update` so Phase 9 scope reflects that SYNC-06 is already done (Phase 9 remains owner of SYNC-07/08/09/10/11 only).

    Do NOT:
    - Edit any other content in UPSTREAM.md (cap-at-SHA rules, conflict sections, etc. must be preserved)
    - Remove or rewrite the phase-5 historical context reference — just annotate it with the new script path
    - Rewrite the broader runbook sections; that is Phase 9 SYNC-11 scope
  </action>
  <verify>
    <automated>! grep -q '05-upstream-sync/scripts/verify-sync' UPSTREAM.md && grep -c '\.github/scripts/verify-sync\.sh' UPSTREAM.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '05-upstream-sync/scripts/verify-sync' UPSTREAM.md` returns 0 (no stale script paths remain)
    - `grep -c '\.github/scripts/verify-sync\.sh' UPSTREAM.md` returns at least 2 (the bash-invocation line and the bullet-list entry both updated)
    - Line containing `bash .github/scripts/verify-sync.sh` exists in UPSTREAM.md: `grep -q 'bash \.github/scripts/verify-sync\.sh' UPSTREAM.md` returns 0
    - The line-284 bullet `- .github/scripts/verify-sync.sh — post-merge identity gate` exists: `grep -q '.github/scripts/verify-sync.sh — post-merge identity gate' UPSTREAM.md` returns 0
    - The file still contains the cap-at-SHA rules and conflict sections untouched: `grep -q 'cap-at-SHA' UPSTREAM.md` returns 0 AND `grep -q 'Skip \`verify-sync.sh\` before pushing' UPSTREAM.md` returns 0
    - `wc -l UPSTREAM.md` is within ±2 lines of the pre-edit line count (no large rewrites)
  </acceptance_criteria>
  <done>All verify-sync.sh path references in UPSTREAM.md point to `.github/scripts/verify-sync.sh`; no stale paths remain; runbook content otherwise untouched.</done>
</task>

</tasks>

<verification>
After all three tasks complete, the following must hold:

1. `test -x .github/scripts/verify-sync.sh` (executable at new home)
2. `test ! -f .planning/phases/05-upstream-sync/scripts/verify-sync.sh` (old home gone)
3. `grep -c '^check ' .github/scripts/verify-sync.sh` returns 15
4. `bash -n .github/scripts/verify-sync.sh` exits 0 (valid syntax)
5. `bash .github/scripts/verify-sync.sh` runs all 15 checks; output contains BRAND-01a, BRAND-02a, BRAND-03a, ICON-02a labels (exit code may be non-zero until Plans 06-02/03 land — this is expected and correct at end of this plan)
6. `! grep -q '05-upstream-sync/scripts/verify-sync' UPSTREAM.md`
7. `grep -q 'check "ICON-02a' .github/scripts/verify-sync.sh`

Commit suggestion: `chore(sync): relocate verify-sync.sh to .github/scripts/ and add BRAND/ICON assertions (SYNC-06, BRAND-04, ICON-02)`
</verification>

<success_criteria>
- verify-sync.sh is at its permanent location `.github/scripts/verify-sync.sh` (SYNC-06)
- 15 assertions present: 11 SYNC-05* preserved + 4 new (BRAND-01a, BRAND-02a, BRAND-03a, ICON-02a) (BRAND-04, ICON-02 verification surface)
- Script is syntactically valid bash (bash -n passes)
- imagemagick dep check present and fails early with install hint if missing
- UPSTREAM.md paths updated in all locations (no `.planning/phases/05-upstream-sync/scripts/verify-sync` references remain)
- Existing SYNC-05* checks unchanged (same labels, same commands, same awk acknowledgments-skip)
- Plans 06-02/03/04 can invoke `bash .github/scripts/verify-sync.sh` as their per-task feedback command
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-icon-polish/06-01-SUMMARY.md` following the standard summary template. Note in the summary that:
- SYNC-06 is complete ahead of its original Phase 9 schedule
- `/gsd:roadmap-update` should be run post-phase to reflect the scope shift
- The 4 new assertions (BRAND-01a, BRAND-02a, BRAND-03a) are expected to fail until Plans 06-02 and 06-03 complete; ICON-02a likely passes immediately since the current icon.ico already has all 6 layers per research
</output>
