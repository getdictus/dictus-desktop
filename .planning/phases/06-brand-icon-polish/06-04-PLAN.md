---
phase: 06-brand-icon-polish
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src-tauri/icons/
  - src-tauri/tauri.conf.json
autonomous: false
requirements: [ICON-01, ICON-02, ICON-03, ICON-04]
user_setup:
  - service: dictus-brand
    why: "Source-of-truth 1024×1024 square RGBA PNG must be added to dictus-brand repo before tauri icon regeneration"
    dashboard_config:
      - task: "Add 1024×1024 square RGBA PNG (transparent background, square corners, Dictus waveform glyph) to ../dictus-brand/ repo at a path of Pierre's choosing (suggested: dictus-brand/source/appicon-desktop-1024.png)"
        location: "dictus-brand repo (sibling to dictus-desktop)"
must_haves:
  truths:
    - "A 1024×1024 square RGBA PNG with transparent background exists in the dictus-brand repo and is referenced as the icon source-of-truth"
    - "Running bun run tauri icon against that source regenerates src-tauri/icons/ with correct sizes and formats"
    - "src-tauri/icons/icon.ico embeds at least 6 layers including 16, 24, 32, 48, 64, and 256 px"
    - "src-tauri/tauri.conf.json bundle.icon array includes 256×256 and 512×512 PNG entries explicitly"
    - "Linux icon source has transparent corners (pixel alpha at (0,0) is 0) — no baked-in rounded corners or black background"
    - "verify-sync.sh ICON-02a assertion passes"
    - "cargo build succeeds after the config change (no missing icon file errors from the bundler)"
  artifacts:
    - path: "src-tauri/icons/icon.ico"
      provides: "Windows multi-layer icon with 16/24/32/48/64/256 px layers"
    - path: "src-tauri/icons/icon.icns"
      provides: "macOS icon file (OS re-masks rounded corners at runtime)"
    - path: "src-tauri/icons/32x32.png"
      provides: "Linux small icon"
    - path: "src-tauri/icons/128x128.png"
      provides: "Linux medium icon"
    - path: "src-tauri/icons/128x128@2x.png"
      provides: "Linux high-DPI (256×256) icon"
    - path: "src-tauri/tauri.conf.json"
      provides: "Bundle config listing all platform icon variants"
      contains: "256x256"
  key_links:
    - from: "../dictus-brand/<pierre-chosen-path>/appicon-desktop-1024.png"
      to: "src-tauri/icons/* (all generated files)"
      via: "bun run tauri icon <source>"
      pattern: "1024.*appicon"
    - from: "src-tauri/tauri.conf.json bundle.icon"
      to: "Tauri bundler → Linux deb/AppImage + Windows .exe + macOS .app"
      via: "Bundle configuration array"
      pattern: "bundle.*icon"
---

<objective>
Regenerate the entire platform icon set from a new square 1024×1024 RGBA source PNG (ICON-04), fix the Linux black-corners artifact by using a source with transparent corners (ICON-01), verify the Windows multi-layer ICO is intact (ICON-02), and extend `tauri.conf.json` bundle.icon to explicitly list the 256×256 and 512×512 PNG sizes so Linux deb/AppImage builds ship them (ICON-03).

Purpose: The current Linux icon shows a black corner artifact because the upstream Handy source PNG bakes in iOS-style rounded corners (non-transparent pixels at the corners). Linux ships the PNG as-is and cannot re-mask. Creating a new square-corner-transparent source in dictus-brand and regenerating cleanly fixes all four icon requirements at once.

Output: New 1024×1024 source committed to dictus-brand; `src-tauri/icons/` fully regenerated; `tauri.conf.json` extended with 256×256 and 512×512 entries; verify-sync.sh ICON-02a check passes; `cargo build` succeeds.

**This plan contains two checkpoints:**
1. `checkpoint:human-action` — Pierre must add the 1024×1024 square PNG to dictus-brand BEFORE Task 2 can run (Claude cannot open a browser or manipulate files outside this repo's working dir).
2. `checkpoint:human-verify` — Final visual verification of Linux/Windows icons requires Pierre to install a deb/AppImage/MSI on the target OS (Claude cannot render OS-level shell UI).
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-brand-icon-polish/06-CONTEXT.md
@.planning/phases/06-brand-icon-polish/06-RESEARCH.md
@.planning/phases/06-brand-icon-polish/06-VALIDATION.md
@.github/scripts/verify-sync.sh
@src-tauri/tauri.conf.json

<interfaces>
<!-- Current tauri.conf.json > bundle.icon (MUST BE EXTENDED): -->

```json
"icon": [
  "icons/32x32.png",
  "icons/128x128.png",
  "icons/128x128@2x.png",
  "icons/icon.icns",
  "icons/icon.ico"
]
```

<!-- Current src-tauri/icons/ inventory (ls output from 2026-04-15): -->
<!-- 128x128.png, 128x128@2x.png, 32x32.png, 64x64.png, -->
<!-- icon.icns, icon.ico, icon.png, logo.png, -->
<!-- Square{107,142,150,284,30,310,44,71,89}Logo.png, StoreLogo.png, -->
<!-- android/, ios/ -->

<!-- NOTE: The current directory has NO explicit 256x256.png or 512x512.png files. -->
<!-- 128x128@2x.png IS 256×256 pixels (Retina 2x of 128×128) but is named differently. -->
<!-- icon.png IS 512×512 but is named "icon.png" not "512x512.png". -->
<!-- Research §Pitfall 3 flags that tauri icon CLI output filenames are uncertain. -->
<!-- Task 3 (tauri.conf.json edit) must be based on FILES THAT ACTUALLY EXIST after Task 2's regeneration, not assumed filenames. -->

<!-- Expected tauri.conf.json bundle.icon after Task 3 (if tauri icon generates exact names): -->

```json
"icon": [
  "icons/32x32.png",
  "icons/128x128.png",
  "icons/128x128@2x.png",
  "icons/256x256.png",
  "icons/512x512.png",
  "icons/icon.icns",
  "icons/icon.ico"
]
```

<!-- If tauri icon does NOT generate literal 256x256.png or 512x512.png files, -->
<!-- Task 3 MUST either (a) generate them manually via ImageMagick resize from the 1024 source -->
<!-- or (b) reference the actual generated filename. Planner leaves this decision to Task 3 executor -->
<!-- based on observed tauri icon output. -->

<!-- verify-sync.sh ICON-02a assertion (from Plan 06-01 Task 2): -->

```bash
check "ICON-02a icon.ico contains all 6 required layer sizes (16,24,32,48,64,256)" \
  "[ \$(identify -format '%w\n' src-tauri/icons/icon.ico 2>/dev/null | sort -u | grep -cE '^(16|24|32|48|64|256)$') -eq 6 ]"
```

<!-- External dependency (Pierre-owned): -->
<!-- ../dictus-brand/ — sibling repo; Pierre adds the 1024×1024 square PNG before Task 2 runs. -->
<!-- RESEARCH.md Open Question #2: confirm dictus-brand is cloned at ../dictus-brand/ relative to dictus-desktop. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1 (CHECKPOINT): Pierre adds 1024×1024 square source PNG to dictus-brand</name>
  <files>
    - ../dictus-brand/&lt;pierre-chosen-path&gt;/appicon-desktop-1024.png (new, external repo)
  </files>
  <read_first>
    - .planning/phases/06-brand-icon-polish/06-CONTEXT.md §"Icon source-of-truth (ICON-04)"
    - .planning/phases/06-brand-icon-polish/06-RESEARCH.md §"Pattern 3: Icon Pipeline" §"Pitfall 7"
  </read_first>
  <action>
    **This is a human-action checkpoint. Claude cannot produce this asset — Pierre owns the dictus-brand repo and the visual design.**

    Pierre must add a new PNG file to the sibling `dictus-brand` repository with these exact properties:

    - Format: PNG with RGBA channels
    - Dimensions: exactly 1024 × 1024 pixels
    - Corners: SQUARE (NO rounded corners — Linux ships the icon as-is without re-masking; rounded corners result in the black-corners artifact that ICON-01 fixes)
    - Background: fully transparent (alpha channel = 0 on all non-glyph pixels, including all 4 corners)
    - Glyph: the same Dictus waveform used on iOS/mobile, rendered centered on the transparent square canvas
    - Suggested path: `../dictus-brand/source/appicon-desktop-1024.png` (Pierre may choose a different path — e.g., a new `../dictus-brand/desktop/` subfolder — and must communicate the final path back via the resume-signal)

    **Must NOT reuse:**
    - `../dictus-brand/source/logo.png` (1024×1024) without verifying the spec above — RESEARCH.md Pitfall 7 flags ambiguity
    - `../dictus-brand/source/appicon-ios-1024.png` (or any iOS icon) — iOS assets have rounded corners baked in, which is the ICON-01 root cause

    **Pierre self-checks before signaling ready:**
    ```bash
    # From the dictus-desktop repo root, after adding the PNG to dictus-brand:
    TARGET=../dictus-brand/<chosen-path>/appicon-desktop-1024.png
    identify -format "%wx%h %[channels]\n" "$TARGET"
    # Expected: "1024x1024 rgba" (or "1024x1024 srgba")

    identify -format "%[fx:p{0,0}.a] %[fx:p{1023,0}.a] %[fx:p{0,1023}.a] %[fx:p{1023,1023}.a]\n" "$TARGET"
    # Expected: "0 0 0 0"
    ```

    Pierre commits the new file to the dictus-brand repo with a message like:
    `feat(desktop): add 1024px square source for dictus-desktop icon regeneration (ICON-04)`

    **Dictus-brand location assumption:** The dictus-brand repo is expected at `../dictus-brand/` (sibling to dictus-desktop). RESEARCH.md Open Question #2 flags that this assumption must be verified. If Pierre has dictus-brand cloned elsewhere, the actual path must be communicated so Task 2 can reference it.
  </action>
  <what-needed>
    A 1024×1024 RGBA PNG with transparent square corners and the Dictus waveform glyph, committed to dictus-brand at a path Pierre chooses.
  </what-needed>
  <how-to-verify>
    1. Confirm the file exists at the agreed path:
       `ls -la ../dictus-brand/<agreed-path>/appicon-desktop-1024.png`
    2. Confirm dimensions are 1024×1024:
       `identify -format "%wx%h\n" ../dictus-brand/<agreed-path>/appicon-desktop-1024.png`
       Expected output: `1024x1024`
    3. Confirm all four corners are transparent (pixel alpha = 0):
       `identify -format "%[fx:p{0,0}.a] %[fx:p{1023,0}.a] %[fx:p{0,1023}.a] %[fx:p{1023,1023}.a]\n" ../dictus-brand/<agreed-path>/appicon-desktop-1024.png`
       Expected: `0 0 0 0`
    4. Confirm dictus-brand has the new commit: `git -C ../dictus-brand log -1 --format=%s` references ICON-04 or the new desktop asset
  </how-to-verify>
  <verify>
    <automated>test -f "$DICTUS_BRAND_ICON_PATH" && [ "$(identify -format '%wx%h\n' "$DICTUS_BRAND_ICON_PATH")" = "1024x1024" ] && [ "$(identify -format '%[fx:p{0,0}.a] %[fx:p{1023,0}.a] %[fx:p{0,1023}.a] %[fx:p{1023,1023}.a]\n' "$DICTUS_BRAND_ICON_PATH")" = "0 0 0 0" ]</automated>
  </verify>
  <acceptance_criteria>
    - The PNG file exists at the path Pierre communicated via resume-signal (1024×1024, RGBA, all four corner pixels with alpha=0)
    - Pierre has committed the asset to the dictus-brand repo
    - The path is recorded in the Task 2 `<read_first>` reference for the subsequent executor
  </acceptance_criteria>
  <done>
    A 1024×1024 square-corner-transparent RGBA PNG exists in dictus-brand; Pierre has resumed execution with the final path; Task 2 can now run.
  </done>
  <resume-signal>Type "ready: ../dictus-brand/&lt;actual-path&gt;/appicon-desktop-1024.png" (replacing with the actual path used), or type "blocked: &lt;reason&gt;" if the asset cannot be produced right now.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Regenerate src-tauri/icons/ via tauri icon from new square source (ICON-01, ICON-04)</name>
  <files>
    - src-tauri/icons/ (multiple files regenerated)
  </files>
  <read_first>
    - src-tauri/icons/ (current inventory — must know what existed before regeneration for diff review)
    - .planning/phases/06-brand-icon-polish/06-CONTEXT.md §"Icon generation pipeline"
    - .planning/phases/06-brand-icon-polish/06-RESEARCH.md §"Pattern 3: Icon Pipeline (ICON-01, ICON-03, ICON-04)" §"Pitfall 3"
    - `../dictus-brand/&lt;path-signaled-in-Task-1&gt;/appicon-desktop-1024.png` (source PNG added by Pierre)
  </read_first>
  <action>
    Use the Tauri CLI's icon generator to regenerate the full platform icon set from the new square source. The command is idempotent — re-running overwrites existing files.

    1. **Pre-flight checks** (abort if any fail):
       - Confirm source exists and is 1024×1024 RGBA:
         `SOURCE=../dictus-brand/<path-from-Task-1>/appicon-desktop-1024.png`
         `test -f "$SOURCE" || { echo "source missing: $SOURCE"; exit 1; }`
         `identify -format "%wx%h\n" "$SOURCE"` must print `1024x1024`
       - Confirm corner alpha is 0 on all four corners:
         `identify -format "%[fx:p{0,0}.a] %[fx:p{1023,0}.a] %[fx:p{0,1023}.a] %[fx:p{1023,1023}.a]\n" "$SOURCE"` must print `0 0 0 0`
       - If either check fails, stop and surface the issue — do NOT proceed with regeneration.

    2. **Snapshot the current directory** for diff review:
       `ls src-tauri/icons/ > /tmp/icons-before.txt`

    3. **Run the Tauri icon generator:**
       `bun run tauri icon "$SOURCE"`

       This command:
       - Regenerates `src-tauri/icons/icon.icns` (macOS)
       - Regenerates `src-tauri/icons/icon.ico` (Windows, multi-layer)
       - Regenerates `src-tauri/icons/32x32.png`, `128x128.png`, `128x128@2x.png` (Linux + iOS)
       - May also regenerate `icon.png`, `Square*Logo.png` variants, and `StoreLogo.png`
       - Does NOT touch `src-tauri/icons/android/` or `src-tauri/icons/ios/` directories unless additional flags are passed — leave them as-is

    4. **Inventory the output:**
       `ls src-tauri/icons/ > /tmp/icons-after.txt`
       `diff /tmp/icons-before.txt /tmp/icons-after.txt` — expect new files possibly including `256x256.png` and `512x512.png`, and modified existing files. Record the list of generated files for Task 3.

    5. **Verify corner transparency was preserved through regeneration** (critical for ICON-01):
       `identify -format "%[fx:p{0,0}.a]\n" src-tauri/icons/128x128@2x.png` — must print `0` (or `nan` if the PNG has no alpha channel, which means FAIL)
       `identify -format "%[fx:p{0,0}.a]\n" src-tauri/icons/32x32.png` — must print `0`

    6. **Verify Windows ICO layers** (ICON-02 confirmation):
       `identify -format '%w\n' src-tauri/icons/icon.ico | sort -u`
       Expected output contains all of: `16`, `24`, `32`, `48`, `64`, `256`. Per RESEARCH.md this is already passing on the current ICO; after regeneration it must still pass.

    7. **Address RESEARCH.md Pitfall 3 (uncertain `256x256.png` / `512x512.png` naming):**
       If `tauri icon` DID generate files literally named `256x256.png` and `512x512.png`, record that and proceed to Task 3.
       If `tauri icon` did NOT generate those exact filenames, generate them manually from the source using ImageMagick to give Task 3 predictable targets:
       ```
       test -f src-tauri/icons/256x256.png || convert "$SOURCE" -resize 256x256 src-tauri/icons/256x256.png
       test -f src-tauri/icons/512x512.png || convert "$SOURCE" -resize 512x512 src-tauri/icons/512x512.png
       ```
       After this, both files must exist at exactly those names; record in summary whether they came from `tauri icon` or ImageMagick fallback.

    8. **Run verify-sync.sh ICON-02a check:**
       `bash .github/scripts/verify-sync.sh 2>&1 | grep 'ICON-02a'`
       Expected: `ok  ICON-02a icon.ico contains all 6 required layer sizes (16,24,32,48,64,256)`

    Do NOT:
    - Edit individual generated PNGs by hand — `tauri icon` and ImageMagick are the only writers of `src-tauri/icons/*`
    - Touch `src-tauri/icons/android/` or `src-tauri/icons/ios/` — mobile assets are out of scope for this phase
    - Delete unused PNGs from the directory in this task (cleanup is optional and can be a separate commit)
    - Update `tauri.conf.json` yet — that is Task 3's scope
    - Reuse `src-tauri/icons/logo.png` as source (per RESEARCH.md Pitfall 7, use dictus-brand source for provenance regardless of `logo.png` corner state)
  </action>
  <verify>
    <automated>test -f src-tauri/icons/icon.ico && test -f src-tauri/icons/icon.icns && test -f src-tauri/icons/256x256.png && test -f src-tauri/icons/512x512.png && [ "$(identify -format '%w\n' src-tauri/icons/icon.ico | sort -u | grep -cE '^(16|24|32|48|64|256)$')" -eq 6 ] && [ "$(identify -format '%[fx:p{0,0}.a]\n' src-tauri/icons/128x128@2x.png)" = "0" ] && bash .github/scripts/verify-sync.sh 2>&1 | grep -q 'ok  ICON-02a'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src-tauri/icons/icon.ico` returns 0
    - `test -f src-tauri/icons/icon.icns` returns 0
    - `test -f src-tauri/icons/32x32.png` returns 0
    - `test -f src-tauri/icons/128x128.png` returns 0
    - `test -f src-tauri/icons/128x128@2x.png` returns 0
    - `test -f src-tauri/icons/256x256.png` returns 0 (from tauri icon OR ImageMagick fallback — summary must state which)
    - `test -f src-tauri/icons/512x512.png` returns 0 (from tauri icon OR ImageMagick fallback — summary must state which)
    - `identify -format '%w\n' src-tauri/icons/icon.ico | sort -u | grep -cE '^(16|24|32|48|64|256)$'` returns exactly `6`
    - `identify -format "%[fx:p{0,0}.a]\n" src-tauri/icons/128x128@2x.png` returns `0` (top-left corner transparent)
    - `identify -format "%[fx:p{0,0}.a]\n" src-tauri/icons/32x32.png` returns `0` (top-left corner transparent)
    - `identify -format "%wx%h\n" src-tauri/icons/256x256.png` returns `256x256`
    - `identify -format "%wx%h\n" src-tauri/icons/512x512.png` returns `512x512`
    - `bash .github/scripts/verify-sync.sh 2>&1 | grep -q 'ok  ICON-02a'` returns 0
    - `ls src-tauri/icons/android/` and `ls src-tauri/icons/ios/` directories still exist untouched (mobile assets not regenerated — compare against `/tmp/icons-before.txt`)
  </acceptance_criteria>
  <done>Icon set regenerated from square transparent 1024px source; 256×256 and 512×512 files present; Linux corner transparency confirmed; Windows ICO still has all 6 required layers; ICON-02a verify-sync check passes.</done>
</task>

<task type="auto">
  <name>Task 3: Extend tauri.conf.json bundle.icon array with 256×256 and 512×512 entries (ICON-03)</name>
  <files>
    - src-tauri/tauri.conf.json
  </files>
  <read_first>
    - src-tauri/tauri.conf.json (current bundle.icon array — 5 entries)
    - src-tauri/icons/ (actual filenames generated by Task 2 — especially whether 256x256.png and 512x512.png exist with those exact names)
    - .planning/phases/06-brand-icon-polish/06-CONTEXT.md §"Icon generation pipeline" ("tauri.conf.json > bundle.icon list")
    - .planning/phases/06-brand-icon-polish/06-RESEARCH.md §"Pattern 3: Icon Pipeline" §"Pitfall 3"
  </read_first>
  <action>
    Extend the `bundle.icon` array in `src-tauri/tauri.conf.json` to include two new PNG entries so Linux bundlers explicitly ship 256×256 and 512×512 variants.

    **Before edit** — the array is (verified 2026-04-15):
    ```json
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
    ```

    **After edit** — the array must be:
    ```json
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/256x256.png",
      "icons/512x512.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
    ```

    **Constraints on the edit:**
    - Only touch the `"icon": [...]` array inside `bundle`. Leave productName, version, identifier, plugins.updater, macOS signing, Linux deb/rpm/appimage config, Windows nsis config, etc., EXACTLY as they are.
    - Preserve JSON formatting: 2-space indentation, trailing-comma-less array.
    - After editing, run `jq . src-tauri/tauri.conf.json > /dev/null` to confirm the file is still valid JSON.

    **Validate the bundler can find all listed icons:**
    ```
    for f in $(jq -r '.bundle.icon[]' src-tauri/tauri.conf.json); do
      test -f src-tauri/$f || { echo "MISSING: src-tauri/$f"; exit 1; }
    done
    echo "All 7 bundle.icon entries exist."
    ```

    **Run cargo build to confirm the bundler accepts the config:**
    ```
    cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -20
    ```
    Expected: build succeeds. If it fails complaining about a missing icon file, Task 2 did not generate that file — go back and fix.

    **Run the full verify-sync.sh suite one more time to confirm Phase 6 is fully green:**
    ```
    bash .github/scripts/verify-sync.sh
    ```
    Expected: all 15 checks pass; script exits 0 (assuming Plans 06-02 and 06-03 have also completed; if they haven't, BRAND-01a/02a/03a may still be red — in that case re-run after they finish).

    Do NOT:
    - Re-order existing entries (only append the two new ones in the position shown above — between `128x128@2x.png` and `icon.icns`)
    - Change array syntax to a different JSON style
    - Touch unrelated config blocks
    - Add Linux-only or Windows-only nested overrides (keep the single flat array)
  </action>
  <verify>
    <automated>jq -e '.bundle.icon | length == 7 and any(. == "icons/256x256.png") and any(. == "icons/512x512.png")' src-tauri/tauri.conf.json && for f in $(jq -r '.bundle.icon[]' src-tauri/tauri.conf.json); do test -f src-tauri/$f || exit 1; done && cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -3 | grep -qE 'Finished|warning' && bash .github/scripts/verify-sync.sh</automated>
  </verify>
  <acceptance_criteria>
    - `jq -r '.bundle.icon | length' src-tauri/tauri.conf.json` returns `7`
    - `jq -r '.bundle.icon[]' src-tauri/tauri.conf.json | grep -q '^icons/256x256.png$'` returns 0
    - `jq -r '.bundle.icon[]' src-tauri/tauri.conf.json | grep -q '^icons/512x512.png$'` returns 0
    - `jq -r '.bundle.icon[]' src-tauri/tauri.conf.json | grep -q '^icons/icon.icns$'` returns 0 (preserved)
    - `jq -r '.bundle.icon[]' src-tauri/tauri.conf.json | grep -q '^icons/icon.ico$'` returns 0 (preserved)
    - `jq -r '.productName' src-tauri/tauri.conf.json` returns `"Dictus"` (unchanged)
    - `jq -r '.identifier' src-tauri/tauri.conf.json` returns `"com.dictus.desktop"` (unchanged)
    - `jq -r '.plugins.updater.endpoints[0]' src-tauri/tauri.conf.json` still contains `getdictus/dictus-desktop` (unchanged)
    - Every file in `.bundle.icon` exists under `src-tauri/`: the for-loop above completes without the `MISSING:` line
    - `cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -3` contains `Finished` (build succeeds)
    - Running `bash .github/scripts/verify-sync.sh` exits 0 when ALL of Plans 06-02 + 06-03 + 06-04 have landed (this plan may finish ahead or behind 06-02/03 — if any of BRAND-01a/02a/03a red, that is an external blocker, not a failure of this plan's work)
  </acceptance_criteria>
  <done>bundle.icon array extended to 7 entries; all referenced files exist; tauri.conf.json is valid JSON with untouched productName/identifier/updater blocks; cargo build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 (CHECKPOINT): Pierre verifies rendered icons on Linux and Windows</name>
  <files>
    - src-tauri/target/release/bundle/deb/ (Linux build output, inspected visually)
    - src-tauri/target/release/bundle/msi/ (Windows build output, inspected visually)
  </files>
  <read_first>
    - src-tauri/icons/icon.ico (after Task 2 regeneration — for cross-reference)
    - src-tauri/tauri.conf.json (after Task 3 update — confirms the icon list shipped to bundlers)
  </read_first>
  <action>
    **This is a human-verify checkpoint. Claude cannot render OS shell UI across platforms — Pierre must install and visually inspect.**

    Pierre should perform these build + install + visual checks on the target platforms:

    **Linux verification (ICON-01 — black corners fix):**
    1. Build a Linux package: `bun run tauri build --bundles deb appimage` (from a Linux machine or VM)
    2. Install the deb (or run the AppImage): `sudo dpkg -i src-tauri/target/release/bundle/deb/dictus_*.deb`
    3. Open the application launcher / GNOME Activities / KDE Kickoff and locate the Dictus icon
    4. Inspect at 32×32, 48×48, 64×64, 128×128 scales (most desktops show several sizes)
    5. **Pass criterion:** No black square in any corner; the icon's transparent background shows the desktop theme behind it, and the waveform glyph is centered on a square canvas
    6. If the icon still shows black corners at any scale, the source PNG is not transparent enough — go back to Task 1 and verify the source

    **Windows verification (ICON-02 — multi-layer ICO):**
    1. Build a Windows MSI: `bun run tauri build --bundles msi` (from a Windows machine)
    2. Install the MSI and pin the app to the taskbar
    3. Inspect the icon at 100%, 125%, 150%, and 200% display scaling (Settings → System → Display → Scale)
    4. Right-click the `Dictus.exe` in the install directory → Properties → the icon in the header
    5. **Pass criterion:** At every scale, the Dictus logo is sharp — no pixelated scaling, no incorrect layer fallback
    6. Optionally inspect with Resource Hacker or `identify -format '%w %h\n' icon.ico` to confirm all six layers (16, 24, 32, 48, 64, 256)

    **macOS sanity (no regression):**
    1. Build: `bun run tauri build --bundles dmg`
    2. Install and pin to dock
    3. **Pass criterion:** Icon has rounded corners applied by macOS (as before) and the waveform glyph looks correct; no visual regression from the previous icon

    **DebugPaths panel sanity (BRAND-03, belongs to Plan 06-03 but easy to check here):**
    1. Run `bun run tauri dev` on any platform
    2. Settings → Debug Paths
    3. Confirm the app-data path shown is the real OS path (e.g., macOS: `/Users/<name>/Library/Application Support/com.dictus.desktop`) — not `%APPDATA%/handy`

    **History panel sanity (BRAND-01, belongs to Plan 06-02 but easy to check here):**
    1. Record a short clip in the running dev build
    2. Open the History panel
    3. Confirm the filename starts with `dictus-` (not `handy-`)

    Pierre signals approval or flags issues via the resume-signal.
  </action>
  <what-built>
    - A new 1024×1024 square RGBA PNG committed to dictus-brand as the desktop icon source-of-truth (ICON-04)
    - Regenerated `src-tauri/icons/` from that source — `icon.ico` (6 layers), `icon.icns`, PNGs at 32/128/256/512, Windows Square*Logo variants (ICON-01, ICON-02)
    - Extended `tauri.conf.json bundle.icon` array with explicit 256×256 and 512×512 entries (ICON-03)
    - verify-sync.sh ICON-02a assertion green (automated check; human verification below is for visual fidelity that automation cannot confirm)
  </what-built>
  <how-to-verify>
    See the step-by-step per-platform instructions in the `<action>` block above. Pierre verifies that:
    1. Linux icon has NO black corners at any scale (ICON-01)
    2. Windows icon is sharp at 100/125/150/200% display scaling (ICON-02)
    3. macOS icon shows no regression (rounded corners still applied by OS, waveform glyph correct)
    4. Optional cross-plan sanity: DebugPaths shows real path (BRAND-03), history uses dictus- prefix (BRAND-01)
  </how-to-verify>
  <verify>
    <automated>identify -format '%w\n' src-tauri/icons/icon.ico | sort -u | grep -cE '^(16|24|32|48|64|256)$' | grep -q '^6$'</automated>
  </verify>
  <acceptance_criteria>
    - Pierre has typed "approved" in response to the resume-signal OR has flagged a specific issue via "issue: ..."
    - If "approved": Linux shows no black corners at any scale; Windows icon is sharp at all scaling levels; macOS shows no regression
    - Automated backstop: `identify` still reports all 6 ICO layers present post-verification (Task 2's regeneration was not accidentally reverted)
  </acceptance_criteria>
  <done>
    Pierre has visually confirmed icon rendering on Linux, Windows, and macOS (as applicable to his dev environment); any rendering issues are resolved before committing the plan as complete.
  </done>
  <resume-signal>Type "approved" if Linux icon renders clean with no black corners AND Windows icon renders sharp at all scales AND macOS shows no regression. Type "issue: &lt;description&gt;" to flag a problem and request remediation.</resume-signal>
</task>

</tasks>

<verification>
After all four tasks complete and Pierre has approved the visual checkpoint:

1. `test -f src-tauri/icons/icon.ico && test -f src-tauri/icons/256x256.png && test -f src-tauri/icons/512x512.png`
2. `identify -format '%w\n' src-tauri/icons/icon.ico | sort -u | grep -cE '^(16|24|32|48|64|256)$'` returns 6
3. `jq -r '.bundle.icon | length' src-tauri/tauri.conf.json` returns 7
4. `cargo build --manifest-path src-tauri/Cargo.toml` succeeds
5. `bash .github/scripts/verify-sync.sh` exits 0 (all 15 checks green, assuming 06-02 and 06-03 have also merged)
6. Pierre has signed off on Linux (no black corners) and Windows (sharp at all scales) via the checkpoint

Commit suggestions:
- `chore(icons): regenerate from square transparent source (ICON-01, ICON-02, ICON-04)`
- `chore(icons): list 256x256 and 512x512 PNGs in bundle.icon (ICON-03)`
</verification>

<success_criteria>
- New 1024×1024 square RGBA source PNG committed to dictus-brand (ICON-04)
- Linux icon renders with transparent corners at every bundler scale (ICON-01) — verified by Pierre
- Windows .exe embeds a multi-layer ICO with all 6 required sizes (16/24/32/48/64/256) (ICON-02)
- `tauri.conf.json bundle.icon` array lists 256×256 and 512×512 PNGs explicitly (ICON-03)
- `cargo build` succeeds with the new config
- verify-sync.sh ICON-02a assertion passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-icon-polish/06-04-SUMMARY.md` following the standard summary template. Record:
- The final path used in dictus-brand for the 1024 source PNG
- Whether tauri icon generated 256x256.png / 512x512.png directly or whether ImageMagick fallback was used
- Pierre's visual sign-off status for Linux and Windows icons
- Any residual mobile icon cleanup (android/, ios/) left as future work
</output>
