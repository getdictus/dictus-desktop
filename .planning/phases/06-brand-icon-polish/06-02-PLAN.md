---
phase: 06-brand-icon-polish
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src-tauri/src/actions.rs
  - src-tauri/src/managers/history.rs
  - src-tauri/src/tray.rs
  - src-tauri/src/portable.rs
autonomous: true
requirements: [BRAND-01, BRAND-02]
must_haves:
  truths:
    - "New recordings produced by the app are named dictus-{timestamp}.wav (not handy-{timestamp}.wav)"
    - "The portable mode marker file content is the literal string 'Dictus Portable Mode'"
    - "The portable-mode detector recognizes 'Dictus Portable Mode' as the magic string; the literal 'Handy Portable Mode' string is absent from the source"
    - "All existing cargo tests pass after the string replacements"
    - "The portable module doc comment references Dictus, not Handy"
    - "Running verify-sync.sh now passes the BRAND-01a and BRAND-02a assertions"
  artifacts:
    - path: "src-tauri/src/actions.rs"
      provides: "Recording filename format producing dictus- prefix"
      contains: "dictus-"
    - path: "src-tauri/src/portable.rs"
      provides: "Portable-mode write + detect using 'Dictus Portable Mode'"
      contains: "Dictus Portable Mode"
    - path: "src-tauri/src/managers/history.rs"
      provides: "Test fixtures using dictus- filenames"
      contains: "dictus-"
    - path: "src-tauri/src/tray.rs"
      provides: "Tray test fixture using dictus-1.wav"
      contains: "dictus-1.wav"
  key_links:
    - from: "src-tauri/src/actions.rs:538"
      to: "src-tauri/src/managers/history.rs (DB row file_name column)"
      via: "format!(\"dictus-{}.wav\", ...) passed to hm.recordings_dir()"
      pattern: "format!\\(\"dictus-\\{\\}\\.wav\""
    - from: "src-tauri/src/portable.rs:30 (write marker)"
      to: "src-tauri/src/portable.rs:98 (is_valid_portable_marker read)"
      via: "Marker file content matches same literal string"
      pattern: "Dictus Portable Mode"
---

<objective>
Replace the last two user-visible Handy string literals in the Rust backend: (1) the recording filename prefix `handy-{timestamp}.wav` → `dictus-{timestamp}.wav` (BRAND-01, at actions.rs:538 plus two test fixtures), and (2) the portable-mode marker `"Handy Portable Mode"` → `"Dictus Portable Mode"` (BRAND-02, straight string replace in portable.rs with no dual-read fallback, per CONTEXT.md simplified decision).

Purpose: These are the last two source-visible Handy leaks that the BRAND-01a and BRAND-02a verify-sync assertions (added by Plan 06-01) check for. Once this plan completes, new recordings and portable installs speak pure Dictus.

Output: Updated Rust source with zero `"handy-"` or `"Handy Portable Mode"` strings (outside the `handy_keys` external crate reference); all existing tests pass; verify-sync.sh BRAND-01a and BRAND-02a checks turn green.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-brand-icon-polish/06-CONTEXT.md
@.planning/phases/06-brand-icon-polish/06-RESEARCH.md
@.planning/phases/06-brand-icon-polish/06-VALIDATION.md
@.github/scripts/verify-sync.sh
@src-tauri/src/actions.rs
@src-tauri/src/portable.rs
@src-tauri/src/managers/history.rs
@src-tauri/src/tray.rs

<interfaces>
<!-- Exact current code state, read directly from the files (2026-04-15): -->

src-tauri/src/actions.rs line 538:
```rust
let file_name = format!("handy-{}.wav", chrono::Utc::now().timestamp());
```

src-tauri/src/portable.rs, relevant segments:
```rust
// Line 5 (doc comment):
/// Portable mode support for Handy.

// Line 30 (legacy-marker upgrade branch inside init()):
let _ = std::fs::write(&marker_path, "Handy Portable Mode");

// Line 98 (is_valid_portable_marker helper):
.map(|s| s.trim().starts_with("Handy Portable Mode"))

// Tests at lines 107-165 use six temp dir names:
// handy_test_valid, handy_test_empty, handy_test_wrong,
// handy_test_legacy_no_data, handy_test_ws
// (test_missing_file uses a hardcoded nonexistent path — no dir rename needed there)
// Tests also use write!(f, "Handy Portable Mode") and write!(f, "  Handy Portable Mode\n")
```

src-tauri/src/managers/history.rs line 689 (inside a #[cfg(test)] helper):
```rust
format!("handy-{}.wav", timestamp),
```

src-tauri/src/tray.rs line 273 (inside #[cfg(test)] build_entry helper):
```rust
file_name: "handy-1.wav".to_string(),
```

The `handy_keys` external crate reference appears in `src-tauri/src/lib.rs` and `src-tauri/src/shortcut/handy_keys.rs` — DO NOT TOUCH these; TECH-01 is deferred. The BRAND-01a assertion in verify-sync.sh excludes this crate via `grep -v handy_keys`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace handy- filename prefix in actions.rs and test fixtures (BRAND-01)</name>
  <files>
    - src-tauri/src/actions.rs
    - src-tauri/src/managers/history.rs
    - src-tauri/src/tray.rs
  </files>
  <read_first>
    - src-tauri/src/actions.rs (lines 530-545 — recording save block)
    - src-tauri/src/managers/history.rs (lines 680-700 — test insert helper)
    - src-tauri/src/tray.rs (lines 265-285 — build_entry test helper)
    - .github/scripts/verify-sync.sh (BRAND-01a assertion — defines what "done" looks like)
    - .planning/phases/06-brand-icon-polish/06-CONTEXT.md §"Recording filename (BRAND-01)"
  </read_first>
  <behavior>
    After this task:
    - `format!("dictus-{}.wav", timestamp)` produces new recording filenames
    - `src-tauri/src/managers/history.rs` test helper inserts rows with `format!("dictus-{}.wav", timestamp)` — existing tests still pass
    - `src-tauri/src/tray.rs` `build_entry` test helper returns entries with `file_name: "dictus-1.wav"` — existing tests still pass
    - No production-code path emits `"handy-"` as a filename prefix anywhere under `src-tauri/src/` (the `handy_keys` crate is excluded)
  </behavior>
  <action>
    Perform three targeted single-line edits. Do NOT refactor surrounding code; these are straight literal replacements.

    **Edit 1 — src-tauri/src/actions.rs line 538:**
    Replace:
    ```rust
    let file_name = format!("handy-{}.wav", chrono::Utc::now().timestamp());
    ```
    With:
    ```rust
    let file_name = format!("dictus-{}.wav", chrono::Utc::now().timestamp());
    ```

    **Edit 2 — src-tauri/src/managers/history.rs line 689 (inside a #[cfg(test)] insert helper):**
    Replace:
    ```rust
    format!("handy-{}.wav", timestamp),
    ```
    With:
    ```rust
    format!("dictus-{}.wav", timestamp),
    ```

    **Edit 3 — src-tauri/src/tray.rs line 273 (inside #[cfg(test)] build_entry helper):**
    Replace:
    ```rust
    file_name: "handy-1.wav".to_string(),
    ```
    With:
    ```rust
    file_name: "dictus-1.wav".to_string(),
    ```

    After edits, run cargo tests to confirm nothing regressed:
    ```
    cargo test --manifest-path src-tauri/Cargo.toml 2>&1 | tail -30
    ```

    Then run the BRAND-01a check:
    ```
    bash .github/scripts/verify-sync.sh 2>&1 | grep 'BRAND-01a'
    ```
    It should print `ok  BRAND-01a no handy- filename prefix in src-tauri (except handy_keys crate)`.

    Do NOT:
    - Rename existing `handy-*.wav` files on disk (deferred per CONTEXT.md Deferred Ideas)
    - Change any other `handy` reference in these files (e.g., the deferred binary name)
    - Modify test logic in `history.rs` or `tray.rs` beyond the literal-string update above
    - Touch `src-tauri/src/shortcut/handy_keys.rs` or anything in `lib.rs` referencing the `handy_keys` crate
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml --lib 2>&1 | tail -5 && bash .github/scripts/verify-sync.sh 2>&1 | grep -q 'ok  BRAND-01a' && ! (grep -rn 'handy-' src-tauri/src/ | grep -v handy_keys | grep -q .)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'format!("dictus-{}.wav"' src-tauri/src/actions.rs` returns 0
    - `grep -q 'format!("handy-{}.wav"' src-tauri/src/actions.rs` returns 1 (no remaining handy- format call)
    - `grep -q 'format!("dictus-{}.wav", timestamp)' src-tauri/src/managers/history.rs` returns 0
    - `grep -q '"dictus-1.wav".to_string()' src-tauri/src/tray.rs` returns 0
    - `grep -rn 'handy-' src-tauri/src/ | grep -v handy_keys` produces NO matches (overall pipeline exit code 1 = no lines)
    - `cargo test --manifest-path src-tauri/Cargo.toml --lib 2>&1 | tail -3` shows `test result: ok.` (no regressions)
    - Running `bash .github/scripts/verify-sync.sh 2>&1 | grep -q 'ok  BRAND-01a'` returns 0
  </acceptance_criteria>
  <done>All `handy-` filename strings in `src-tauri/src/` are replaced (except legitimate `handy_keys` crate refs); cargo tests green; BRAND-01a verify-sync check passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace "Handy Portable Mode" literal and test dirs in portable.rs (BRAND-02)</name>
  <files>
    - src-tauri/src/portable.rs
  </files>
  <read_first>
    - src-tauri/src/portable.rs (full file — must see all occurrences lines 5, 30, 98, 107-165)
    - .github/scripts/verify-sync.sh (BRAND-02a assertion pattern)
    - .planning/phases/06-brand-icon-polish/06-CONTEXT.md §"Portable mode marker (BRAND-02) — simplified"
    - .planning/phases/06-brand-icon-polish/06-RESEARCH.md §"Pattern 1: Rust String Replacement (BRAND-01, BRAND-02)"
  </read_first>
  <behavior>
    After this task:
    - `init()` writes `"Dictus Portable Mode"` when upgrading a legacy empty marker file
    - `is_valid_portable_marker()` returns true when the marker content begins with `"Dictus Portable Mode"`
    - All 6 existing portable tests continue to pass with the new string + renamed test dirs
    - The source file contains ZERO occurrences of `"Handy Portable Mode"` (as a string literal or in comments)
    - The module doc comment reads "Portable mode support for Dictus."
    - All test temp-dir names start with `dictus_test_` instead of `handy_test_`
    - NO dual-read logic is added — if a user has an existing `"Handy Portable Mode"` marker, portable mode will no longer activate (explicitly accepted per CONTEXT.md: Pierre is sole user, no installed base to protect)
  </behavior>
  <action>
    Perform a set of focused string replacements across `src-tauri/src/portable.rs`. All edits are literal substitutions; no control-flow or signature changes.

    **Edit 1 — Line 5 doc comment:**
    Replace:
    ```rust
    /// Portable mode support for Handy.
    ```
    With:
    ```rust
    /// Portable mode support for Dictus.
    ```

    **Edit 2 — Line 30 (inside `init()` legacy-marker upgrade branch):**
    Replace:
    ```rust
    let _ = std::fs::write(&marker_path, "Handy Portable Mode");
    ```
    With:
    ```rust
    let _ = std::fs::write(&marker_path, "Dictus Portable Mode");
    ```

    **Edit 3 — Line 98 (inside `is_valid_portable_marker()`):**
    Replace:
    ```rust
    .map(|s| s.trim().starts_with("Handy Portable Mode"))
    ```
    With:
    ```rust
    .map(|s| s.trim().starts_with("Dictus Portable Mode"))
    ```

    **Edit 4 — Test dir names (inside `#[cfg(test)] mod tests`, lines 107-165):**

    Apply these exact renames (case-sensitive):
    - `handy_test_valid` → `dictus_test_valid`
    - `handy_test_empty` → `dictus_test_empty`
    - `handy_test_wrong` → `dictus_test_wrong`
    - `handy_test_legacy_no_data` → `dictus_test_legacy_no_data`
    - `handy_test_ws` → `dictus_test_ws`

    **Edit 5 — Test body literal strings (inside the same `#[cfg(test)] mod tests`):**

    Replace both `write!` forms:
    - `write!(f, "Handy Portable Mode").unwrap();` → `write!(f, "Dictus Portable Mode").unwrap();`
    - `write!(f, "  Handy Portable Mode\n").unwrap();` → `write!(f, "  Dictus Portable Mode\n").unwrap();`

    Leave assertions (`assert!(is_valid_portable_marker(...))`, `assert!(!is_valid_portable_marker(...))`) structurally unchanged — the helper now matches the new string, so semantics are preserved.

    **Explicit override to REQUIREMENTS.md:**
    - REQUIREMENTS.md BRAND-02 says "still recognizing legacy `Handy Portable Mode` on existing installs."
    - CONTEXT.md overrides this: "straight string replace. No dual-read, no legacy recognition." Pierre is the sole user; there is no installed base to protect.
    - DO NOT add a fallback `.starts_with("Handy Portable Mode")` branch.
    - DO NOT introduce a migration that rewrites existing `"Handy Portable Mode"` markers on startup.

    After edits:
    1. `cargo test --manifest-path src-tauri/Cargo.toml portable 2>&1 | tail -20` — all 6 portable tests should pass
    2. `bash .github/scripts/verify-sync.sh 2>&1 | grep 'BRAND-02a'` — should print `ok  BRAND-02a ...`

    Do NOT:
    - Modify the `PORTABLE_DATA_DIR` static, `init()` control flow, or any function signature
    - Rename the `is_valid_portable_marker` function
    - Update translation files — portable-mode magic strings are filesystem content, not user-visible i18n strings
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml portable 2>&1 | tail -5 && bash .github/scripts/verify-sync.sh 2>&1 | grep -q 'ok  BRAND-02a' && ! grep -q '"Handy Portable Mode"' src-tauri/src/portable.rs && ! grep -q 'handy_test_' src-tauri/src/portable.rs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"Dictus Portable Mode"' src-tauri/src/portable.rs` returns 0
    - `grep -q '"Handy Portable Mode"' src-tauri/src/portable.rs` returns 1 (literal absent)
    - `grep -q '/// Portable mode support for Dictus' src-tauri/src/portable.rs` returns 0
    - `grep -q '/// Portable mode support for Handy' src-tauri/src/portable.rs` returns 1 (old doc comment absent)
    - `grep -q "starts_with(\"Dictus Portable Mode\")" src-tauri/src/portable.rs` returns 0
    - `grep -c 'dictus_test_' src-tauri/src/portable.rs` returns 5 (five renamed test dirs)
    - `grep -q 'handy_test_' src-tauri/src/portable.rs` returns 1 (no old test dir names remain)
    - `cargo test --manifest-path src-tauri/Cargo.toml portable 2>&1 | tail -3` shows `test result: ok.` with 6 tests passing
    - `bash .github/scripts/verify-sync.sh 2>&1 | grep -q 'ok  BRAND-02a'` returns 0
  </acceptance_criteria>
  <done>All "Handy Portable Mode" literals and handy_test_ dir names replaced; doc comment updated; 6 portable tests green; BRAND-02a verify-sync check passes.</done>
</task>

</tasks>

<verification>
After both tasks complete, the following must hold:

1. `! grep -rn 'handy-' src-tauri/src/ | grep -v handy_keys | grep -q .` (no `handy-` filename prefix anywhere except in the `handy_keys` crate reference)
2. `! grep -q '"Handy Portable Mode"' src-tauri/src/portable.rs`
3. `cargo test --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5` shows all tests passing
4. `bash .github/scripts/verify-sync.sh 2>&1 | grep -E 'BRAND-01a|BRAND-02a'` shows both as `ok`

Commit suggestion: `chore(brand): replace handy recording prefix and portable marker with dictus (BRAND-01, BRAND-02)`
</verification>

<success_criteria>
- New recordings ship with `dictus-{timestamp}.wav` filenames (BRAND-01)
- Portable-mode marker uses `"Dictus Portable Mode"`; no dual-read path (BRAND-02)
- All 6 portable tests pass; all test fixtures use dictus- strings
- Module doc comment says "Dictus"
- verify-sync.sh BRAND-01a and BRAND-02a assertions both pass
- `handy_keys` external crate untouched (TECH-01 still deferred)
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-icon-polish/06-02-SUMMARY.md` following the standard summary template. Note the deliberate non-implementation of legacy-read fallback (dual-read overridden in CONTEXT.md).
</output>
