---
phase: 10-prerequisite-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - UPSTREAM.md
  - .github/upstream-sha.txt
  - CLAUDE.md
  - AGENTS.md
  - src-tauri/Cargo.lock
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - package.json
  - src-tauri/src/settings.rs
  - src-tauri/src/lib.rs
autonomous: false
requirements: [PREP-03]
user_setup: []

must_haves:
  truths:
    - "Upstream Sync #2 (17-commit delta) is merged with AWS Bedrock neutralized via git revert aee682f"
    - "verify-sync.sh (15 identity assertions) exits 0 after the merge"
    - "cargo build succeeds on the merged tree"
    - "Dictus version 0.1.3 is preserved (not bumped to upstream v0.8.3)"
    - "Dictus CLAUDE.md is preserved verbatim; upstream AGENTS.md is accepted as a new file"
    - "UPSTREAM.md documents the Fork Policy: Selective Cherry-Pick transition with an Exclusion Log; aee682f is exclusion entry #1"
    - "All 7 CI platforms are green on main-build.yml"
  artifacts:
    - path: ".github/upstream-sha.txt"
      provides: "Updated 40-char upstream HEAD SHA"
      contains: "40-char SHA (not fdc8cb712dc247931099359a2d3bc8a413cf33ec)"
    - path: "UPSTREAM.md"
      provides: "Fork Policy + Exclusion Log section"
      contains: "## Fork Policy: Selective Cherry-Pick"
    - path: "src-tauri/tauri.conf.json"
      provides: "Preserved Dictus identity and version"
      contains: "0.1.3"
  key_links:
    - from: "merge commit"
      to: ".github/upstream-sha.txt"
      via: "upstream-sha.txt updated only in the merge commit that lands on the working branch"
      pattern: "40-char SHA"
    - from: "UPSTREAM.md Exclusion Log"
      to: "aee682f (AWS Bedrock)"
      via: "first exclusion entry"
      pattern: "aee682f"
---

<objective>
Merge the 17-commit upstream delta (from synced SHA `fdc8cb712dc247931099359a2d3bc8a413cf33ec` to upstream HEAD), neutralize the AWS Bedrock provider via `git revert aee682f`, preserve Dictus identity through 9+ documented hot zones, and transition the fork policy to selective cherry-pick.

Purpose: PREP-03 — bring the fork current one last time via bulk merge, record the policy shift to selective cherry-pick going forward, and document the Bedrock exclusion. This unblocks all v1.3 feature work on an up-to-date base. This is the LAST planned bulk catch-up merge.
Output: Merged sync branch, updated `UPSTREAM.md` (Fork Policy + Exclusion Log), updated `upstream-sha.txt`, `verify-sync.sh` green, all 7 CI platforms green.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-prerequisite-gate/10-CONTEXT.md
@.planning/phases/10-prerequisite-gate/10-RESEARCH.md
@UPSTREAM.md
@.github/scripts/verify-sync.sh
@.github/upstream-sha.txt
@.planning/phases/05-upstream-sync/05-CONTEXT.md

<facts>
<!-- Verified values from 10-RESEARCH.md — use directly, do not re-derive. -->

Current synced SHA (in .github/upstream-sha.txt): fdc8cb712dc247931099359a2d3bc8a413cf33ec
Upstream remote: upstream → https://github.com/cjpais/Handy.git
Working branch: feat/v1.3-smart-modes
Sync branch naming: upstream/sync-2026-05-29

17-COMMIT DELTA (oldest-first, verified via git log):
  1. f26fe0d  Fix (linux) overlay problem in kde (#1121)            — Accept (overlay.rs)
  2. 0392b7b  docs(readme): Linux startup troubleshooting (#1266)   — Accept
  3. 966ff99  query gpu async (#1246)                               — Accept; FLAG for Phase 11 (changes lib.rs, shortcut/mod.rs, bindings.ts)
  4. 11311be  fix(overlay): parse HANDY_NO_GTK_LAYER_SHELL bool     — Accept (overlay.rs)
  5. 564fbc8  docs: unify CLAUDE.md and AGENTS.md (#1272)           — KEEP Dictus CLAUDE.md; accept AGENTS.md as new file
  6. aee682f  feat: add AWS Bedrock (Mantle) provider (#1288)       — REVERT after merge (changes only settings.rs, 10-line push block)
  7. a4d671a  fix: improve German translation (#1292)              — Accept (de/translation.json)
  8. c1e11fa  refactor(nix): rustPlatform.bindgenHook (#1255)       — Accept
  9. af6ec6c  chore(nix): bun node_modules normalization (#1256)    — Accept
 10. 4b7bb4e  docs(audio): clarify mic-init timing log (#1330)      — Accept
 11. 8346bc2  fix(nix): macOS build for nixpkgs (#1316)            — Accept (also touches build.rs — accept upstream)
 12. 085cd53  release v0.8.3                                        — KEEP Dictus version 0.1.3 (bumps tauri.conf.json, Cargo.toml, package.json)
 13. a385371  refactor(nix): cargo-tauri.hook standard phases       — Accept
 14. 1d042f3  docs(agents): actionable workflow rules (#1393)       — Accept (AGENTS.md update)
 15. e3206aa  refactor(nix): drop LD_LIBRARY_PATH wrapper (#1392)   — Accept
 16. 933a525  docs: overlay/pasting Linux workaround (#1426)        — Accept
 17. 10a4c31  docs: Portuguese translation (#1422)                  — Accept (pt/translation.json)

aee682f is commit #6 oldest-first (NOT #12 — CONTEXT.md was wrong; RESEARCH corrected it).

NEW HOT ZONES (not in original UPSTREAM.md §4):
  - CLAUDE.md (commit 564fbc8): use `git checkout --ours CLAUDE.md` — Dictus version is entirely project-specific, safe to keep ours.
  - AGENTS.md (commit 564fbc8): new upstream file, no Dictus equivalent — accept upstream as-is.
  - tauri.conf.json + Cargo.toml + package.json version (commit 085cd53): KEEP "version": "0.1.3" — reject upstream 0.8.3.
  - lib.rs (commit 966ff99): async GPU pre-warm via spawn_blocking conflicts with Dictus Phase 7/8 shutdown handlers — accept upstream's GPU pre-warm addition, KEEP Dictus shutdown handlers. FLAG 966ff99 forward to Phase 11; do NOT adopt the async accelerator API here.
  - build.rs (commit 8346bc2): adds SDKROOT/SWIFTC env overrides — accept upstream as-is (no Dictus content).

EXISTING HOT ZONES (UPSTREAM.md §4.1–4.9) — honor all, especially:
  - §4.1 tauri.conf.json: resolve field-by-field, never `git checkout --theirs`. Keep productName "Dictus", identifier "com.dictus.desktop", updater endpoint "getdictus/dictus-desktop".
  - §4.2 llm_client.rs headers: keep User-Agent "Dictus/1.0", "X-Title: Dictus", intentional Handy REFERER (lines 63–76) — do NOT "fix".
  - §4.7 Cargo.lock: NEVER hand-resolve. On conflict, take upstream's Cargo.lock then `cargo generate-lockfile` (upstream-then-regenerate).

verify-sync.sh assertions (15): SYNC-05a..05k, BRAND-01a, BRAND-02a, BRAND-03a, ICON-02a.
  SYNC-05j runs `cargo build --manifest-path src-tauri/Cargo.toml`.
  SYNC-05k checks upstream-sha.txt is full 40-char SHA.

PITFALL: upstream-sha.txt is updated ONLY in the merge commit that lands on the branch — never early from the sync branch (UPSTREAM.md §5). Updating early breaks the upstream-detection workflow.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sync branch, fetch upstream, merge 17-commit delta, resolve hot zones, revert Bedrock</name>
  <read_first>
    - UPSTREAM.md (full runbook: §4.1–4.9 hot-zone conflict guides, §5 merge steps, §6 post-merge checklist, anti-patterns section)
    - .planning/phases/05-upstream-sync/05-CONTEXT.md (prior sync decisions: branch naming, PR-to-main gate, no develop branch)
    - .github/upstream-sha.txt (current synced SHA fdc8cb712dc247931099359a2d3bc8a413cf33ec)
    - .github/scripts/verify-sync.sh (the 15 assertions that must pass)
    - src-tauri/src/llm_client.rs (lines 63–76 — intentional identity headers that must NOT be "fixed")
    - src-tauri/tauri.conf.json (current Dictus identity + version 0.1.3)
    - CLAUDE.md (Dictus project instructions — must be preserved verbatim)
  </read_first>
  <action>
    Execute the upstream sync following UPSTREAM.md §5. Concrete steps:

    1. Confirm clean working tree on `feat/v1.3-smart-modes`: `git status` must show no uncommitted changes (planning docs are on main; this work is on the working branch).
    2. `git fetch upstream`
    3. Create the dated sync branch FROM the working branch: `git checkout feat/v1.3-smart-modes && git checkout -b upstream/sync-2026-05-29`
    4. Record the upstream HEAD SHA for later (do NOT write it to upstream-sha.txt yet): `git rev-parse upstream/main` → save this full 40-char SHA.
    5. Merge: `git merge upstream/main --no-ff` (brings all 17 commits, preserves merge history).
    6. Resolve conflicts per hot zones. Use these EXACT resolutions:
       - `CLAUDE.md`: `git checkout --ours CLAUDE.md && git add CLAUDE.md` (keep Dictus version entirely — it is project-specific, not branding).
       - `AGENTS.md`: accept upstream version as-is (`git checkout --theirs AGENTS.md && git add AGENTS.md` if conflicted; it is a new upstream file with no Dictus equivalent).
       - `src-tauri/tauri.conf.json`: resolve field-by-field. KEEP "productName": "Dictus", "identifier": "com.dictus.desktop", updater endpoint containing "getdictus/dictus-desktop", and "version": "0.1.3" (reject upstream 0.8.3). NEVER `git checkout --theirs`.
       - `src-tauri/Cargo.toml`: keep `version = "0.1.3"` (reject upstream 0.8.3 bump); accept upstream non-version changes.
       - `package.json`: keep Dictus version (reject upstream 0.8.3 bump).
       - `src-tauri/src/llm_client.rs`: keep User-Agent "Dictus/1.0", "X-Title: Dictus", and the intentional Handy REFERER at lines 63–76 (do NOT "fix" to a Dictus URL — it is deliberate).
       - `src-tauri/src/lib.rs`: accept upstream's async GPU pre-warm `spawn_blocking` addition (commit 966ff99) AND keep all Dictus Phase 7/8 shutdown handlers. Do not delete Dictus shutdown logic. Do NOT adopt or wire the new async `get_available_accelerators` API beyond what is needed to compile — flag it forward to Phase 11.
       - `src-tauri/build.rs`: accept upstream as-is (SDKROOT/SWIFTC env overrides, no Dictus content).
       - `src-tauri/Cargo.lock`: do NOT hand-resolve. Take upstream's version (`git checkout --theirs src-tauri/Cargo.lock && git add src-tauri/Cargo.lock`), then after the merge completes run `cargo generate-lockfile --manifest-path src-tauri/Cargo.toml` to regenerate against the resolved Cargo.toml (UPSTREAM.md §4.7 upstream-then-regenerate).
       - All other files (overlay.rs, translation.json files, nix files, docs, README): accept upstream cleanly.
    7. Complete the merge commit: `git commit --no-edit` (or with the default merge message).
    8. Regenerate the lockfile: `cargo generate-lockfile --manifest-path src-tauri/Cargo.toml`, then `git add src-tauri/Cargo.lock && git commit -m "chore(10): regenerate Cargo.lock after upstream merge"` (only if the lockfile changed).
    9. Revert AWS Bedrock: `git revert aee682f --no-edit`. This neutralizes the `PostProcessProvider` push block in `src-tauri/src/settings.rs` (10 lines). If the revert does not apply cleanly, resolve by removing only the Bedrock `providers.push(...)` block in settings.rs and committing.
    10. Run `cargo fmt --manifest-path src-tauri/Cargo.toml` and `cargo build --manifest-path src-tauri/Cargo.toml` to confirm the tree compiles.

    Do NOT update `.github/upstream-sha.txt` in this task — that happens in Task 2 as part of the landing commit.
  </action>
  <verify>
    <automated>cargo build --manifest-path src-tauri/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline upstream/sync-2026-05-29` shows a merge commit bringing upstream/main AND a `Revert "feat: add AWS Bedrock` commit
    - `git grep -i "Bedrock\|Mantle" -- src-tauri/src/settings.rs` returns no active provider push block (Bedrock neutralized)
    - `grep '"version": "0.1.3"' src-tauri/tauri.conf.json` matches (Dictus version preserved, not 0.8.3)
    - `grep 'version = "0.1.3"' src-tauri/Cargo.toml` matches
    - `git diff main -- CLAUDE.md` shows no change (Dictus CLAUDE.md preserved)
    - `test -f AGENTS.md` succeeds (upstream AGENTS.md accepted)
    - `cargo build --manifest-path src-tauri/Cargo.toml` exits 0
    - `git grep -F 'X-Title' -- src-tauri/src/llm_client.rs` still matches "Dictus"
  </acceptance_criteria>
  <done>The 17-commit delta is merged, Bedrock is reverted, Dictus identity and version 0.1.3 are intact, the tree builds, and Cargo.lock was regenerated (never hand-edited).</done>
</task>

<task type="auto">
  <name>Task 2: Update upstream-sha.txt, add Fork Policy + Exclusion Log to UPSTREAM.md, run verify-sync.sh, open PR</name>
  <read_first>
    - UPSTREAM.md (current structure — find where to add the new section; §1 has a stale 4-commit delta table)
    - .github/upstream-sha.txt (the file to update with the new 40-char HEAD SHA)
    - .planning/phases/10-prerequisite-gate/10-CONTEXT.md (Fork Policy decision + Exclusion Log requirement)
  </read_first>
  <action>
    1. Update `.github/upstream-sha.txt` to the full 40-char upstream HEAD SHA captured in Task 1 step 4 (`git rev-parse upstream/main`). Overwrite the old value `fdc8cb712dc247931099359a2d3bc8a413cf33ec`. The file must contain exactly the 40-char SHA (no trailing prose).

    2. Add a new section to `UPSTREAM.md` titled exactly `## Fork Policy: Selective Cherry-Pick`. Content must state:
       - As of 2026-05-29, this fork transitions FROM bulk merges TO selective cherry-pick. Sync #2 (this merge) is the LAST planned bulk catch-up merge.
       - Future upstream deltas are triaged commit-by-commit; only commits aligned with Dictus's local-first direction are cherry-picked.
       - Include an `### Exclusion Log` subsection with a markdown table with columns: `| SHA | PR | Title | Reason Excluded | Date |`.
       - First (and only current) row: `| aee682f | #1288 | feat: add AWS Bedrock (Mantle) provider | Local-first: cloud provider not owned by Dictus; reverted in Sync #2 | 2026-05-29 |`

    3. The old §1 "Known post-v0.8.2 upstream delta (4 commits)" table is now stale (delta is 17 commits). Either update it to reference Sync #2 or leave it with a note `> Superseded by Sync #2 (2026-05-29)` — Claude's discretion. Do not delete the runbook content (§4 hot zones, §5 steps).

    4. Commit the landing changes: `git add .github/upstream-sha.txt UPSTREAM.md && git commit -m "docs(10): sync #2 — update upstream SHA, add Fork Policy + Exclusion Log"`.

    5. Run the identity gate: `bash .github/scripts/verify-sync.sh`. It must exit 0 (all 15 assertions: SYNC-05a..05k, BRAND-01a, BRAND-02a, BRAND-03a, ICON-02a). If any assertion fails, fix the underlying file per the hot-zone rules and re-run — do NOT weaken the script.

    6. Open the PR for CI: `gh pr create --base feat/v1.3-smart-modes --head upstream/sync-2026-05-29 --title "Upstream Sync #2 (PREP-03)"`. PR body MUST include a per-commit triage table (SYNC-A1) covering all 17 commits with Accept/Revert/Keep-Dictus categorization (use the 17-commit table from `<facts>`), and an explicit note: "Commit 966ff99 (async GPU query) flagged forward to Phase 11 — accepted in merge, not adopted." Body formatting is Claude's discretion.
  </action>
  <verify>
    <automated>bash .github/scripts/verify-sync.sh</automated>
  </verify>
  <acceptance_criteria>
    - `.github/upstream-sha.txt` content matches `^[0-9a-f]{40}$` AND is not `fdc8cb712dc247931099359a2d3bc8a413cf33ec`
    - `grep -F '## Fork Policy: Selective Cherry-Pick' UPSTREAM.md` matches
    - `grep -F 'Exclusion Log' UPSTREAM.md` matches
    - `grep -F 'aee682f' UPSTREAM.md` matches (Bedrock exclusion entry present)
    - `bash .github/scripts/verify-sync.sh` exits 0
    - `gh pr view upstream/sync-2026-05-29 --json title` shows the Sync #2 PR exists
  </acceptance_criteria>
  <done>upstream-sha.txt holds the new 40-char HEAD, UPSTREAM.md documents the selective cherry-pick policy with the Bedrock exclusion logged, verify-sync.sh exits 0, and a PR with the 17-commit triage table is open for CI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm all 7 CI platforms green and merge the sync PR</name>
  <action>Human-verify checkpoint: confirm the Sync #2 PR CI matrix is green on all 7 platforms per the steps below, review the per-commit triage and Fork Policy section, then merge the PR. See how-to-verify for exact steps.</action>
  <what-built>
    Upstream Sync #2 merged into branch `upstream/sync-2026-05-29` with Bedrock reverted, Dictus identity preserved, Fork Policy + Exclusion Log added to UPSTREAM.md, verify-sync.sh passing locally, and a PR opened with full 17-commit triage.
  </what-built>
  <how-to-verify>
    1. Open the Sync #2 PR: `gh pr view upstream/sync-2026-05-29 --web`
    2. Confirm the `main-build.yml` CI matrix is green on ALL 7 platforms (STRICT gate — no subset close):
       - macOS ARM64 (macos-26, aarch64-apple-darwin)
       - macOS x64 (macos-latest, x86_64-apple-darwin)
       - Ubuntu 22.04 x64
       - Ubuntu 24.04 x64
       - Ubuntu 24.04 ARM64
       - Windows x64
       - Windows ARM64
    3. Review the per-commit triage table in the PR body for accuracy against `git log fdc8cb712..upstream/main`.
    4. Read the new "Fork Policy: Selective Cherry-Pick" section in UPSTREAM.md — confirm the Bedrock aee682f exclusion entry is present with rationale.
    5. If all 7 platforms are green and the triage/policy read correctly, merge the PR into `feat/v1.3-smart-modes`.
  </how-to-verify>
  <acceptance_criteria>
    - All 7 CI platforms report success in main-build.yml on the Sync #2 PR
    - PR is merged into feat/v1.3-smart-modes
  </acceptance_criteria>
  <resume-signal>Type "approved" once all 7 CI platforms are green and the PR is merged, or describe failures to address.</resume-signal>
</task>

</tasks>

<verification>
- `bash .github/scripts/verify-sync.sh` exits 0 (15 identity assertions)
- `cargo build --manifest-path src-tauri/Cargo.toml` exits 0
- `.github/upstream-sha.txt` is a new 40-char SHA
- `UPSTREAM.md` contains "## Fork Policy: Selective Cherry-Pick" and "aee682f"
- All 7 CI platforms green on main-build.yml
</verification>

<success_criteria>
Upstream Sync #2 is merged with per-commit triage complete; `verify-sync.sh` exits 0; the fork policy documents selective cherry-pick going forward and records the AWS Bedrock `aee682f` exclusion; all 7 CI platforms are green. (PREP-03)
</success_criteria>

<output>
After completion, create `.planning/phases/10-prerequisite-gate/10-01-SUMMARY.md`. Note in the summary: commit 966ff99 (async GPU query) is flagged forward to Phase 11 GPU detection — accepted in the merge but not adopted in Phase 10.
</output>
