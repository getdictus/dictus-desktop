---
milestone: v1.1
name: Auto-Update & Upstream Sync
audited: 2026-04-14T20:51:06Z
status: tech_debt
scores:
  requirements: 15/15
  phases: 2/2
  integration: 3/4 flows complete (Flow A has a post-sync gate gap, non-blocking)
  flows: 3/4
gaps:
  requirements: []
  integration:
    - from: "UPSTREAM.md step 6 (post-sync gate)"
      to: ".planning/phases/04-updater-infrastructure/scripts/validate.sh (UPDT-03, UPDT-05 assertions)"
      issue: "Post-sync gate only runs verify-sync.sh (SYNC-05 identity checks). It does NOT re-assert tauri.conf.json plugins.updater.pubkey (UPDT-03) or bundle.createUpdaterArtifacts (UPDT-05). A future sync conflict resolution that accidentally removes the pubkey or flips createUpdaterArtifacts to false will pass the post-sync gate and ship an unsigned/broken release."
      severity: regression_risk
      affected_requirements: ["UPDT-03", "UPDT-05"]
      fix: "Add `bash .planning/phases/04-updater-infrastructure/scripts/validate.sh` to UPSTREAM.md step 6 alongside verify-sync.sh."
  flows: []
tech_debt:
  - phase: 04-updater-infrastructure
    items:
      - "validate.sh:54-55 — TECH-03 anti-regression grep uses -B5 window; comment is 9 lines above otool call, so script exits 1 regardless of correctness. All 8 substantive UPDT config assertions still pass. (INFO — documented in 04-VERIFICATION.md)"
      - "build.yml:306,321 — blob.handy.computer CDN for onnxruntime download. Tracked as INFR-01 (Dictus CDN migration) in REQUIREMENTS.md Future section."
      - "macOS x86_64 'Verify macOS dylib bundling' CI step cosmetic failure — probes a binary named `handy` pending TECH-03 binary rename. Not a blocker; builds succeed."
  - phase: 05-upstream-sync
    items:
      - "05-VERIFICATION.md body Requirements Coverage table still shows SYNC-02 BLOCKED even though frontmatter gaps_resolved block and the codebase confirm bc576b3 fixed it. Documentation hygiene — update table body to SATISFIED + cross-reference bc576b3."
      - "05-VALIDATION.md frontmatter shows status: draft, nyquist_compliant: false, wave_0_complete: false. Document was never finalized after phase executed green. Run `/gsd:validate-phase 5` to retroactively close Nyquist coverage."
      - "verify-sync.sh:39 SYNC-05f pattern simplifies to match any `\"Dictus\"` string — does not strictly verify the X-Title header. Test-quality issue; currently passes because file content is correct."
nyquist:
  compliant_phases: ["04-updater-infrastructure"]
  partial_phases: ["05-upstream-sync"]
  missing_phases: []
  overall: partial
human_verification_remaining:
  - "Confirm Ed25519 private key is no longer present at ~/.tauri/dictus.key on Pierre's machine (triple-backup to Bitwarden + iCloud age-encrypted)"
  - "Confirm GitHub Secrets TAURI_SIGNING_PRIVATE_KEY and TAURI_SIGNING_PRIVATE_KEY_PASSWORD are set on getdictus/dictus-desktop"
  - "Confirm v0.1.0 release assets all prefixed dictus_ (27 assets, no handy_* artifacts)"
  - "Confirm Windows installer SmartScreen behaviour acceptable for v0.1.0"
  - "Trigger upstream-sync.yml via workflow_dispatch when upstream matches upstream-sha.txt — verify changed=false branch (no issue created)"
  - "Post Sync #2, verify no duplicate issue created on subsequent Monday runs (idempotency regression check)"
---

# Milestone v1.1 — Auto-Update & Upstream Sync — Audit Report

**Audited:** 2026-04-14
**Overall status:** `tech_debt` — all requirements satisfied, no critical blockers, accumulated deferred items and one regression-risk wiring gap flagged for review.

## Executive Summary

Both phases delivered against goals. All 15 v1.1 requirements (UPDT-01..10 + SYNC-01..05) are traced to passed VERIFICATION.md evidence in Phase 4 and Phase 5. The v0.1.0 release was published, signed, and verified live. The first upstream sync merged 4 commits from `cjpais/Handy` onto `main` with identity integrity preserved.

The audit surfaces one **regression-risk wiring gap** (post-sync gate does not cover UPDT-03/UPDT-05 — a future bad merge could silently break the updater), one **Nyquist compliance gap** (Phase 5 VALIDATION.md never finalized), and several **documentation hygiene** items. None block archiving v1.1, but they are worth reviewing before starting v1.2.

---

## Requirements Coverage (3-source cross-reference)

All 15 requirements are **SATISFIED** — confirmed by VERIFICATION.md passed status, SUMMARY.md frontmatter claims, REQUIREMENTS.md traceability `[x]`, and live evidence.

### Auto-Updater (Phase 4)

| REQ | VERIFICATION | SUMMARY claim | REQUIREMENTS | Final |
|-----|--------------|---------------|--------------|-------|
| UPDT-01 | passed (runbook + 04-03 exec) | 04-01, 04-03 | `[x]` | **satisfied** |
| UPDT-02 | passed (runbook + signed v0.1.0) | 04-01, 04-03 | `[x]` | **satisfied** |
| UPDT-03 | passed (pubkey valid base64) | 04-03 | `[x]` | **satisfied** (regression-risk flagged) |
| UPDT-04 | passed (endpoint = getdictus) | 04-03 | `[x]` | **satisfied** |
| UPDT-05 | passed (createUpdaterArtifacts: true) | 04-03 | `[x]` | **satisfied** (regression-risk flagged) |
| UPDT-06 | passed (release.yml:77) | 04-02 | `[x]` | **satisfied** |
| UPDT-07 | passed (build.yml:22) | 04-02 | `[x]` | **satisfied** |
| UPDT-08 | passed (build.yml:371) | 04-02 | `[x]` | **satisfied** |
| UPDT-09 | passed (UpdateChecker.tsx:206) | 04-02 | `[x]` | **satisfied** |
| UPDT-10 | passed (verify-updater-release.sh exit 0) | 04-04 | `[x]` | **satisfied** |

### Upstream Sync (Phase 5)

| REQ | VERIFICATION | SUMMARY claim | REQUIREMENTS | Final |
|-----|--------------|---------------|--------------|-------|
| SYNC-01 | passed (workflow + issue creation wired) | 05-01 | `[x]` | **satisfied** |
| SYNC-02 | passed after re-verify (bc576b3 fix — 40-char SHA + SYNC-05k assertion) | 05-01 | `[x]` | **satisfied** (VERIFICATION body table stale — doc hygiene) |
| SYNC-03 | passed (UPSTREAM.md fork point + runbook) | 05-02 | `[x]` | **satisfied** |
| SYNC-04 | passed (PR #3 merged, 4 commits on main) | 05-03 | `[x]` | **satisfied** |
| SYNC-05 | passed (11-assertion verify-sync.sh green) | 05-01, 05-03 | `[x]` | **satisfied** |

**Orphans:** None. Every REQ-ID in the traceability table has matching VERIFICATION evidence.

---

## Phase Verification Summary

| Phase | Status | Score | Re-verified |
|-------|--------|-------|-------------|
| 04 Updater Infrastructure | passed | 10/10 must-haves | No |
| 05 Upstream Sync | passed | 5/5 must-haves | Yes (SYNC-02 fix in bc576b3) |

Both phases flagged INFO-level anti-patterns but zero blocker anti-patterns.

---

## Cross-Phase Integration

Integration checker analysed 4 flows spanning Phase 4 + Phase 5:

| Flow | Verdict | Notes |
|------|---------|-------|
| A — Sync → release → client update | COMPLETE with gap | Post-sync gate does not re-assert UPDT-03 (pubkey) and UPDT-05 (createUpdaterArtifacts). Future sync could silently break updater. |
| B — Identity integrity across workflows | COMPLETE | UPSTREAM.md §4.1 + conflict table correctly mark tauri.conf.json CRITICAL. SYNC-05f pattern is over-broad but currently passes. |
| C — SYNC-02 idempotency fix wiring | COMPLETE | upstream-sha.txt = 40 chars. SYNC-05k asserts invariant. upstream-sync.yml compares full SHAs. Fix is wired — VERIFICATION.md body table staleness is doc-only. |
| D — SYNC-04 SUMMARY orphan | N/A (false alarm) | 05-03-SUMMARY uses `requirements:` key (not `requirements_completed:`). Lists SYNC-04 + SYNC-05. 4 upstream commits confirmed reachable from `main`. |

---

## Tech Debt Inventory

### Phase 04 — Updater Infrastructure
1. **validate.sh TECH-03 heuristic always fails** — `-B5` grep window too narrow (comment 9 lines above `otool`). All 8 substantive UPDT config assertions still pass; script just exits 1 due to this one anti-regression check. Fix: widen to `-B15` or dedicated grep. (INFO)
2. **`blob.handy.computer` CDN URL** at `build.yml:306,321` for onnxruntime. Tracked as **INFR-01** in REQUIREMENTS.md Future section.
3. **macOS x86_64 `otool` CI check cosmetic failure** — probes `Contents/MacOS/handy` binary pending TECH-03 rename. Builds still succeed.

### Phase 05 — Upstream Sync
1. **`05-VERIFICATION.md` body contradicts its own frontmatter** — Requirements Coverage table still shows `SYNC-02 BLOCKED` even though frontmatter `gaps_resolved`, Observable Truths, and the codebase all confirm the fix. Update body table to SATISFIED with bc576b3 reference.
2. **`05-VALIDATION.md` never finalized** — `status: draft`, `nyquist_compliant: false`, all Wave 0 boxes unchecked even though the phase executed green. Run `/gsd:validate-phase 5` to close the Nyquist gap retroactively.
3. **`verify-sync.sh:39` SYNC-05f pattern over-broad** — regex simplifies to match any occurrence of `"Dictus"`; does not strictly verify the `X-Title` header field. Currently passes because `llm_client.rs` content is correct.

### Cross-Phase — Regression Risk
- **UPSTREAM.md step 6 should also run `validate.sh`.** One-line addition guards UPDT-03 + UPDT-05 against future upstream merge conflict resolutions. Non-blocking for v1.1 (no current regression) — but worth adding before Sync #2.

---

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status | Action |
|-------|---------------|-------------------|-----------------|--------|--------|
| 04 Updater | exists | `true` | `true` | COMPLIANT | none |
| 05 Upstream Sync | exists (draft) | `false` | `false` | PARTIAL | `/gsd:validate-phase 5` |

**Overall:** `partial` — one phase needs retroactive Nyquist close-out.

---

## Human Verification Still Outstanding

Carried forward from phase VERIFICATIONs — none gate milestone archival, but worth tracking:

1. Confirm `~/.tauri/dictus.key` deleted on Pierre's machine (triple-backup confirmed)
2. Confirm GitHub Secrets `TAURI_SIGNING_PRIVATE_KEY*` visible at `getdictus/dictus-desktop/settings/secrets/actions`
3. Confirm v0.1.0 release assets all prefixed `dictus_` (27 assets)
4. Confirm Windows installer SmartScreen behaviour acceptable
5. Trigger `upstream-sync.yml` with upstream matching `upstream-sha.txt` — verify `changed=false` branch
6. Post Sync #2, confirm no duplicate issue on subsequent Monday cron

---

## Verdict

**Status: `tech_debt`** — v1.1 is shippable and every requirement is satisfied with live evidence. No critical blockers. The accumulated items above are worth acknowledging before archiving so they land in v1.2 planning or the project backlog.

_Audited: 2026-04-14_
_Auditor: Claude (gsd orchestrator + gsd-integration-checker)_
